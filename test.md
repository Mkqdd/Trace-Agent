# Trace-Agent 当前阶段功能测试方案

## 目标

当前阶段最重要的不是先搭完整、工程化的测试体系，而是先确认项目的各项核心功能都能正常运行。

所以这份方案只聚焦一件事：

- 验证主链是否能跑通
- 验证各类指标是否都能处理
- 验证关键产物是否正确生成
- 验证 gap 补查和降级逻辑是否真的生效

等这些功能都稳定以后，再补更细的单元测试、集成测试和 CI 门禁。

## 当前最该测的 6 类功能

### 1. Baseline 是否能正常工作

要确认以下几类指标都能被 `plan` 主链正常处理：

- `IP`
- `JA3`
- `JA4`
- `SSL_SHA1`
- `DOMAIN/URL`

检查点：

- 能否进入正确的 investigator
- 本地 MySQL 是否能正常查询
- 外部富化是否能正常执行
- 不会因为单个外部请求失败就整条链路中断

### 2. analysis 是否能正常生成

这是当前最关键的中间产物。

每条样例都要看：

- 是否生成 `analysis.json`
- `assessment.family / confidence / severity` 是否存在
- `evidence[]` 是否有内容
- `findings[]` 是否存在
- gap 存在时 `gaps[]` 是否合理

### 3. report 和 topology 是否能正常生成

每条样例都要确认：

- `report.md` 是否生成
- `topology.json` / `topology.html` 是否生成
- report / topology / analysis 的家族、严重度、置信度是否一致

### 4. React 补查是否真的能触发

当前不是要求 React 一定非常强，而是先确认这条链路是否真的工作。

要验证：

- gap 存在时，是否会进入补查
- React 是否会按受限工具集运行
- 有证据时，supplemental 是否能回写到 analysis
- 没证据时，是否会返回 `unresolved` 而不是胡编

### 5. 降级逻辑是否正常

至少要手工验证这些场景：

- MySQL 不可用
- `VT_API_KEY` 缺失
- `SERPAPI_API_KEY` 缺失
- 网页抓取失败
- 搜索为空

通过标准：

- 主流程仍能跑完
- 能产出 `analysis/report/topology`
- 不会直接崩溃

### 6. 输出质量是否基本合理

当前不追求“完美报告”，但至少要保证：

- 不出现 analysis 里没有的家族/IOC
- 不把内部字段直接暴露到报告中
- 关键证据和最终结论大体匹配
- 中文报告能读，不是明显的调试输出

## 建议的样例集

当前建议先用少量固定样例做功能检查，不必一下子铺很大。

建议至少覆盖：

- `demo_alert.json` 里的现有样例
- `react_gap_probe.json`
- 1 条本地库直接命中的 `JA4` 样例
- 1 条本地库直接命中的 `SSL_SHA1` 样例
- 1 条本地未命中、只能靠外部情报的样例
- 1 条故意缺少外部依赖的降级样例

## 建议的测试顺序

### 第一轮：主链冒烟

目标：先确认“能跑”。

建议做法：

1. 跑 `demo_alert.json`
2. 检查每条输出目录下是否都有：
   - `input_alert.json`
   - `event.json`
   - `analysis.json`
   - `topology.json`
   - `topology.html`
   - `report.md`
3. 抽查 `000-005` 的结果是否基本合理

### 第二轮：类型覆盖

目标：确认不是只有 IP 能跑，其他指纹类型也能工作。

重点看：

- `JA3`
- `JA4`
- `SSL_SHA1`
- `IP`

每种类型至少成功跑 1 条。

### 第三轮：Gap 补查

目标：确认 React 补查链路真的活着。

建议用：

- `react_gap_probe.json`

重点看：

- 是否触发补查
- 是否生成 `supplemental`
- 如果没有新证据，是否明确是 `unresolved`

### 第四轮：降级验证

目标：确认依赖缺失时不会崩。

建议依次测试：

- 不配 VT key
- 不配 SerpAPI key
- 临时断开 MySQL

重点看：

- 主流程能不能继续
- 报告是否还能生成
- 结论是否有适度降级

## 每轮测试的记录方式

建议不要一开始就写复杂自动化框架，先做一份手工检查记录即可。

每条样例记录这几个结果：

- 样例编号
- 指标类型
- 是否跑通
- 是否生成 `analysis/report/topology`
- 是否命中本地情报
- 是否拿到外部情报
- 是否触发 React
- 是否存在明显错误

可以直接整理成一个简单表格：

| case | type | baseline | analysis | report | topology | react | 降级/异常 | 备注 |
|------|------|----------|----------|--------|----------|-------|-----------|------|

## 当前阶段的通过标准

我建议先把通过标准定得务实一点：

- `demo_alert.json` 能完整跑通
- 主流指标类型至少各成功 1 条
- `analysis/report/topology` 能稳定生成
- React 补查至少有 1 条样例能成功触发
- 依赖缺失时主流程不崩
- 报告内容没有明显幻觉或结构性错误

只要这几条成立，就说明项目已经具备进入下一阶段的基础。

## 下一步建议

当前最合理的顺序是：

1. 先按这份方案做功能测试
2. 把功能问题修掉
3. 再从最关键的地方补自动化测试

如果后面开始补自动化，我建议第一个再补的是：

- `analysis` 的结构与结论校验

因为它是现在整个 agent 的中枢。
