# 首页摘要

- **结论**：确认安全事件。
- **严重度**：高。
- **研判把握**：高。
- **已确认范围**：`ws-eng-02` 和 `ws-eng-05`。
- **最强证据**：可疑 TLS 会话、可疑 TLS 信标、PsExec 远程服务创建。
- **关键缺口**：扩线得到的 3 条关联事件尚未完成独立验证。
- **立即动作**：优先隔离或重点监控资产 `ws-eng-02` 和 `ws-eng-05`，排查并封禁外部基础设施 `203.0.113.77` 和 `sync-cdn-notify.net`。
- **一句话结论**：当前只能依据已整理的交付判断和证据包保守生成报告材料。

## 调查起点与已知线索

本次调查始于 2026 年 7 月 14 日 01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`。随后，在 01:10:00 UTC，该工作站命中了指向罕见外部基础设施 `203.0.113.77` 的可疑 TLS 信标。这些初始线索表明，该事件值得进一步调查。

在 01:14:20 UTC，`ws-eng-02` 重新连接到了同一 C2 基础设施。这一系列事件构成了本次调查的入口线索，显示了可疑活动的连续性和潜在威胁。

## 调查范围与研判假设

本次调查的锚点是种子资产 `ws-eng-02`，该资产已被确认为受影响对象。同样，`ws-eng-05` 也被确认为受影响资产。而 `ws-eng-09` 则处于待确认状态，需要进一步独立验证其是否真正受到影响。

核心外部基础设施包括 IP 地址 `203.0.113.77` 和域名 `sync-cdn-notify.net`。这些对象是外联和封禁排查的重点，但不应被视为内部受影响资产。

## 事件过程与行为模式

事件过程始于 2026 年 7 月 14 日 01:04:00 UTC，`ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`。随后，在 01:10:00 UTC，该工作站命中了指向罕见外部基础设施 `203.0.113.77` 的可疑 TLS 信标。在 01:14:20 UTC，`ws-eng-02` 重新连接到了同一 C2 基础设施。

在 01:18:00 UTC，可疑的 `rundll32.exe` 从用户可写路径启动了 loader DLL。紧接着，在 01:21:10 UTC，`ws-eng-02` 到 `ws-eng-05` 发生了 PsExec 远程服务创建，显示出潜在的横向移动迹象。

在 01:32:10 UTC，`ws-eng-05` 解析了一个新的罕见域名 `cdn-notify-edge.net`，这可能是扩线后的候选事件。在 01:39:20 UTC，`ws-eng-05` 到 `ws-eng-09` 发生了 WMI 远程进程创建，进一步显示了潜在的横向移动。

在 01:47:30 UTC，`ws-eng-09` 连接了次级基础设施 `cdn-notify-edge.net / 198.51.100.44`，显示出可能的进一步扩散。然而，由于 EDR 传感器报告了降级的进程遥测数据，无法完全重建第二次信标的执行链路。

## 关键事实与证据判断

关键事实包括 2026 年 7 月 14 日 01:04:00 UTC，`ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`。随后，在 01:10:00 UTC，该工作站命中了指向罕见外部基础设施 `203.0.113.77` 的可疑 TLS 信标。在 01:14:20 UTC，`ws-eng-02` 重新连接到了同一 C2 基础设施。

在 01:18:00 UTC，可疑的 `rundll32.exe` 从用户可写路径启动了 loader DLL。紧接着，在 01:21:10 UTC，`ws-eng-02` 到 `ws-eng-05` 发生了 PsExec 远程服务创建，显示出潜在的横向移动迹象。

在 01:28:40 UTC，`ws-eng-05` 连接了原始信标基础设施 `sync-cdn-notify.net / 203.0.113.77`，进一步支持了横向移动的判断。

归纳判断显示，检索到 6 条偏可疑事件，围绕 `sync-cdn-notify.net`、`203.0.113.77` 和 `10.70.3.45` 展开。扩线检索到 4 条可疑关联事件，围绕 `sync-cdn-notify.net`、`cdn-notify-edge.net` 和 `203.0.113.77` 展开，当前先作为待验证候选线索。

## 关联范围与候选边界

已确认受影响对象包括 `ws-eng-02` 和 `ws-eng-05`。待确认对象包括 `ws-eng-09`，需要进一步独立验证其是否真正受到影响。此外，`ws-lab-02` 也是扩展查询候选对象。

核心外部基础设施包括 IP 地址 `203.0.113.77` 和域名 `sync-cdn-notify.net`。这些对象是外联和封禁排查的重点，但不应被视为内部受影响资产。

候选事件包括 2026 年 7 月 14 日 01:32:10 UTC，`ws-eng-05` 解析了一个新的罕见域名 `cdn-notify-edge.net`。在 01:47:30 UTC，`ws-eng-09` 连接了次级基础设施 `cdn-notify-edge.net / 198.51.100.44`，显示出可能的进一步扩散。然而，由于 EDR 传感器报告了降级的进程遥测数据，无法完全重建第二次信标的执行链路。

## 反证、缺口与结论边界

当前最关键缺口在于扩线得到的 3 条关联事件尚未完成独立验证。仍需显式检查维护窗口、补丁、备份或共享基线等反证。此外，仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。

背景或替代解释事件包括 2026 年 7 月 14 日 01:24:00 UTC，`ws-eng-05` 开始批准的 SCCM 补丁暂存，可能解释部分常规管理活动。在 01:26:00 UTC，`ws-eng-05` 从批准的 Microsoft 端点下载了 Windows 更新内容。在 01:36:00 UTC，`ws-eng-05` 通过 TLS 转向第二个外部 IP。在 01:39:20 UTC，`ws-eng-05` 到 `ws-eng-09` 发生了 WMI 远程进程创建。这些事件只能用于收窄边界，不能单独推翻主链。

在 01:44:10 UTC，`ws-eng-09` 解析了扩线后才出现的次级域名 `cdn-notify-edge.net`。在 01:46:20 UTC，`ws-lab-02` 通过不同供应商域名访问了同一托管 IP，提示该次级 IP 可能是共享基础设施。这些解释只能用于收窄边界，不能单独推翻主链。

## 结论与后续动作

最终结论是确认安全事件。当前证据足以支撑事件成立，但仍有关键缺口需要进一步验证。立即动作包括优先隔离或重点监控资产 `ws-eng-02` 和 `ws-eng-05`，以及在边界和代理设备上排查并封禁外部基础设施 `203.0.113.77` 和 `sync-cdn-notify.net`。

短期动作包括对扩线得到的关联事件逐条补做独立验证，确认它们是否也命中本地指纹、IOC 或外部情报。长期动作包括以种子指标和事件簇中的域名/IP 为关键关联指标，继续检索同时间窗内的重复通信。

技术细节、IOC/IOA、关键对象清单、观测引用、证据细目和待补缺口详见技术附录。