# 首页摘要

- **结论**：可疑事件，建议继续复核。
- **严重度**：中
- **研判把握**：中
- **已确认范围**：`ws-eng-02` 和 `ws-eng-05`
- **最强证据**：`ws-eng-02` 在 `2026-07-14 01:10:00 UTC` 发生了可疑的 TLS 会话，并且 `rundll32.exe` 在 `2026-07-14 01:18:00 UTC` 从用户可写的路径启动了一个加载器 DLL。
- **关键缺口**：仍需围绕当前关键关联指标做一次显式扩线，确认是否存在同指标的更大范围复现。
- **立即动作**：对重点资产补采主机侧日志，确认是否存在执行、持久化或横向移动证据。
- **一句话结论**：当前只能依据已整理的交付判断和证据包保守生成报告材料。

## 调查起点与已知线索

可疑事件，建议继续复核；事件簇已经从单点异常推进到高风险阶段或保守收敛后的多资产范围，足以支撑事件成立。种子告警自带家族/工具提示：Suspected multi-host beacon spread。已观察到关联资产或共享基础设施上的扩展信号，但尚需继续确认是否属于同一事件。交付门槛尚未满足：仍有 5 条候选扩线事件没有完成独立验证。尚未执行显式反证检查。仍缺少情报或结构化整理动作，报告材料尚未收束。仍需围绕当前关键关联指标做一次显式扩线，确认是否存在同指标的更大范围复现。

2026-07-14 01:04:00 UTC，资产 `ws-eng-02` 关联对象 `sync-cdn-notify.net`，在可疑的 TLS 会话之前解析了一个罕见的域名。这一事实表明了可疑活动的时间线，但需要结合其他证据才能得出更强的结论。

2026-07-14 01:10:00 UTC，资产 `ws-eng-02` 关联对象 `sync-cdn-notify.net / 203.0.113.77`，种子 JA4 警报匹配到了可疑的 TLS 信标到罕见的外部基础设施。这一事实作为主支撑事件，提供了关键的证据支持当前的判断。

2026-07-14 01:14:20 UTC，资产 `ws-eng-02` 关联对象 `sync-cdn-notify.net / 203.0.113.77`，种子工作站重新连接到了相同的 C2 基础设施。这一事实进一步支持了可疑活动的时间线，但同样需要结合其他证据才能得出更强的结论。

## 调查范围与研判假设

对象 `ws-eng-02` 是种子资产，种子告警首先落在该资产上。当前已纳入主证据链的资产，事件中出现过的资产对象。对象 `ws-eng-05` 也是已确认受影响资产，当前已纳入主证据链的资产。事件中出现过的资产对象。对象 `ws-eng-09` 是待确认关联资产，当前仍需独立验证是否真正受影响。事件中出现过的资产对象。对象 `203.0.113.77` 是核心外部基础设施，当前主证据链中反复出现的关键对象。当前关键关联指标里出现的扩线候选地址。

这些已确认和待确认的对象以及核心外部基础设施，构成了当前调查的范围和研判假设。已确认对象提供了明确的证据支持，而待确认对象则需要进一步验证以确认其是否真正受到影响。核心外部基础设施作为关联基础设施，需要进行封禁排查。

## 事件过程与行为模式

2026-07-14 01:04:00 UTC，资产 `ws-eng-02` 关联对象 `sync-cdn-notify.net`，在可疑的 TLS 会话之前解析了一个罕见的域名。这一事实表明了可疑活动的时间线，但需要结合其他证据才能得出更强的结论。

2026-07-14 01:10:00 UTC，资产 `ws-eng-02` 关联对象 `sync-cdn-notify.net / 203.0.113.77`，种子 JA4 警报匹配到了可疑的 TLS 信标到罕见的外部基础设施。这一事实作为主支撑事件，提供了关键的证据支持当前的判断。

2026-07-14 01:14:20 UTC，资产 `ws-eng-02` 关联对象 `sync-cdn-notify.net / 203.0.113.77`，种子工作站重新连接到了相同的 C2 基础设施。这一事实进一步支持了可疑活动的时间线，但同样需要结合其他证据才能得出更强的结论。

2026-07-14 01:18:00 UTC，资产 `ws-eng-02`，可疑的 `rundll32.exe` 从用户可写的路径启动了一个加载器 DLL。这一事实作为可疑执行线索，提供了进一步的证据支持当前的判断，但需要结合其他证据才能得出更强的结论。

这些事件推进节点形成了可疑活动的行为模式，但需要进一步的证据来确认其性质和范围。

## 关键事实与证据判断

2026-07-14 01:04:00 UTC，资产 `ws-eng-02` 关联对象 `sync-cdn-notify.net`，在可疑的 TLS 会话之前解析了一个罕见的域名。这一事实表明了可疑活动的时间线，但需要结合其他证据才能得出更强的结论。

2026-07-14 01:10:00 UTC，资产 `ws-eng-02` 关联对象 `sync-cdn-notify.net / 203.0.113.77`，种子 JA4 警报匹配到了可疑的 TLS 信标到罕见的外部基础设施。这一事实作为主支撑事件，提供了关键的证据支持当前的判断。

2026-07-14 01:14:20 UTC，资产 `ws-eng-02` 关联对象 `sync-cdn-notify.net / 203.0.113.77`，种子工作站重新连接到了相同的 C2 基础设施。这一事实进一步支持了可疑活动的时间线，但同样需要结合其他证据才能得出更强的结论。

2026-07-14 01:18:00 UTC，资产 `ws-eng-02`，可疑的 `rundll32.exe` 从用户可写的路径启动了一个加载器 DLL。这一事实作为可疑执行线索，提供了进一步的证据支持当前的判断，但需要结合其他证据才能得出更强的结论。

这些判断支撑事件共同构成了当前事件的主要证据基础，但需要进一步的证据来确认其性质和范围。

## 关联范围与候选边界

对象 `ws-eng-02` 是种子资产，种子告警首先落在该资产上。

当前已纳入主证据链的资产，事件中出现过的资产对象。

对象 `ws-eng-05` 也是已确认受影响资产，当前已纳入主证据链的资产。 事件中出现过的资产对象。 对象 `ws-eng-09` 是待确认关联资产，当前仍需独立验证是否真正受影响。 事件中出现过的资产对象。 对象 `203.0.113.77` 是核心外部基础设施，当前主证据链中反复出现的关键对象。 当前关键关联指标里出现的扩线候选地址。 这些已确认和待确认的对象以及核心外部基础设施，构成了当前调查的范围和研判假设。 已确认对象提供了明确的证据支持，而待确认对象则需要进一步验证以确认其是否真正受到影响。 核心外部基础设施作为关联基础设施，需要进行封禁排查。

## 反证、缺口与结论边界

仍需围绕当前关键关联指标做一次显式扩线，确认是否存在同指标的更大范围复现。

扩线得到的 5 条关联事件尚未完成独立验证，是否应并入主事件范围？

仍需显式检查维护窗口、补丁、备份或共享基线等反证。 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。 这些未闭合的缺口限制了更强结论或范围扩展，但不等于否定当前已支撑的事实。 需要进一步的证据和行动来确认这些缺口。

## 结论与后续动作

可疑事件，建议继续复核；事件簇已经从单点异常推进到高风险阶段或保守收敛后的多资产范围，足以支撑事件成立。种子告警自带家族/工具提示：Suspected multi-host beacon spread。已观察到关联资产或共享基础设施上的扩展信号，但尚需继续确认是否属于同一事件。交付门槛尚未满足：仍有 5 条候选扩线事件没有完成独立验证。尚未执行显式反证检查。仍缺少情报或结构化整理动作，报告材料尚未收束。仍需围绕当前关键关联指标做一次显式扩线，确认是否存在同指标的更大范围复现。

对重点资产补采主机侧日志，确认是否存在执行、持久化或横向移动证据。继续围绕相同域名、目标 IP 和 JA4 通信指纹搜索更宽时间窗内的关联事件。优先核验新扩出的关联事件是否存在独立命中，不要只因为共享基础设施就直接并入主证据链。

以上行动建议对应当前证据和边界，旨在进一步确认和扩展当前的调查范围。

技术细节、IOC/IOA、关键对象清单、观测引用、证据细目和待补缺口详见技术附录。