# 首页摘要
- **结论**：确认安全事件
- **严重度**：高危
- **研判把握**：高把握（置信度 94%）
- **已确认范围**：已确认受影响资产 `ws-eng-02`、`ws-eng-05`，均出现指向同一外部基础设施 `sync-cdn-notify.net` / `203.0.113.77` 的信标行为，且 `ws-eng-02` 在信标后出现可疑执行与横向移动行为。
- **一句话结论**：种子告警 `Suspected multi-host beacon spread` 在 `ws-eng-02` 触发后，同源资产在约 17 分钟内完成“解析-信标-重连-可疑执行-横向移动”，且横向目标 `ws-eng-05` 随后向同一基础设施发起信标，形成主线闭环。
- **立即动作**：优先隔离 `ws-eng-02` 和 `ws-eng-05`，在边界和代理设备上排查并封禁 `sync-cdn-notify.net` 与 `203.0.113.77`，并以种子指标为 关键关联指标 扩线检索同时间窗内重复通信。

## 1. 事件结论与当前判断
本次调查结论为确认安全事件。研判依据基于一个由 6 条偏可疑事件构成的时间线簇，这些事件紧密围绕域名 `sync-cdn-notify.net`、外部 IP `203.0.113.77` 和内网 IP `10.70.3.45` 展开。种子告警 `Suspected multi-host beacon spread` 在资产 `ws-eng-02` 上命中 JA4 指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc`，构成了事件起点，随后的 DNS 解析、TLS 重连、可疑进程执行与 PsExec 横向移动接续发生，最终在横向目标资产 `ws-eng-05` 上观察到对同一信标基础设施的回连。这条主线证据链已满足“确认安全事件”的交付门槛：事件簇已从单点异常演进为可解释的、带有执行和位移特征的多资产行为序列。

当前判断聚焦于已确认的可信锚点--`ws-eng-02` 和 `ws-eng-05` 均出现对外部信标基础设施的受控式通信模式，且 `ws-eng-02` 存在可疑执行行为。审查范围内未出现直接反证，但若干未闭合缺口和待核查线索使本报告不能将该事件自动等同于“攻击者完全控制”、“完整的可疑执行活动链”或“长期持久化”。这些边界将在第 5 节中详细梳理。

## 2. 事件过程与关键时间线
分析窗口为 **2026-07-14 01:04:00 UTC 至 2026-07-14 01:28:40 UTC**，已确认主链由以下关键事件构成。事件始于 2026-07-14 01:04:00 UTC，`ws-eng-02`（源地址 `10.70.3.12`）解析了稀有域名 `sync-cdn-notify.net`，这一 DNS 请求出现在可疑 TLS 会话之前，成为序列的早期信号。紧接着在 01:10:00 UTC，同一资产触发种子告警：JA4 指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc` 命中了指向 `sync-cdn-notify.net` / `203.0.113.77` 的 TLS 信标，该通信被标记为信标活动。告警之后仅约 4 分钟，01:14:20 UTC 时 `ws-eng-02` 再次连接相同的外部基础设施，展现出典型的重连与保持信道的行为。

信标序列之后，`ws-eng-02` 上出现了可疑执行事件。01:18:00 UTC，`rundll32.exe` 从用户可写路径加载了 DLL 加载线索，这一进程执行事件与信标活动在时间上紧密相邻。随后在 01:21:10 UTC，`ws-eng-02` 通过 PsExec 向另一内网地址 `10.70.3.45`（对应资产 `ws-eng-05`）发起了远程服务创建，构成横向移动线索。横向移动发生约 7 分钟后，01:28:40 UTC，`ws-eng-05`（源地址 `10.70.3.45`）开始主动联系同一信标基础设施 `sync-cdn-notify.net` / `203.0.113.77`，表现为与原始种子告警一致的可疑流和信标特征。这一闭环体现了“种子资产信标→执行→横向→新资产信标”的显著模式。

此外，候选扩线提示内部 IP `10.70.3.45` 和 JA4 指纹本身可能存在超出当前两条已确认资产的额外通信，但相关迹象仍属待验证状态，尚未纳入已确认范围。

## 3. 关键证据判断
本事件被判定为确认安全事件，在于证据链条不是孤立的单点告警，而是一组时间递进、目的性显著的相关事件。种子告警自带家族提示 `Suspected multi-host beacon spread`，为整个事件提供了方向性解释，但本报告的判断并不依赖这一提示作为强归因，而是基于已确认主链的内在逻辑：`ws-eng-02` 先解析稀有域名，随即在同一域名和外部 IP 上建立信标并在告警后重连，说明这不是瞬时或偶发的单次连接，而是具有持续回连意图的通信。更关键的是，在信标重连之后立即出现 `rundll32.exe` 从用户可写路径加载 DLL 的可疑执行，提供了命令与控制活动之外的主机侧执行线索，加强了“信标不止于通信”的推断。此后通过 PsExec 向 `ws-eng-05` 发起远程服务创建，将行为从单一资产扩展到第二个资产，而 `ws-eng-05` 在显著短的时间间隔内向同一信标基础设施发起连接，构成了横向移动与信标扩散的行为闭环。这种时间、对象和网络目标的高度一致性，使当前主链足以支撑事件成立。

但本报告必须指出，当前证据强度的上限受到若干因素限制。执行链细节（如 `rundll32.exe` 的具体命令行参数、加载的 DLL 文件路径、落地文件哈希、父子进程关系及内存取证证据）尚不完整，因此只能定性为可疑执行线索，不能据此确认完整的载荷执行闭环或恶意软件家族归属。横向移动虽通过 PsExec 远程服务创建发生，但目前未获得目标主机 `ws-eng-05` 上的落地文件、持久化机制或后续执行证据，因此不能将 `ws-eng-05` 直接表述为已被完整控制的节点。同理，未发现持久化机制或长期驻留证据，因此本报告不能将事件描述为已观察到获得长期驻留能力。这些限制并不否定当前的事件级结论，但明确划定了结论不延伸到的区域。

## 4. 影响范围、候选对象与外部基础设施
已确认受影响资产为 `ws-eng-02` 和 `ws-eng-05`。其中 `ws-eng-02` 是种子告警的触发资产，同时出现 DNS 解析、信标、重连、可疑执行和 PsExec 横向移动多重行为，已将其纳入确认受影响范围，需要优先隔离和取证。`ws-eng-05` 作为横向移动的直接目标，并在接收到远程服务创建后短时间内向同一外部信标基础设施发起回连，其行为模式与 `ws-eng-02` 早期信标高度一致，因此也被纳入确认受影响范围，需要进行隔离和进一步的主机取证。当前证据尚不足以将这两台资产标记为完整的攻击链控制节点，但已确认其存在需要响应的事件级行为关联。

属于候选范围但尚未确认的扩线对象包括 JA4 指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc` 和内网 IP `10.70.3.45`。该指纹是种子告警命中的关键通信特征，若在更广时间窗或更大资产范围内出现，可能揭示更多受影响的内部资产，但目前仅将该指纹视为待验证的扩线候选查询指标，不可直接写成已确认传播或已确认受影响。内网 IP `10.70.3.45` 作为当前已确认受影响资产 `ws-eng-05` 的关联地址，同样属于候选扩线范围，需进一步核查其在其他事件中的出现情况。

事件涉及的核心外部基础设施为域名 `sync-cdn-notify.net` 和外部 IP `203.0.113.77`。两者在时间线中反复出现，是所有信标和重连行为的目的地，必须作为外联对象、关联基础设施进行排查和封禁。需要特别强调的是，外部基础设施不能被视为内部受影响资产，其在报告中的定位是排查封禁对象和后续威胁情报关联分析的基础，而非被攻击者控制的企业内部资源。关联内部地址 `10.70.3.12` 是 `ws-eng-02` 的源地址，属于事件中出现的共享或背景网络信息，用于辅助定位和日志检索。

## 5. 反证、替代解释与未闭合缺口
审查过程中尚未发现能推翻当前事件结论的直接反证。当前主链证据稳定，事件之间的时间顺序、对象关联和网络目标具有内在一致性，没有观测到与信标或横向移动明显矛盾的正常业务操作日志。尽管如此，若干未闭合缺口必须被记录为结论边界的组成部分，它们限制了对更强断言的支持能力。

首先，反向解释和共享基础设施替代方案在当前证据集合中缺乏支持事实。例如，假设 `sync-cdn-notify.net` 或 `203.0.113.77` 是内容分发网络或正常共享服务，则需要解释为什么两个内部工作站在短时间内以信标式节奏连接这一稀有域名，并与可疑执行和横向移动时点重合。当前没有维护窗口、补丁部署或共享管理基线记录来解释 `rundll32.exe` 从用户可写路径加载 DLL 和 PsExec 远程服务创建，因此这些行为无法被归入已知的正常运维操作。这意味着“良性背景或共享基础设施”这一替代解释在当前窗口内缺乏有效支撑。

其次，几个关键缺口限制了结论的强度。虽然调查已围绕种子告警补足了最小上下文并识别了关键资产，但更大范围的扩线检索仍未完成，因此不能确认是否仅有 `ws-eng-02` 和 `ws-eng-05` 受影响，也无法排除其他资产通过同一 JA4 指纹或同一外部基础设施产生信标。同样，关于 seed 家族、指纹和外部基础设施的公开技术背景尚未系统收集，导致当前未能完全排除该基础设施是被入侵的中继节点、共享代理或被误用的合法服务等可能性，这会限制更高置信度的基础设施级归因。此外，主机侧的执行细节、持久化证据和横向移动后的资产行为仍未完整补证，这些缺口共同导致本报告不能做出“攻击者完整控制工作站”、“恶意软件全链条执行”或“已建立持久化驻留”等更强判断，但并不削弱当前已确认的事件级交付结论。

## 6. 处置建议与后续核查
- **优先隔离并取证已确认资产**：对象为 `ws-eng-02` 和 `ws-eng-05`。日志源包括终端进程日志（如 Sysmon Event ID 1、7、11）、服务创建日志（如 Windows Event ID 7045）、DNS 查询日志及网络流日志，重点核查 `rundll32.exe` 的命令行、父进程链、DLL 落地路径、PsExec 服务名称及创建后进程，并通过文件哈希和内存取证确认是否有已知恶意载荷或持久化机制。
- **排查并封禁外部基础设施**：对象为 `sync-cdn-notify.net` 和 `203.0.113.77`。日志源为全边界防火墙、Web 代理和 DNS 递归解析器日志，验证目标为回溯本窗口及更早时间范围内所有内网资产对这些基础设施的通信，评估是否需要扩大封禁范围并提取关联域名/IP 以纳入威胁情报监控。
- **以种子指标扩线检索重复通信**：对象为 JA4 指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc`、域名 `sync-cdn-notify.net`、外部 IP `203.0.113.77` 及横向移动涉及的内网 IP `10.70.3.45`。日志源为 TLS 检查日志、网络流日志和 EDR 遥测，验证目标为确认是否存在除 `ws-eng-02` 和 `ws-eng-05` 以外的其他内部资产在相同或相近时间窗内命中相同指纹或通信目标，以评估事件真实扩散范围。
- **补充主机侧执行与横向移动细节**：对象为 `ws-eng-02` 和 `ws-eng-05`。日志源为终端进程创建、文件读写、注册表修改和服务管理日志，验证目标为补全 `rundll32.exe` 的加载链、PsExec 远程服务创建的具体二进制与路径，以及 `ws-eng-05` 在信标前是否产生了任何执行或持久化线索，从而支持对攻击者活动深度的进一步评估。

---

# 事件调查技术附录

以下内容主要供分析、复盘和技术排查使用，不直接替代主报告结论。

## 1. IOC / IOA 清单

| 类型 | 值 | 角色 | 状态 |
| -- | -- | -- | -- |
| IP | 203.0.113.77 | 核心外部基础设施 | 已确认 |
| 域名 | sync-cdn-notify.net | 核心外部基础设施 | 已确认 |
| 指纹 | t13d1516h2_2ba4190accc0_13fe129f5abc | 扩展查询候选 | 待确认 |
| 内网 IP | 10.70.3.12 | 关联内部地址 | 已确认 |
| 内网 IP | 10.70.3.45 | 关联内部地址 | 已确认 |

## 2. 关键对象清单

| 对象 | 类型 | 当前角色 | 是否已纳入证据链 | 备注 |
| -- | -- | -- | -- | -- |
| ws-eng-02 | 资产 | 种子资产 | 是 | 种子告警首先落在该资产上。；当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| ws-eng-05 | 资产 | 已确认受影响资产 | 是 | 当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| 203.0.113.77 | IP | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选地址。 |
| sync-cdn-notify.net | 域名 | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选域名。 |
| t13d1516h2_2ba4190accc0_13fe129f5abc | JA4 | 种子命中指纹 | 是 | 种子告警命中的关键通信指纹。 |
| Suspected multi-host beacon spread | 家族提示 | 家族/工具提示 | 是 | 辅助解释本案的家族或工具背景，不直接等于强归因。 |
| 10.70.3.12 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.70.3.45 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。；当前 pivot 里出现的扩线候选地址。 |
| t13d1516h2_2ba4190accc0_13fe129f5abc | 指纹 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选指纹。 |

## 3. 关键证据细目

### 证据 E-01
- **证据类型**：主支撑证据
- **证据类别**：调查触发证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:10:00 UTC，资产 `ws-eng-02` 围绕 203.0.113.77 / sync-cdn-notify.net 的异常通信触发了种子告警。
- **为什么重要**：这是种子命中的直接落点，说明调查起点并不是孤立的指纹命中。
- **边界与限制**：单条种子命中本身不能独立证明事件成立，仍需要结合重复通信、资产背景或主机侧线索共同定性。
- **主要依据来源**：种子事件窗观测
- **观测引用**：`obs-001`

### 证据 E-02
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:28:40 UTC，资产 `ws-eng-05` 再次与 203.0.113.77 / sync-cdn-notify.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测
- **观测引用**：`obs-001`

### 证据 E-03
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:04:00 UTC，资产 `ws-eng-02` 先解析了少见域名 `sync-cdn-notify.net`，为后续与同一基础设施的通信提供了准备。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测
- **观测引用**：`obs-001`

### 证据 E-04
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（主机）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:18:00 UTC，资产 `ws-eng-02` 在异常通信后出现 `rundll32.exe` 可疑启动，伴随 DLL 加载，路径位于用户可写目录。
- **为什么重要**：它把异常从通信层推进到主机执行或其他更高风险阶段，对事件定性的权重明显更高。
- **边界与限制**：当前仍缺少更细的命令行、落地文件、持久化或内存证据，因此可以确认存在执行线索，但还不能完整描述具体载荷。
- **主要依据来源**：种子事件窗观测
- **观测引用**：`obs-001`

### 证据 E-05
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:21:10 UTC，资产 `ws-eng-02` 出现指向 `10.70.3.45` 的远程服务创建，说明异常已经开始跨主机推进。
- **为什么重要**：它表明异常已经从外联推进到跨主机动作，比单纯重复通信更能支撑事件升级。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测
- **观测引用**：`obs-001`

### 证据 E-06
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:28:40 UTC，资产 `ws-eng-05` 再次与 203.0.113.77 / sync-cdn-notify.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测
- **观测引用**：`obs-001`

## 4. 观测引用对照

| 观测编号 | 来源工具 | 事实摘要 | 关系 |
| -- | -- | -- | -- |
| `obs-001` | `search_seed_context` | 检索到 6 条偏可疑事件，围绕 sync-cdn-notify.net、203.0.113.77、10.70.3.45 展开。 | `supporting` |

## 5. 正文章节引用映射

| 正文章节 | 主要 evidence / observation | 用途 |
| -- | -- | -- |
| 首页摘要 | E-01、E-02、expand_cluster_scope | 当前状态 / 当前最强证据 / 当前最关键缺口 |
| 第 5 节 | E-01、E-02、E-03、E-04、E-05、E-06 | 主支撑证据 / 反证与替代解释 |
| 第 9 节 | expand_cluster_scope、check_counterevidence、structure_evidence | 证据链摘要 / 缺口说明 |
| 第 10 节 | E-01、E-02 | 结论 / 动作 / 复核重点 |

## 6. 证据来源与参考资料

| 来源类型 | 来源名称 | 用途 | 边界 |
| -- | -- | -- | -- |
| 上游检测 | t13d1516h2_2ba4190accc0_13fe129f5abc / Suspected multi-host beacon spread | 提供本案的调查起点与初始命中线索。 | 命中本身并不自动等于事件成立，仍需要后续本地事实补强。 |
| 内部观测 | trace_store.seed_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |

## 7. 证据缺口与待补查询

| 缺口 | 限制的结论 | 当前是否可补 | 最值得补的查询 |
| -- | -- | -- | -- |
| 仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。 | 当前仍缺少对“仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。”的直接补证。 | 是 | 仍需围绕当前关键关联指标开展一轮扩线核查，确认是否存在同指标的更大范围复现。 |
| 补充 seed 家族、指纹或外部基础设施的公开技术背景，明确它只能作为背景还是能支撑基础设施解释。 | 当前不适合仅凭背景情报完成基础设施或家族级强归因。 | 是 | 补充 seed 家族、指纹或外部基础设施的公开技术背景，明确它只能作为背景还是能支撑基础设施解释。 |
| 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 | 当前不能完全排除维护窗口、共享基础设施或正常组件造成的替代解释。 | 是 | 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 |
| 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。 | 当前仍缺少对“仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。”的直接补证。 | 是 | 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。 |
