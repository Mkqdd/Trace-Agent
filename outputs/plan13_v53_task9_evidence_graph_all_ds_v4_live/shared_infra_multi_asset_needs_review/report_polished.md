# 首页摘要
- **结论:** 可疑事件，建议继续复核
- **严重度:** 中危
- **研判把握:** 较高（置信度 70%）
- **已确认范围:** 种子资产 `ws-legal-03` 对共享基础设施 `198.51.100.131` 上的 `auth-queue-sync.net` 产生持续性可疑 TLS 信标，已纳入确认受影响范围
- **一句话结论:** `ws-legal-03` 出现排除共享基础设施和业务背景后仍需解释的可疑信标行为，当前缺少主机侧执行证据且关联资产待验证，不足以交付最终结论
- **立即动作:** 对 `ws-legal-03` 补采主机侧日志，并围绕同域名、目标 IP 和 JA4 指纹在更宽时间窗内扩线搜索关联事件

## 1. 事件结论与当前判断
本次调查围绕种子告警“Suspicious shared-infra beacon requiring review”展开。告警源 IP `10.44.1.18` 对应的资产 `ws-legal-03`，在 2026-06-20 10:12:00 UTC 与域名 `auth-queue-sync.net`（解析至 `198.51.100.131`）建立了一次异常的 TLS 会话，命中 JA4 指纹 `t13d190900_7711aa22bb33_9911ccddee77`。在告警发生前后，同一资产又向相同的域名和 IP 发起了新的 TLS 连接，使该行为具有延续性。

当前证据已形成一个围绕 `ws-legal-03`、`auth-queue-sync.net` 和 `198.51.100.131` 的可疑事件簇。然而，由于缺少主机侧执行、持久化或横向移动证据，加之关联资产 `ws-legal-09` 与 `ws-legal-12` 的受影响状态尚未验证，以及存在共享基础设施和业务背景的替代解释，目前只能将事件定性为需要人工复核的可疑事件。确定更强结论（如已观察到控制主机、已完成横向移动闭环）的证据基础尚不具备。

## 2. 事件过程与关键时间线
2026-06-20 10:12:00 UTC，资产 `ws-legal-03`（源 IP `10.44.1.18`）发出的 TLS 连接命中种子告警，目标为域名 `auth-queue-sync.net` 对应的共享基础设施 IP `198.51.100.131`，JA4 指纹记录为 `t13d190900_7711aa22bb33_9911ccddee77`。该行为被标记为异常信标。随后在 10:16:20 UTC，同一资产再次向同一目标发起 TLS 会话，被标记为可疑 C2 信标（command-and-control 和 beacon 标签）。10:21:40 UTC，发生了第三条短 TLS 会话，复用相同的域名和相似的共享托管路径。这三个连续事件构成了当前已确认的主线推进。

在候选扩展方面，有两个待验证方向需要关注。首先，资产 `ws-legal-12`（源 IP `10.44.1.42`）在 10:49:30 UTC 通过不同业务域名 `legal-docs-cdn.example` 接触了相同的 IP `198.51.100.131`，暗示该 IP 存在共享托管特征。其次，`ws-legal-03` 在 11:08:30 UTC 进行了一次 DNS 查询，解析了另一个罕见的队列主题域名 `login-queue-cdn.net`，并在 11:11:00 UTC 与该域名（解析至 `203.0.113.131`）建立了一条未经主机证实的 TLS 连接。这些信号目前仅为待验证线索，不能直接认定为事件影响范围的扩大。

## 3. 关键证据判断
当前结论认为 `ws-legal-03` 存在需要进一步解释的可疑行为，主要基于已确认主链中的三个反复事实。其一，`ws-legal-03` 反复主动连接至一个罕见且共享托管的域名 `auth-queue-sync.net`，其行为特征偏离了常规业务访问模式。其二，这三次 TLS 会话均集中在种子告警的紧密时间窗口内，其后又触发了同主题的后续 DNS 和连接活动，使“孤立误报”的解释力度减弱。其三，行为与 C2 信标的典型模式（周期性、短会话、非标准指纹）具有一定相似性，但并未完成完整的自动化确认流程。

然而，多个关键缺口限制了更强结论的成立。资产 `ws-legal-03` 在相关窗口内缺失命令行日志遥测，使得我们无法确认任何主机侧执行、脚本加载、持久化机制或内存痕迹。因此，当前只能将事件定性为网络信标层面的可疑事件，而不能推断攻击代码已在主机上成功执行或已观察到获得控制权。任何关于“已观察到控制该节点”、“恶意软件已完整运行”的论断，在当前证据下都是过头结论。

## 4. 影响范围、候选对象与外部基础设施
当前已确认受影响范围仅包含资产 `ws-legal-03`，其 IP 为 `10.44.1.18`。该资产是种子告警的源端，也是全部三条已确认可疑 TLS 会话和后续 DNS 解析的主体。所有已确认的恶意度最高的指标均与此资产绑定，因此应作为现阶段隔离调查和主机取证的首要对象，并纳入确认受影响范围。

在此之外，有两类候选对象不能直接认定为已影响范围。第一类是资产 `ws-legal-09` 与 `ws-legal-12`，两者均通过不同业务域名接触到共享 IP `198.51.100.131`，但这可能是共享基础设施背景的副作用。第二类是与本次事件直接交互的外部基础设施，涵盖核心对象 `198.51.100.131` 和域名 `auth-queue-sync.net`，以及后续解析出现的 `login-queue-cdn.net` 和 `203.0.113.131`。这些外部对象是排查封禁和外联对象分析的重点，但不能被误写为受影响资产。此外，`login.microsoftonline.com` 和 `identity-cache.vendor.example` 等域名，在当前时间线中仅作为正常业务背景出现，用于解释部分流量与 SSO 浏览器工作流重叠的背景事实。

## 5. 反证、替代解释与未闭合缺口
一系列候选背景事件和边界信号为本次可疑主链提供了替代解释的线索，这些线索限制了过度归因的可能，但尚不能单独推翻已确认主链的疑点。资产 `ws-legal-09` 在 10:46:10 UTC 使用了同一个共享 IP `198.51.100.131` 上得到授权的 `identity-cache.vendor.example` 域名，随后被动基础设施上下文显示该 IP 在同一周内托管了多个无关客户域名，强化了 `198.51.100.131` 作为大规模 CDN 或共享托管平台的属性。此外，资产 `ws-legal-03` 在首次可疑会话的时间段内，恰好有一个经批准的 SSO 浏览器扩展更新窗口，并且其后出现了与 `login.microsoftonline.com`（IP `20.190.160.22`）的正常身份认证流量。这些背景提供了“浏览器在维护窗口内通过共享 IP 建立非标准但合法的 CAS/CDN 连接”的替代解释。

然而，多个未闭合缺口使上述替代解释仍不足以让事件降级为无害。主机侧执行证据的缺失是最大的边界限制，导致我们无法通过进程树、命令行审计或映像加载事件来确认网络行为的前置触发来源。同时，`ws-legal-03` 稍后解析并连接与 `auth-queue-sync.net` 同主题的 `login-queue-cdn.net` 这一现象，并未得到共享基础设施的强力解释。关联资产 `ws-legal-09` 和 `ws-legal-12` 缺乏 EDR 覆盖，无法排除其被渗透的风险。最后，种子告警附带的家族提示“Suspicious shared-infra beacon requiring review”目前缺乏额外外部情报的验证，使得本次信标是否为已知工具或签名库产物的判断仍处于信息真空中。

## 6. 处置建议与后续核查
- **对 `ws-legal-03` 补采主机侧日志** 重点关注 当前调查窗口内的进程创建事件（Sysmon EID 1）、网络连接事件（EID 3）与文件写入事件（EID 11）。验证目标：确认外联至 `auth-queue-sync.net` 的 TLS 连接的源进程、命令行参数及父进程链，判断是否存在脚本执行、可疑二进制加载或持久化痕迹。
- **围绕关键 IoC 进行更宽时间窗扩线** 使用 SIEM 或日志检索对同域名 `auth-queue-sync.net`、目标 IP `198.51.100.131` 和 JA4 指纹 `t13d190900_7711aa22bb33_9911ccddee77` 进行至少 7 天回溯搜索。验证目标：确认环境中是否还有其他资产存在同样特征的外联，排除孤点告警或发现更大的受影响面。
- **将候选关联资产纳入复核清单** 对 `ws-legal-09` 和 `ws-legal-12` 执行定向核查，重点调取其网络流连接日志和 DNS 日志。验证目标：区分这两个资产接触 `198.51.100.131` 的行为是否完全符合业务基线（如合法 CAS/CDN 访问），还是存在与 `ws-legal-03` 同模式的可疑信标活跃度。
- **验证家族提示并补充外部上下文** 检索 `auth-queue-sync.net`、`cdn-auth-queue.net` 和 `login-queue-cdn.net` 的 WHOIS、被动 DNS 及公开威胁情报。验证目标：判断这些域名是否为已知的可疑活动指示器，或者确属新生 CDN 业务域名，以明确本次事件应被归为可疑事件还是降级为背景误报。

---

# 事件调查技术附录

以下内容主要供分析、复盘和技术排查使用，不直接替代主报告结论。

## 1. IOC / IOA 清单

| 类型 | 值 | 角色 | 状态 |
| -- | -- | -- | -- |
| IP | 198.51.100.131 | 核心外部基础设施 | 已确认 |
| 域名 | auth-queue-sync.net | 核心外部基础设施 | 已确认 |
| IP | 20.190.160.22 | 背景/上下文指标 | 已确认 |
| 域名 | identity-cache.vendor.example | 背景/上下文指标 | 已确认 |
| 域名 | login.microsoftonline.com | 背景/上下文指标 | 已确认 |
| 指纹 | t13d190900_7711aa22bb33_9911ccddee77 | 扩展查询候选 | 待确认 |
| 内网 IP | 10.44.1.18 | 关联内部地址 | 已确认 |
| 内网 IP | 10.44.1.29 | 关联内部地址 | 已确认 |
| 内网 IP | 10.44.1.42 | 关联内部地址 | 已确认 |
| IP | 203.0.113.131 | 扩展查询候选 | 待确认 |
| 域名 | cdn-auth-queue.net | 扩展查询候选 | 待确认 |
| 域名 | legal-docs-cdn.example | 扩展查询候选 | 待确认 |
| 域名 | login-queue-cdn.net | 扩展查询候选 | 待确认 |

## 2. 关键对象清单

| 对象 | 类型 | 当前角色 | 是否已纳入证据链 | 备注 |
| -- | -- | -- | -- | -- |
| ws-legal-03 | 资产 | 种子资产 | 是 | 种子告警首先落在该资产上。；当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| ws-legal-09 | 资产 | 待确认关联资产 | 否 | 当前仍需独立验证是否真正受影响。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| ws-legal-12 | 资产 | 待确认关联资产 | 否 | 当前仍需独立验证是否真正受影响。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| 198.51.100.131 | IP | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选地址。 |
| auth-queue-sync.net | 域名 | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| 20.190.160.22 | IP | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选地址。 |
| identity-cache.vendor.example | 域名 | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| login.microsoftonline.com | 域名 | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| t13d190900_7711aa22bb33_9911ccddee77 | JA4 | 种子命中指纹 | 是 | 种子告警命中的关键通信指纹。 |
| Suspicious shared-infra beacon requiring review | 家族提示 | 家族/工具提示 | 是 | 辅助解释本案的家族或工具背景，不直接等于强归因。 |
| 10.44.1.18 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.44.1.29 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.44.1.42 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 203.0.113.131 | IP | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选地址。 |
| cdn-auth-queue.net | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| legal-docs-cdn.example | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| login-queue-cdn.net | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| t13d190900_7711aa22bb33_9911ccddee77 | 指纹 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选指纹。 |

## 3. 关键证据细目

### 证据 E-01
- **证据类型**：主支撑证据
- **证据类别**：调查触发证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-20 10:12:00 UTC，资产 `ws-legal-03` 出现了一条与 198.51.100.131 / auth-queue-sync.net 相关的关键观测。
- **为什么重要**：这是种子命中的直接落点，说明调查起点并不是孤立的指纹命中。
- **边界与限制**：单条种子命中本身不能独立证明事件成立，仍需要结合重复通信、资产背景或主机侧线索共同定性。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002`

### 证据 E-02
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-20 10:21:40 UTC，资产 `ws-legal-03` 再次与 198.51.100.131 / auth-queue-sync.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002`

### 证据 E-03
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-20 10:16:20 UTC，资产 `ws-legal-03` 再次与 198.51.100.131 / auth-queue-sync.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002`

### 证据 E-04
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：中
- **事实描述**：在 2026-06-20 10:42:00 UTC，资产 `ws-legal-09` 先解析了少见域名 `cdn-auth-queue.net`。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：这条观测本身更适合用来补足上下文，而不是单独承担最终结论。
- **主要依据来源**：关联事件扩查
- **观测引用**：`obs-002`

### 证据 E-05
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：中
- **事实描述**：在 2026-06-20 10:49:30 UTC，资产 `ws-legal-12` 出现了一条与 198.51.100.131 / legal-docs-cdn.example 相关的关键观测。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：这条观测本身更适合用来补足上下文，而不是单独承担最终结论。
- **主要依据来源**：关联事件扩查
- **观测引用**：`obs-002`

### 证据 C-01
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-20 10:46:10 UTC，资产 `ws-legal-09` 与 198.51.100.131 / identity-cache.vendor.example 的访问更接近计划内更新、维护或共享基础设施背景。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：关联事件扩查
- **观测引用**：`obs-002`

### 证据 C-02
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（上下文）
- **证据强度**：高
- **事实描述**：在 2026-06-20 10:52:00 UTC，资产 `相关资产` 与 198.51.100.131 / auth-queue-sync.net 的访问更接近计划内更新、维护或共享基础设施背景。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：关联事件扩查
- **观测引用**：`obs-002`

## 4. 观测引用对照

| 观测编号 | 来源工具 | 事实摘要 | 关系 |
| -- | -- | -- | -- |
| `obs-001` | `search_seed_context` | 检索到 3 条偏可疑事件，围绕 auth-queue-sync.net、198.51.100.131 展开。 | `supporting` |
| `obs-002` | `search_related_events` | 扩线检索到 3 条可疑关联事件，围绕 auth-queue-sync.net、198.51.100.131 展开；当前先作为待验证候选线索。 | `candidate` |

## 5. 正文章节引用映射

| 正文章节 | 主要 evidence / observation | 用途 |
| -- | -- | -- |
| 首页摘要 | E-01、E-02、expand_cluster_scope | 当前状态 / 当前最强证据 / 当前最关键缺口 |
| 第 5 节 | E-01、E-02、E-03、E-04、E-05、C-01、C-02 | 主支撑证据 / 反证与替代解释 |
| 第 9 节 | expand_cluster_scope、check_counterevidence、execution_gap | 证据链摘要 / 缺口说明 |
| 第 10 节 | E-01、E-02 | 结论 / 动作 / 复核重点 |

## 6. 证据来源与参考资料

| 来源类型 | 来源名称 | 用途 | 边界 |
| -- | -- | -- | -- |
| 上游检测 | t13d190900_7711aa22bb33_9911ccddee77 / Suspicious shared-infra beacon requiring review | 提供本案的调查起点与初始命中线索。 | 命中本身并不自动等于事件成立，仍需要后续本地事实补强。 |
| 内部观测 | trace_store.seed_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 内部观测 | trace_store.related_events | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |

## 7. 证据缺口与待补查询

| 缺口 | 限制的结论 | 当前是否可补 | 最值得补的查询 |
| -- | -- | -- | -- |
| 是否存在主机侧执行、持久化或横向移动证据？ | 当前尚未观测到稳定的执行阶段证据，因此还不能确认主机侧执行、持久化或横向移动是否已经成立。 | 是 | 是否存在主机侧执行、持久化或横向移动证据？ |
| 仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。 | 当前仍缺少对“仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。”的直接补证。 | 是 | 仍需围绕当前关键关联指标开展一轮扩线核查，确认是否存在同指标的更大范围复现。 |
| 关联资产 ws-legal-09、ws-legal-12 是否真正受影响？ | 当前仍缺少对“关联资产 ws-legal-09、ws-legal-12 是否真正受影响？”的直接补证。 | 是 | 关联资产 ws-legal-09、ws-legal-12 是否真正受影响？ |
| 补充 seed 家族、指纹或外部基础设施的公开技术背景，明确它只能作为背景还是能支撑基础设施解释。 | 当前不适合仅凭背景情报完成基础设施或家族级强归因。 | 是 | 补充 seed 家族、指纹或外部基础设施的公开技术背景，明确它只能作为背景还是能支撑基础设施解释。 |
| 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 | 当前不能完全排除维护窗口、共享基础设施或正常组件造成的替代解释。 | 是 | 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 |
| 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。 | 当前仍缺少对“仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。”的直接补证。 | 是 | 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。 |
| seed 自带的家族提示 Suspicious shared-infra beacon requiring review 是否有更多外部证据支撑？ | 当前仍缺少对“seed 自带的家族提示 Suspicious shared-infra beacon requiring review 是否有更多外部证据支撑？”的直接补证。 | 是 | seed 自带的家族提示 Suspicious shared-infra beacon requiring review 是否有更多外部证据支撑？ |
