# 首页摘要
- **结论**：可疑事件，建议继续复核。
- **严重度**：中
- **研判把握**：中
- **已确认范围**：`ws-eng-02`，`ws-eng-05`
- **最强证据**：事件簇已经从单点异常推进到高风险阶段或保守收敛后的多资产范围，足以支撑事件成立。
- **关键缺口**：仍有 2 条候选扩线事件没有完成独立验证，且尚未执行显式反证检查。
- **立即动作**：对重点资产补采主机侧日志，确认是否存在执行、持久化或横向移动证据。
- **一句话结论**：事件存在可疑执行线索，需进一步复核以确认影响范围和性质。

## 调查起点与已知线索

此次调查始于一个种子告警，该告警自带家族/工具提示：Suspected multi-host beacon spread。这一提示表明可能存在跨多个主机的信标传播行为。事件的起点是资产 `ws-eng-02` 在 2026-07-14 01:04:00 UTC 解析了一个罕见的域名 `sync-cdn-notify.net`，随后在 01:10:00 UTC 发生了一个可疑的TLS会话，匹配到了罕见的外部基础设施。这些事件表明了事件的初始触发点，并提供了初步的线索。

## 已确认与候选受影响资产

目前，已确认受影响的资产包括 `ws-eng-02` 和 `ws-eng-05`。这两台资产均出现了可疑的网络活动，如解析罕见域名、连接到外部基础设施以及横向移动的迹象。此外，资产 `ws-eng-09` 被列为待确认关联资产，当前仍需独立验证是否真正受到影响。这些资产构成了当前事件的主要关注范围。

## 事件过程

事件的发展过程如下：

- 2026-07-14 01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`。
- 01:10:00 UTC，`ws-eng-02` 发生了一个可疑的TLS会话，匹配到了罕见的外部基础设施 `sync-cdn-notify.net / 203.0.113.77`。
- 01:14:20 UTC，`ws-eng-02` 重新连接到了相同的C2基础设施。
- 01:18:00 UTC，`ws-eng-02` 上运行了可疑的 `rundll32.exe`，从用户可写的路径加载了一个DLL。
- 01:21:10 UTC，`ws-eng-02` 使用PsExec创建了远程服务，从 `ws-eng-02` 到 `ws-eng-05`。
- 01:28:40 UTC，`ws-eng-05` 接触了原始的信标基础设施。

这些事件展示了事件的持续性和横向移动，支持了事件存在的判断。

## 证据判断

以下是支持事件存在的主要证据：

- 2026-07-14 01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`。
- 01:10:00 UTC，`ws-eng-02` 发生了一个可疑的TLS会话，匹配到了罕见的外部基础设施 `sync-cdn-notify.net / 203.0.113.77`。
- 01:14:20 UTC，`ws-eng-02` 重新连接到了相同的C2基础设施。
- 01:18:00 UTC，`ws-eng-02` 上运行了可疑的 `rundll32.exe`，从用户可写的路径加载了一个DLL。
- 01:21:10 UTC，`ws-eng-02` 使用PsExec创建了远程服务，从 `ws-eng-02` 到 `ws-eng-05`。
- 01:28:40 UTC，`ws-eng-05` 接触了原始的信标基础设施。

这些事件展示了事件的持续性和横向移动，支持了事件存在的判断。然而，仍需进一步验证横向移动的独立性，以及确认是否存在持久化的证据。

## 关联范围

与事件相关的对象包括：

- 资产 `ws-eng-02` 和 `ws-eng-05`，已被确认为受影响资产。
- 资产 `ws-eng-09`，作为待确认关联资产，当前仍需独立验证是否真正受到影响。
- 核心外部基础设施包括域名 `sync-cdn-notify.net` 和 IP 地址 `203.0.113.77`，这些对象在事件中反复出现。
- 扩展查询候选包括资产 `ws-lab-02` 和域名 `cdn-notify-edge.net`，这些对象当前作为待验证候选线索保留。

这些对象构成了当前事件的主要关联范围。

## 反证与边界

当前判断存在以下反证和边界：

- 仍需围绕当前 关键关联指标 做一次显式扩线，确认是否存在同指标的更大范围复现。
- 扩线得到的 2 条关联事件尚未完成独立验证，是否应并入主事件范围？
- 仍需显式检查维护窗口、补丁、备份或共享基线等反证。

这些反证和边界限制了当前判断的确定性，需要进一步验证以确认事件的真实性质。

## 结论与行动

根据当前的证据和分析，我们得出结论：这是一个可疑事件，建议继续复核。下一步应采取以下行动：

- 对重点资产补采主机侧日志，确认是否存在执行、持久化或横向移动证据。
- 继续围绕相同域名、目标 IP 和 JA4 通信指纹 搜索更宽时间窗内的关联事件。
- 优先核验新扩出的关联事件是否存在独立命中，不要只因为共享基础设施就直接并入主证据链。
- 把关联资产 `ws-eng-09` 纳入复核清单，确认它们是共享基础设施背景还是真实受影响对象。

技术细节、IOC/IOA、关键对象清单、观测引用、证据细目和待补缺口详见技术附录。