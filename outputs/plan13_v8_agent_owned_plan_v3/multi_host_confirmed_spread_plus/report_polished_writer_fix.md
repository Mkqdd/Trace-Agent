# 首页摘要
- **结论**：可疑事件，建议继续复核
- **严重度**：中
- **研判把握**：中
- **已确认范围**：ws-eng-02, ws-eng-05
- **最强证据**：2026-07-14 01:10:00 UTC，资产 `ws-eng-02`，关联对象 `sync-cdn-notify.net / 203.0.113.77`，Seed JA4 alert matched a suspicious TLS beacon to rare external infrastructure.
- **关键缺口**：仍需围绕当前 关键关联指标 做一次显式扩线，确认是否存在同指标的更大范围复现。
- **立即动作**：对重点资产补采主机侧日志，确认是否存在执行、持久化或横向移动证据。
- **一句话结论**：事件存在可疑执行线索和潜在横向推进，需进一步验证。

## 调查起点与已知线索

此次调查源于一个可疑的告警，该告警自带家族/工具提示：Suspected multi-host beacon spread。这一提示表明可能存在跨多个主机的信标传播行为。事件的发展已经从单点异常推进到了高风险阶段，涉及多个资产，足以支撑事件成立。然而，目前仍需进一步验证以确认事件的具体范围和影响。

## 已确认与候选受影响资产

已确认受影响的资产包括 `ws-eng-02` 和 `ws-eng-05`。这些资产已被纳入主证据链，并且有明确的可疑执行线索和潜在横向推进的迹象。此外，`ws-eng-09` 被列为待确认关联资产，当前仍需独立验证是否真正受到影响。

## 事件过程

事件始于 2026 年 7 月 14 日 01:04:00 UTC，资产 `ws-eng-02` 解析了一个罕见的域名 `sync-cdn-notify.net`，随后在 01:10:00 UTC 发生了一个可疑的 TLS 会话。紧接着，在 01:14:20 UTC，`ws-eng-02` 重新连接到了相同的 C2 基础设施。01:18:00 UTC，`ws-eng-02` 上出现了可疑的 `rundll32.exe` 启动，加载了一个从用户可写路径的 DLL 文件。随后，在 01:21:10 UTC，`ws-eng-02` 使用 PsExec 创建了一个远程服务，指向 `ws-eng-05`。01:28:40 UTC，`ws-eng-05` 开始接触原始的信标基础设施，进一步表明了横向移动的迹象。

## 证据判断

事件过程中，多个关键节点提供了有力的证据支持事件的存在。例如，01:10:00 UTC，资产 `ws-eng-02` 与 `sync-cdn-notify.net / 203.0.113.77` 之间发生了可疑的 TLS 会话，匹配到了罕见的外部基础设施。这些证据表明了事件的持续性和横向移动，支持了事件存在的判断。然而，仍需进一步验证横向移动的独立性，以及确认是否存在持久化的尝试。

## 关联范围

与事件相关的对象包括核心外部基础设施 `sync-cdn-notify.net` 和 `203.0.113.77`，以及待确认关联资产 `ws-eng-09`。这些对象在事件过程中起到了关键作用，但目前仍需进一步验证它们是否真正受到影响。此外，`ws-lab-02` 被列为扩展查询候选，当前作为上下文边界保留。

## 反证与边界

尽管当前证据链较为完整，但仍存在一些反证和边界限制了我们的判断。例如，仍需围绕当前 关键关联指标 做一次显式扩线，确认是否存在同指标的更大范围复现。此外，扩线得到的 2 条关联事件尚未完成独立验证，是否应并入主事件范围仍需进一步确认。最后，仍需显式检查维护窗口、补丁、备份或共享基线等反证，以排除正常操作的可能性。

## 结论与行动

根据现有证据，事件存在可疑执行线索和潜在横向推进，需进一步验证。建议采取以下行动：
- 对重点资产补采主机侧日志，确认是否存在执行、持久化或横向移动证据。
- 继续围绕相同域名、目标 IP 和 JA4 通信指纹 搜索更宽时间窗内的关联事件。
- 优先核验新扩出的关联事件是否存在独立命中，不要只因为共享基础设施就直接并入主证据链。
- 把关联资产 `ws-eng-09` 纳入复核清单，确认它们是共享基础设施背景还是真实受影响对象。

技术细节、IOC/IOA、关键对象清单、观测引用、证据细目和待补缺口详见技术附录。