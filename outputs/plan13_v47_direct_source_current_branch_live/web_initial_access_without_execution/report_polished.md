# 首页摘要

- **结论**：已形成围绕 web-app-02 的可疑安全事件簇，存在疑似 Web Shell 信标行为，事件有待继续复核。
- **严重度**：中危。
- **研判把握**：较高。主证据链在时间线与外联指标上一致，但主机侧命令、落地文件与横向移动证据仍缺失，故当前判断不能闭环到已观察到完全控制的层级。
- **已确认范围**：资产 web-app-02 已被纳入确认为受影响的调查范围，其在观察窗口内存在对核心外部基础设施 auth-edge-panel.net / 198.51.100.177 的可疑 TLS 外联活动。
- **一句话结论**：内部服务器 web-app-02 在收到针对 admin portal 的利用请求后，解析并持续连接外部域名 auth-edge-panel.net，且 TLS 会话命中疑似 Web Shell 信标指纹，构成了待闭环的可疑事件。
- **立即动作**：对 web-app-02 补采并核查主机侧进程执行、命令行、持久化与脚本日志；在边界/EDR/代理中围绕 JA4 指纹 `t13d1517h2_22adbe210bc1_11cae1281e22` 及域名 `auth-edge-panel.net`、IP `198.51.100.177` 执行更长时间窗口的扩线搜索。

## 1. 事件结论与当前判断

本次调查确认 web-app-02 已形成可疑事件簇，目前状态为“可疑事件，建议继续复核”。种子告警由可能的 Web Shell 信标触发，且自带家族工具提示“Possible web beacon without host corroboration”，但该提示仅作为辅助工具背景存在，不构成对攻击者或工具集的强归因。主证据链显示 web-app-02 在执行分析时间窗口内的行为，存在明确的前后因果关联：外部利用请求、域名解析、以及后续面向同一外部基础设施的 TLS 会话，均指向一种可用于命令与控制（C2）的外联模式。

认定此事为安全事件的理由在于，上述事件组合在时间、网络侧指标以及通信指纹上高度收敛。然而，由于目前缺少主机侧执行、持久化和横向移动的直接证据，无法进一步确认恶意软件是否已经完整执行，或攻击者是否已实现对目标资产的完全控制。因此，当前判断止步于“确认 web-app-02 处于可疑安全事件的影响范围内，外联目标为核心外部基础设施”，而不会升级为“主机已被完全攻陷”。

分析窗口内还存在多项可限制更强结论的因素：web-app-02 上恰有一项计划内的 admin portal 补丁部署，且此次窗口内未发现 Web 工作进程产生可疑子进程的遥测；同时 script-block 日志因未启用而不可得。这些均构成了限制“已观察到取得执行能力”这一结论的边界条件。web-app-05 和 web-app-04 上观测到的类似 DNS/TLS 行为目前仅作为候选关联线索，不能作为影响范围已横向扩散的证据。

## 2. 事件过程与关键时间线

以下时间线使用已确认事件节点，按时间推进叙述事件变化；候选与背景事件在各节点中以边界性说明形式出现，不作为主线已发生事实。

- **2026-06-24 14:50:00 UTC** -- 面向公网的 Web 服务器 web-app-02 开始收到针对其 admin portal 的利用请求。这构成了事件入口阶段中的首次可疑交互，说明对外暴露的管理端口被有目的地探测或攻击。  
- **2026-06-24 14:54:00 UTC** -- web-app-02 再度收到另一波 admin portal 利用请求，且使用不同的来源自治域和载荷家族。这加大了外部多来源尝试利用同一入口的可能性，也提升了后续域名解析和出站连接的可疑权重。  
- **2026-06-24 14:58:30 UTC** -- 在连续两波利用请求之后，web-app-02 立即对外解析了域名 `auth-edge-panel.net`。这一解析行为不再是可以独立存在的 DNS 记录，而是与利用行为时间紧密相随的关联动作，推动事件从纯入口探测向 C2 准备过渡。  
- **2026-06-24 15:02:00 UTC** -- 种子告警触发：web-app-02 与 `auth-edge-panel.net` / `198.51.100.177` 之间的一次 TLS 会话，命中了 JA4 指纹 `t13d1517h2_22adbe210bc1_11cae1281e22`，且被识别为可能的 Web Shell 信标。此节点将前述利用、解析和出站行为组合成具备工具特征的通信模式，是形成当前事件判定最关键的一环。  
- **2026-06-24 15:06:40 UTC** -- web-app-02 在种子告警后再次连接同一外部基础设施 `auth-edge-panel.net` / `198.51.100.177`。这种在告警产生后仍维持的重连，进一步说明外联并非偶然或瞬时动作，而是存在连续性的通信意图，增加了 C2 通道依然活跃的判断依据。

在此之后的时间段内，存在若干仅作为背景与候选线索的事件。例如 web-app-02 在 15:10:00 至 15:12:30 之间曾解析并连接了同一基础设施上的兄弟域名 `cdn-auth-edge.net`，但缺少主机侧验证，且只能作为同一基础设施上的扩展观察，不纳入已确认攻击链。同样，web-app-05 在 15:30:00 解析 `cdn-auth-edge.net`，并在 15:32:20 完成一次短 TLS 连接的现象，仅使其成为待确认资产，不能直接并入已确认受影响范围。这些结点均留作后续核查的候选入口。

## 3. 关键证据判断

种子告警命中 JA4 指纹 `t13d1517h2_22adbe210bc1_11cae1281e22`，并标记为可能的 Web Shell 信标，是本次事件中最具判别力的技术指标。该指纹作为种子告警的独立识别特征，不构成一条新的“时间-主体-对象”事件组合，但却是判定后续外联可疑性质的核心依据。围绕该指纹及外部基础设施 `auth-edge-panel.net` / `198.51.100.177` 展开的扩线检索，共返回 5 条偏可疑关联事件，进一步确认了该外部地址在当前环境中并非仅一次偶发出现，而是能够在多个观测点上被发现，但其中一部分事件目前仅被当作候选线索，尚未取得等同种子告警级别的独立确认。

证据链的时间顺序是对判断的另一种强化：web-app-02 上对 admin portal 的利用请求发生在 DNS 解析之前，解析又发生在 TLS 告警之前，而告警之后仍有一次重连。这意味着网络侧事件的先后排列不是随机散落，而是具有“利用-准备-C2-保持”的连贯走向。尽管缺少落地载荷、完整命令行和主机执行链，这些网络侧的时序性和同一基础设施的反复出现，足以将此事从孤立告警提升为值得交付人工复核的可疑事件簇。

另一方面，同一窗口期内的主机遥测显示，web-app-02 的 Web 工作进程在观测期间未产生可疑子进程活动。这一事实本身不会否定网络侧告警的恶意判定，但它限制了将事件定性为“已确认执行 Web Shell 并派生进程”的上限。因此，当前对威胁深度的判断维持在网络通信与潜在 C2 活动层面，而不是已确认的主机控制层面。

Seed 告警中自带的家族提示“Possible web beacon without host corroboration”仅作为工具背景辅助，不能直接作为归因体系的确证。目前缺乏该提示背后关联的具体工具版本、公开情报映射、或同一指纹在多个独立案例中被验证为恶意使用的证据，因此该提示适合作为后续公开情报比对与工具侧画像的起点，而不是判断事件归属的唯一理由。

## 4. 影响范围、候选对象与外部基础设施

当前已确认的调查范围集中在资产 web-app-02。该资产在本案中既是种子资产，也是所有网络侧利用、解析与 TLS 信标通信的唯一起点，构成了必须进行主机侧取证和隔离复核的基础。web-app-02 的外联对象 -- 域名 `auth-edge-panel.net` 与 IP `198.51.100.177` -- 被定位为核心外部基础设施，在当前主证据链中反复出现，作用类似于可用于 C2 的汇聚节点。该组外部基础设施只能作为关联排查、封禁和边界回溯的目标，不应被描述为已受影响资产。

除 web-app-02 外，存在两个候选资产和一组候选域名/指纹对象需以边界性身份理解。web-app-05 在 15:30:00 至 15:32:20 之间曾解析并短暂连接兄弟域名 `cdn-auth-edge.net` 及同一 IP `198.51.100.177`，但本地未曾产生匹配的告警，缺乏和 web-app-02 同级别的横向传播证据或独立命中。因此，web-app-05 在当前阶段只能作为“待确认关联资产”，其是否属于共享基础设施的正常行为，还是已被感染但未触发同类告警，仍有待独立验证。web-app-04 的连接则是通过扫描器遥测厂商域名 `scanner-cdn.example` 到达同一托管 IP，这一路径的存在反而倾向于提供一种共享托管/扫描器背景的解释，削弱了将其直接等同于主机事件的必要性。

候选域名 `cdn-auth-edge.net` 和候选指纹 `t13d1517h2_22adbe210bc1_11cae1281e22` 出现在 关键关联指标 扩线的候选列表中，它们可在后续搜索中作为回溯和复现的关键指标，用于发现是否存在更大范围的受影响对象，但在当前报告阶段只作为被标记的待验证候选指标，不能引申为已确认的横向扩散或持久化基础设施。

## 5. 反证、替代解释与未闭合缺口

当前阶段存在多项对更强结论构成限制的反证或背景因素。首先，web-app-02 上在 15:18:00 确认有一项计划中的 admin portal 补丁部署活跃。尽管一个计划内补丁窗口不能单独说明所有网络行为均为良性，但它提供了一个合法活动背景，用于解释部分管理端网络交互或服务器侧配置变更，并在时间上重合了利用请求之后的时段。这种运维背景限制将后续外联直接等价于“攻击成功导致 C2 持久化”的结论。

其次，主机遥测在 15:15:30 的采样中未捕捉到可见的 Web 进程可疑子进程活动，而且 script-block 日志在 web-app-02 上未被启用，直至 15:50:00 确认主机命令细节仍不可得。这意味着，目前无法依据主机侧证据直接确认利用请求是否引发了后置执行链。网络侧的行为模式虽然高度可疑，但也无法摆脱缺少闭环验证的局面--这一未闭合的执行缺口是限制当前研判不能上升至“已确认主机受控”的核心屏障。

另一组需要关注的替代解释来自兄弟域名 `cdn-auth-edge.net` 与扫描器域名 `scanner-cdn.example`。web-app-02 在同一次通信中对 `cdn-auth-edge.net` 的解析和单一未验证 TLS 会话，无法排除其与计划维护、第三方 CDN 认证或边缘服务共享基础设施之间的关联。web-app-04 通过 `scanner-cdn.example` 连接至同一 IP `198.51.100.177` 的行为，更可能反映一种共享托管与扫描器遥测的背景，而不必然说明 web-app-04 也受到威胁。这些事件只能在可用证据框架内视为背景或替代解释，而不能作为主链攻击动作纳入确认范围。

此外，还有数个未闭合缺口直接限制事件范围的扩展和威胁深度的定级。当前缺失对主机侧执行、持久化或横向移动的直接证据，这使得对攻击链的理解停留在远程利用尝试和 C2 信标层面。关联资产 web-app-05 是否真正受影响，仍处于“待验证”状态，而非确认传播。围绕当前 关键关联指标 指标的显式扩线尚未完成，无法判断同一 JA4 指纹或同一外部基础设施在更宽时间窗和历史数据中是否存在更早或更大范围复现。最后，seed 家族的公开技术背景和外部证据支撑尚未补足，这也使得对入侵工具或行为体的归因缺少外部印证，当前对于该提示只能作为辅助背景而非归因基础。

## 6. 处置建议与后续核查

- **对重点受影响资产 web-app-02 补充主机侧日志，并围绕执行、持久化与横向移动进行针对性排查。**  
  重点采集 web-app-02 上 Web 工作进程的完整命令行参数、子进程派生历史、计划任务或服务创建事件、以及对外连接的进程映射。搜索时间窗口应覆盖 2026-06-24 14:50:00 至 15:50:00 UTC，同时向两端各扩展至少 24 小时，以确认是否存在延时执行或持续留守行为。如果环境中已部署 Sysmon 或 EDR，应检索与父进程（Web 工作进程）相关的文件写入、注册表修改或凭据访问事件。

- **围绕核心外部基础设施和 JA4 指纹进行更广时间窗的复现搜索。**  
  在边界流量、代理日志、DNS 解析记录和 EDR 模块中，围绕 JA4 指纹 `t13d1517h2_22adbe210bc1_11cae1281e22`、域名 `auth-edge-panel.net` 以及 IP `198.51.100.177` 检索至少过去 30 天的记录。验证是否存在 web-app-02 之外的内部主机命中，尤其是来自 web-app-05、web-app-04 或其它 Web 集群节点的连接。对同一 IP `198.51.100.177` 的 DNS 反向解析和相关域名注册信息也应一并进行，以评估其是否属于共享 CDN 或攻击者独立控制的基础设施。

- **将候选资产 web-app-05 以及 web-app-02 上出现的兄弟域名 `cdn-auth-edge.net` 纳入复核清单，但不要直接升级为已受影响范围。**  
  针对 web-app-05，核查其在 2026-06-24 15:30:00 至 15:32:20 UTC 期间的完整网络连接、TLS 握手特征（尤其是 JA4/JA4S）、以及对应 DNS 解析来源，以判断其与核心事件是否存在同源性。对于 `cdn-auth-edge.net`，需确认其是否为所属环境中合法 CDN、认证服务或共享基础设施，并排查是否曾在其它内部资产上被解析或访问过，但其本身目前仍作为候选域名，不构成受影响实体的依据。

- **利用 WAF 和边界安全控制记录检查 admin portal 的利用尝试是否波及更多资产。**  
  从 2026-06-24 14:50:00 起，检索 WAF 完整日志，关注针对 admin portal 的利用请求特征（payload 特征、来源 ASN、User-Agent、请求路径等），确认这些请求是否仅命中 web-app-02，还是同时指向 web-app-04、web-app-05 或其它管理端口暴露的资产。如果 WAF 规则更新在 15:34:00 成功阻止了后续载荷，应评估策略的覆盖完整性，避免存在被绕过的可能性。

- **补充 seed 家族提示与 JA4 指纹的外部威胁情报背景。**  
  将指纹 `t13d1517h2_22adbe210bc1_11cae1281e22` 与域名 `auth-edge-panel.net` 提交到可访问的威胁情报源或内部分析平台，验证其是否关联已知的后门工具、Web Shell 生成器或特定入侵集合。这一情报比对结果将决定该指标只能作为本案内部告警特征使用，还是能上升到可被广泛复用的高可靠度检测对象。该动作本身不会改变当前事件的确认状态，但会影响未来检测规则的优先级。

---

# 事件调查技术附录

以下内容主要供分析、复盘和技术排查使用，不直接替代主报告结论。

## 1. IOC / IOA 清单

| 类型 | 值 | 角色 | 状态 |
| -- | -- | -- | -- |
| IP | 198.51.100.177 | 核心外部基础设施 | 已确认 |
| 域名 | auth-edge-panel.net | 核心外部基础设施 | 已确认 |
| 域名 | scanner-cdn.example | 背景/上下文指标 | 已确认 |
| 指纹 | t13d1517h2_22adbe210bc1_11cae1281e22 | 扩展查询候选 | 待确认 |
| 内网 IP | 10.61.2.20 | 关联内部地址 | 已确认 |
| 内网 IP | 10.61.2.44 | 关联内部地址 | 已确认 |
| 内网 IP | 10.61.2.55 | 关联内部地址 | 已确认 |
| 域名 | cdn-auth-edge.net | 扩展查询候选 | 待确认 |

## 2. 关键对象清单

| 对象 | 类型 | 当前角色 | 是否已纳入证据链 | 备注 |
| -- | -- | -- | -- | -- |
| web-app-02 | 资产 | 种子资产 | 是 | 种子告警首先落在该资产上。；当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| web-app-05 | 资产 | 待确认关联资产 | 否 | 当前仍需独立验证是否真正受影响。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| 198.51.100.177 | IP | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选地址。 |
| auth-edge-panel.net | 域名 | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选域名。 |
| scanner-cdn.example | 域名 | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| t13d1517h2_22adbe210bc1_11cae1281e22 | JA4 | 种子命中指纹 | 是 | 种子告警命中的关键通信指纹。 |
| Possible web beacon without host corroboration | 家族提示 | 家族/工具提示 | 是 | 辅助解释本案的家族或工具背景，不直接等于强归因。 |
| 10.61.2.20 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.61.2.44 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.61.2.55 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| web-app-04 | 资产 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选资产。 |
| cdn-auth-edge.net | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| t13d1517h2_22adbe210bc1_11cae1281e22 | 指纹 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选指纹。 |

## 3. 关键证据细目

### 证据 E-01
- **证据类型**：主支撑证据
- **证据类别**：调查触发证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-24 15:02:00 UTC，资产 `web-app-02` 围绕 198.51.100.177 / auth-edge-panel.net 的异常通信触发了种子告警。
- **为什么重要**：这是种子命中的直接落点，说明调查起点并不是孤立的指纹命中。
- **边界与限制**：单条种子命中本身不能独立证明事件成立，仍需要结合重复通信、资产背景或主机侧线索共同定性。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-02
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-24 14:58:30 UTC，资产 `web-app-02` 对外服务入口出现可疑利用请求。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-03
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-24 15:06:40 UTC，资产 `web-app-02` 再次与 198.51.100.177 / auth-edge-panel.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-04
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-24 14:54:00 UTC，资产 `web-app-02` 对外服务入口出现可疑利用请求。
- **为什么重要**：它补足了外联之前的高风险上下文，使后续可疑通信不再显得孤立。
- **边界与限制**：它解释了事件为何进入高风险状态，但单独仍不能证明漏洞利用已经成功执行。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-05
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-24 14:50:00 UTC，资产 `web-app-02` 对外服务入口出现可疑利用请求。
- **为什么重要**：它补足了外联之前的高风险上下文，使后续可疑通信不再显得孤立。
- **边界与限制**：它解释了事件为何进入高风险状态，但单独仍不能证明漏洞利用已经成功执行。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-06
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：中
- **事实描述**：在 2026-06-24 15:30:00 UTC，资产 `web-app-05` 先解析了少见域名 `cdn-auth-edge.net`。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：这条观测本身更适合用来补足上下文，而不是单独承担最终结论。
- **主要依据来源**：关联事件扩查、资产背景核查
- **观测引用**：`obs-002, obs-003, obs-004`

### 证据 E-07
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：中
- **事实描述**：在 2026-06-24 15:32:20 UTC，资产 `web-app-05` 出现了一条与 198.51.100.177 / cdn-auth-edge.net 相关的关键观测。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：这条观测本身更适合用来补足上下文，而不是单独承担最终结论。
- **主要依据来源**：关联事件扩查、资产背景核查
- **观测引用**：`obs-002, obs-003, obs-004`

### 证据 C-01
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（资产背景）
- **证据强度**：高
- **事实描述**：在 2026-06-24 15:15:30 UTC，资产 `web-app-02` 同时间窗存在已批准的维护、变更或备份活动，可解释部分日常管理行为。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 C-02
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（资产背景）
- **证据强度**：高
- **事实描述**：在 2026-06-24 15:18:00 UTC，资产 `web-app-02` 同时间窗存在已批准的维护、变更或备份活动，可解释部分日常管理行为。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

## 4. 观测引用对照

| 观测编号 | 来源工具 | 事实摘要 | 关系 |
| -- | -- | -- | -- |
| `obs-001` | `search_seed_context` | 检索到 5 条偏可疑事件，围绕 auth-edge-panel.net、198.51.100.177 展开。 | `supporting` |
| `obs-002` | `search_related_events` | 扩线检索到 5 条可疑关联事件，围绕 auth-edge-panel.net、198.51.100.177 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-003` | `search_related_events` | 扩线检索到 5 条可疑关联事件，围绕 auth-edge-panel.net、198.51.100.177 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-004` | `expand_asset_scope` | 检索到 2 条上下文事件，当前主要用于扩边界和补足范围。 | `context` |

## 5. 正文章节引用映射

| 正文章节 | 主要 evidence / observation | 用途 |
| -- | -- | -- |
| 首页摘要 | E-01、E-02、expand_cluster_scope | 当前状态 / 当前最强证据 / 当前最关键缺口 |
| 第 5 节 | E-01、E-02、E-03、E-04、E-05、E-06、E-07、C-01、C-02 | 主支撑证据 / 反证与替代解释 |
| 第 9 节 | expand_cluster_scope、check_counterevidence、execution_gap | 证据链摘要 / 缺口说明 |
| 第 10 节 | E-01、E-02 | 结论 / 动作 / 复核重点 |

## 6. 证据来源与参考资料

| 来源类型 | 来源名称 | 用途 | 边界 |
| -- | -- | -- | -- |
| 上游检测 | t13d1517h2_22adbe210bc1_11cae1281e22 / Possible web beacon without host corroboration | 提供本案的调查起点与初始命中线索。 | 命中本身并不自动等于事件成立，仍需要后续本地事实补强。 |
| 内部观测 | trace_store.seed_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 内部观测 | trace_store.related_events | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 内部观测 | trace_store.asset_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |

## 7. 证据缺口与待补查询

| 缺口 | 限制的结论 | 当前是否可补 | 最值得补的查询 |
| -- | -- | -- | -- |
| 是否存在主机侧执行、持久化或横向移动证据？ | 当前尚未观测到稳定的执行阶段证据，因此还不能确认主机侧执行、持久化或横向移动是否已经成立。 | 是 | 是否存在主机侧执行、持久化或横向移动证据？ |
| 仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。 | 当前仍缺少对“仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。”的直接补证。 | 是 | 仍需围绕当前关键关联指标开展一轮扩线核查，确认是否存在同指标的更大范围复现。 |
| 关联资产 web-app-05 是否真正受影响？ | 当前仍缺少对“关联资产 web-app-05 是否真正受影响？”的直接补证。 | 否 | 该缺口已多轮核查但仍未获得新的有效信息，更适合作为报告未决事项。 |
| 补充 seed 家族、指纹或外部基础设施的公开技术背景，明确它只能作为背景还是能支撑基础设施解释。 | 当前不适合仅凭背景情报完成基础设施或家族级强归因。 | 是 | 补充 seed 家族、指纹或外部基础设施的公开技术背景，明确它只能作为背景还是能支撑基础设施解释。 |
| 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 | 当前不能完全排除维护窗口、共享基础设施或正常组件造成的替代解释。 | 是 | 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 |
| 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。 | 当前仍缺少对“仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。”的直接补证。 | 是 | 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。 |
| seed 自带的家族提示 Possible web beacon without host corroboration 是否有更多外部证据支撑？ | 当前仍缺少对“seed 自带的家族提示 Possible web beacon without host corroboration 是否有更多外部证据支撑？”的直接补证。 | 是 | seed 自带的家族提示 Possible web beacon without host corroboration 是否有更多外部证据支撑？ |
