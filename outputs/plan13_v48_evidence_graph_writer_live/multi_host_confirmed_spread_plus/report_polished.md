# 首页摘要

- **结论**：确认安全事件
- **严重度**：高危
- **研判把握**：94%
- **已确认范围**：ws-eng-02 与 ws-eng-05 两台工作站已纳入确认受影响范围，围绕核心外联基础设施 `sync-cdn-notify.net` 和 `203.0.113.77` 形成多主机信标传播链条。
- **一句话结论**：以种子告警 ws-eng-02 为起点，已确认可疑 TLS 信标通信、主机侧可疑执行动作，以及 PsExec 远程服务创建触发的跨主机信标续现；扩展候选资产与候选通信线索尚待独立验证。
- **立即动作**：隔离 ws-eng-02 与 ws-eng-05，在边界设备上封禁 `sync-cdn-notify.net` 与 `203.0.113.77` 的出站通信，对候选扩线事件逐条完成独立验证。

## 1. 事件结论与当前判断

本次调查起始于 2026-07-14 01:10:00 UTC 落在资产 ws-eng-02 上的一条 TLS 信标告警，告警携带 JA4 指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc`，并内嵌家族提示“Suspected multi-host beacon spread”。围绕该种子告警，在分析窗口 2026-07-14 01:04:00 至 01:49:00 UTC 内共检索到 6 条偏可疑事件，构成一条从单主机 DNS 解析、TLS 信标、主机侧可疑执行，到 PsExec 远程服务创建及第二台主机信标续现的完整事件链。当前研判结论为确认安全事件，主支撑证据已达到交付门槛，ws-eng-02 与 ws-eng-05 已纳入确认受影响范围。

需要明确的是，“确认安全事件”表示当前证据足以支撑事件级结论并触发响应动作，但尚不意味已确认攻击者完全控制、恶意软件完整执行链闭环、持久化建立或长期驻留。扩线产生的候选资产、候选通信线索以及 ws-eng-09 上的 EDR 遥测降级缺口，均构成当前结论的上界，需通过后续独立验证逐步收敛。

## 2. 事件过程与关键时间线

事件链起点位于 2026-07-14 01:04:00 UTC，种子资产 ws-eng-02（源地址 `10.70.3.12`）首先对稀有域名 `sync-cdn-notify.net` 发起 DNS 解析。六分钟后，即 01:10:00 UTC，该主机以相同的 JA4 指纹与 `sync-cdn-notify.net` 对应 IP `203.0.113.77` 建立可疑 TLS 会话，触发种子告警。告警发出后，ws-eng-02 于 01:14:20 UTC 再次连接同一 C2 基础设施 `sync-cdn-notify.net` 和 `203.0.113.77`，显示持续信标行为。

紧接信标序列，在 01:18:00 UTC，ws-eng-02 上出现可疑进程事件--`rundll32.exe` 从用户可写路径加载一个加载器 DLL。这一主机侧执行动作与前置信标通信构成时间线上的紧密咬合。至 01:21:10 UTC，ws-eng-02 通过 PsExec 向另一台主机 ws-eng-05（对应 IP `10.70.3.45`）发起远程服务创建，表明存在横向移动尝试。

关键转折点出现在 01:28:40 UTC：第二台主机 ws-eng-05 在远程服务创建事件约七分钟后，同样联系了原始信标基础设施 `sync-cdn-notify.net` 和 `203.0.113.77`，表明横向扩散后信标活动已扩展至第二台主机。至此，从 ws-eng-02 到 ws-eng-05 的主线传播链条已确认闭合。

在主线之外，尚存在若干候选扩线线索需独立验证。ws-eng-05 在 01:32:10 UTC 对二级稀有域名 `cdn-notify-edge.net` 发起 DNS 解析，并于 01:36:00 UTC 通过 TLS 连接至该域名对应的外部 IP `198.51.100.44`--这些事件目前标记为候选扩线，尚未进入已确认主证据链。此外，在 01:39:20 UTC 观测到 ws-eng-05 通过 WMI 向 ws-eng-09（`10.70.3.90`）发起远程进程创建，但该事件缺少可独立验证的指示物，当前仅保留为边界线索。随后，第三台主机 ws-eng-09 在 01:44:10 UTC 解析了 `cdn-notify-edge.net`，并在 01:47:30 UTC 连接至 `198.51.100.44`；然而 ws-eng-09 的 EDR 传感器在 01:49:00 UTC 报告进程遥测降级，导致其侧的执行谱系无法重建，上述事件均属候选扩线，尚不可并入确认范围。

## 3. 关键证据判断

当前将本事件判定为确认安全事件，证据链在三个维度上形成支撑闭合。第一，信标行为的时序密度与基础设施复用高度一致：ws-eng-02 在告警前后短时间内三次触达同一外联域名 `sync-cdn-notify.net` 及其解析 IP `203.0.113.77`，构成稳定的 C2 通信模式。第二，网络层信号与主机层执行信号形成交叉印证：信标序列之后立即出现 `rundll32.exe` 从用户可写路径加载 DLL 的可疑执行事件，消除了“单纯流量误报”的可能性。第三，横向移动动作与扩散后信标的因果链条清晰可追踪：PsExec 远程服务创建从 ws-eng-02 指向 ws-eng-05，七分钟后 ws-eng-05 即连回相同的 C2 基础设施，这一时间顺序和基础设施重叠并非随机共现所能解释。

与此同时，若干缺口限制了我们得出更强结论。ws-eng-09 的 EDR 进程遥测降级意味着无法重建该主机的完整执行链，因此尚不能确认 ws-eng-09 是否已实际受影响。候选域名 `cdn-notify-edge.net` 及相关 IP `198.51.100.44` 的通信模式虽与主链形态相似，但尚未通过指纹、IOC 或外部情报的独立验证，因此不能推断为主链的线性延续。此外，种子告警所带的家族提示“Suspected multi-host beacon spread”目前仅作为上下文参考，尚缺外部技术报告或归因支撑，不作为强归因依据。这些缺口意味着我们可以在事件层面确认多主机信标扩散的存在，但尚不能断言完整的攻击者控制链、持久化机制或数据外传闭环。

## 4. 影响范围、候选对象与外部基础设施

已纳入确认受影响范围的资产包括两台工作站：ws-eng-02 作为种子资产，是本次事件链的起点，在其上观测到 DNS 解析、TLS 信标、可疑进程执行和 PsExec 横向移动发起等多个阶段的行为，需要优先隔离和取证；ws-eng-05 作为横向扩散后信标续现的第二台主机，已确认与同一 C2 基础设施发生通信，同样需要纳入隔离和取证范围。两台主机的源地址分别为 `10.70.3.12` 和 `10.70.3.45`，在内部网络定位和排查中可作为关联线索使用。

需要与已确认范围明确区分的是候选待验证资产。ws-eng-09 当前仅作为待确认关联对象，虽然在候选事件中观测到其解析了二级域名 `cdn-notify-edge.net` 并发起外联，但由于 EDR 遥测降级无法获取执行链证据，目前不能将其认定为已受影响主机。ws-lab-02 在分析窗口内通过 `telemetry-collector.example` 域名访问了 IP `198.51.100.44`，但其活动背景为 QA 遥测作业，且该 IP 已被证明是共享基础设施，因此 ws-lab-02 不作为受影响候选资产，仅作为背景上下文保留。

核心外部基础设施包括域名 `sync-cdn-notify.net` 及其对应 IP `203.0.113.77`，这两个对象在主证据链中被 ws-eng-02 和 ws-eng-05 反复访问，是本次事件中威胁通信的中枢节点，需要在边界和代理设备上优先排查与封禁。二级候选域名 `cdn-notify-edge.net` 及其关联 IP `198.51.100.44` 出现在候选扩线事件中，但后者的共享托管属性已通过 ws-lab-02 的独立遥测连接得到确认--QA 任务以不同的业务域名 `telemetry-collector.example` 同样解析至该 IP，因此 `198.51.100.44` 不能单一解释为关联外部基础设施，只能作为关联排查对象处理。其他出现在背景事件中的地址如 `13.107.246.45` 和域名 `download.windowsupdate.com` 均为合法微软更新基础设施，不纳入封禁或排查范围。

## 5. 反证、替代解释与未闭合缺口

分析过程中显式检查了维护窗口、补丁活动和共享基础设施等可能的替代解释。在 ws-eng-05 上，2026-07-14 01:24:00 UTC 出现了已批准的 SCCM 补丁登台事件，01:26:00 UTC 又从微软官方端点 `download.windowsupdate.com` 下载了 Windows 更新内容，时间窗口与可疑信标通信存在部分重叠。然而，这两条维护类事件的通信目标均为微软合法基础设施，与可疑信标指向的 `sync-cdn-notify.net` 和 `203.0.113.77` 完全没有重叠，因此补丁活动只能解释 ws-eng-05 在同一时段的部分管理流量背景，却无法解释为何该主机主动连接至种子告警所标记的同一 C2 节点。这一反证的存在并不削弱主链结论，反而帮助收窄了 ws-eng-05 上异常通信的判定边界。

针对候选 IP `198.51.100.44`，ws-lab-02 在 01:46:20 UTC 通过 `telemetry-collector.example` 发起的 QA 遥测连接提供了关键的共享基础设施证据。同一 IP 被两条完全不同业务背景的连接所引用，表明该 IP 属于多租户托管环境，而非攻击者独占的 C2 节点。这一发现限制了将 `198.51.100.44` 和关联域名 `cdn-notify-edge.net` 直接定性为关联外部基础设施的结论强度，但也不能据此否定 ws-eng-05 和 ws-eng-09 与该 IP 通信的可疑性--共享基础设施上同样可能存在恶意租户。

当前最关键的未闭合缺口在于候选扩线事件的独立验证。ws-eng-05 对 `cdn-notify-edge.net` 的访问、ws-eng-09 的外联行为以及 WMI 远程进程创建事件，均尚未通过指纹匹配、IOC 命中或外部情报进行交叉验证。此外，ws-eng-09 上 EDR 遥测降级意味着无法将外联行为与具体进程上下文关联，这直接限制了将该主机纳入确认受影响范围的判断。这些缺口意味着我们当前确认的事件范围以 ws-eng-02 和 ws-eng-05 为界，攻击者是否已进一步扩散至 ws-eng-09 及以远资产，仍属待验证的开放问题。

## 6. 处置建议与后续核查

- **隔离与取证 ws-eng-02 和 ws-eng-05**：针对两台已确认受影响主机，通过 EDR/SIEM 提取在 2026-07-14 01:04:00 至 01:49:00 UTC 期间的全部进程创建、网络连接、服务创建和文件写入日志，重点关注 `rundll32.exe` 加载的 DLL 路径、PsExec 服务注册表痕迹以及 `sync-cdn-notify.net` 和 `203.0.113.77` 的出站连接记录，验证是否存在持久化机制或额外横向移动目标。

- **边界封禁核心外部基础设施**：在网络边界防火墙、代理服务器和 DNS 解析器上，封禁域名 `sync-cdn-notify.net` 及 IP `203.0.113.77` 的出站访问；同时将域名 `cdn-notify-edge.net` 和 IP `198.51.100.44` 加入监控告警列表，暂不直接封禁以避免因共享基础设施误伤正常业务，但需对其通信日志进行回溯和持续监控。

- **独立验证候选扩线事件**：对候选事件 evt-809、evt-810、evt-813 涉及的通信记录，通过 SIEM/flow 日志查询同一 JA4 指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc` 在窗口中是否出现在更多主机上，并将 `cdn-notify-edge.net` 和 `198.51.100.44` 投入本地威胁情报库及公开 OSINT 源进行交叉查询，验证其是否具有已知恶意标记。

- **修复 ws-eng-09 的 EDR 遥测并补全执行链**：针对 ws-eng-09 报告的进程遥测降级问题，立即检查 EDR 传感器状态、内核驱动加载情况以及是否存在篡改保护失效，在恢复遥测后回溯 01:49:00 UTC 前后的进程创建和网络连接事件，尝试重建该主机在候选扩线事件中的执行谱系。

- **扩展同指标时间窗检索**：以 seed 指标 `sync-cdn-notify.net`、`203.0.113.77` 和 JA4 指纹为 关键关联指标，在 SIEM 中扩展检索至分析窗口前后至少 24 小时，确认是否存在更早的初始访问迹象或后续持续信标活动，同时关注是否还有 ws-eng-02、ws-eng-05 之外的内部主机连接至相同外联节点。

---

# 事件调查技术附录

以下内容主要供分析、复盘和技术排查使用，不直接替代主报告结论。

## 1. IOC / IOA 清单

| 类型 | 值 | 角色 | 状态 |
| -- | -- | -- | -- |
| IP | 203.0.113.77 | 核心外部基础设施 | 已确认 |
| 域名 | sync-cdn-notify.net | 核心外部基础设施 | 已确认 |
| IP | 13.107.246.45 | 背景/上下文指标 | 已确认 |
| IP | 198.51.100.44 | 背景/上下文指标 | 已确认 |
| 域名 | download.windowsupdate.com | 背景/上下文指标 | 已确认 |
| 域名 | telemetry-collector.example | 背景/上下文指标 | 已确认 |
| 指纹 | t13d1516h2_2ba4190accc0_13fe129f5abc | 扩展查询候选 | 待确认 |
| 内网 IP | 10.70.3.12 | 关联内部地址 | 已确认 |
| 内网 IP | 10.70.3.45 | 关联内部地址 | 已确认 |
| 内网 IP | 10.70.3.90 | 关联内部地址 | 已确认 |
| 内网 IP | 10.70.8.22 | 关联内部地址 | 已确认 |
| 域名 | cdn-notify-edge.net | 扩展查询候选 | 待确认 |
| 域名 | hunt.io | 扩展查询候选 | 待确认 |
| 域名 | malpedia.caad.fkie.fraunhofer.de | 扩展查询候选 | 待确认 |
| 域名 | thedfirreport.com | 扩展查询候选 | 待确认 |
| 域名 | www.activecountermeasures.com | 扩展查询候选 | 待确认 |
| 域名 | www.microsoft.com | 扩展查询候选 | 待确认 |
| 域名 | www.safeaeon.com | 扩展查询候选 | 待确认 |

## 2. 关键对象清单

| 对象 | 类型 | 当前角色 | 是否已纳入证据链 | 备注 |
| -- | -- | -- | -- | -- |
| ws-eng-02 | 资产 | 种子资产 | 是 | 种子告警首先落在该资产上。；当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| ws-eng-05 | 资产 | 已确认受影响资产 | 是 | 当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。；事件聚类中仍需独立验证的候选资产。；候选事件缺少可独立验证指示物，当前只作为上下文边界保留。 |
| ws-eng-09 | 资产 | 待确认关联资产 | 否 | 当前仍需独立验证是否真正受影响。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。；事件聚类中仍需独立验证的候选资产。 |
| 203.0.113.77 | IP | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选地址。 |
| sync-cdn-notify.net | 域名 | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选域名。 |
| 13.107.246.45 | IP | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选地址。 |
| 198.51.100.44 | IP | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选地址。；候选事件涉及的地址，尚待独立验证。 |
| download.windowsupdate.com | 域名 | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| telemetry-collector.example | 域名 | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| t13d1516h2_2ba4190accc0_13fe129f5abc | JA4 | 种子命中指纹 | 是 | 种子告警命中的关键通信指纹。 |
| Suspected multi-host beacon spread | 家族提示 | 家族/工具提示 | 是 | 辅助解释本案的家族或工具背景，不直接等于强归因。 |
| 10.70.3.12 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.70.3.45 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。；当前 pivot 里出现的扩线候选地址。 |
| 10.70.3.90 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。；当前 pivot 里出现的扩线候选地址。；候选事件缺少可独立验证指示物，当前只作为上下文边界保留。 |
| 10.70.8.22 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| ws-lab-02 | 资产 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选资产。 |
| cdn-notify-edge.net | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。；候选事件涉及的域名，尚未进入已确认主链。 |
| hunt.io | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| malpedia.caad.fkie.fraunhofer.de | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| thedfirreport.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.activecountermeasures.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.microsoft.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.safeaeon.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| t13d1516h2_2ba4190accc0_13fe129f5abc | 指纹 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选指纹。 |

## 3. 关键证据细目

### 证据 E-01
- **证据类型**：主支撑证据
- **证据类别**：调查触发证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:10:00 UTC，资产 `ws-eng-02` 围绕 203.0.113.77 / sync-cdn-notify.net 的异常通信触发了种子告警。
- **为什么重要**：这是种子命中的直接落点，说明调查起点并不是孤立的指纹命中。
- **边界与限制**：单条种子命中本身不能独立证明事件成立，仍需要结合重复通信、资产背景或主机侧线索共同定性。
- **主要依据来源**：种子事件窗观测、关联事件扩查、显式反证检查
- **观测引用**：`obs-001, obs-005, obs-007, obs-010`

### 证据 E-02
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:28:40 UTC，资产 `ws-eng-05` 再次与 203.0.113.77 / sync-cdn-notify.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、资产背景核查、关联事件扩查、显式反证检查
- **观测引用**：`obs-001, obs-004, obs-005, obs-006, obs-007, obs-010`

### 证据 E-03
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:04:00 UTC，资产 `ws-eng-02` 先解析了少见域名 `sync-cdn-notify.net`，为后续与同一基础设施的通信提供了准备。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查、显式反证检查
- **观测引用**：`obs-001, obs-005, obs-007, obs-010`

### 证据 E-04
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（主机）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:18:00 UTC，资产 `ws-eng-02` 在异常通信后出现 `rundll32.exe` 可疑启动，伴随 DLL 加载，路径位于用户可写目录。
- **为什么重要**：它把异常从通信层推进到主机执行或其他更高风险阶段，对事件定性的权重明显更高。
- **边界与限制**：当前仍缺少更细的命令行、落地文件、持久化或内存证据，因此可以确认存在执行线索，但还不能完整描述具体载荷。
- **主要依据来源**：种子事件窗观测、关联事件扩查、显式反证检查
- **观测引用**：`obs-001, obs-005, obs-007, obs-010`

### 证据 E-05
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:21:10 UTC，资产 `ws-eng-02` 出现指向 `10.70.3.45` 的远程服务创建，说明异常已经开始跨主机推进。
- **为什么重要**：它表明异常已经从外联推进到跨主机动作，比单纯重复通信更能支撑事件升级。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查、显式反证检查
- **观测引用**：`obs-001, obs-005, obs-007, obs-010`

### 证据 E-06
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（主机）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:39:20 UTC，资产 `ws-eng-05` 出现指向 `10.70.3.90` 的远程进程创建。
- **为什么重要**：这说明异常已经从种子资产延伸到其他主机，事件范围不再局限于单点。
- **边界与限制**：当前仍缺少更细的命令行、落地文件、持久化或内存证据，因此可以确认存在执行线索，但还不能完整描述具体载荷。
- **主要依据来源**：资产背景核查、关联事件扩查、候选对象情报验证、显式反证检查
- **观测引用**：`obs-004, obs-005, obs-006, obs-007, obs-009, obs-010`

### 证据 E-07
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：中
- **事实描述**：在 2026-07-14 01:44:10 UTC，资产 `ws-eng-09` 先解析了少见域名 `cdn-notify-edge.net`。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：这条观测本身更适合用来补足上下文，而不是单独承担最终结论。
- **主要依据来源**：关联事件扩查、资产背景核查、显式反证检查
- **观测引用**：`obs-005, obs-006, obs-007, obs-010`

### 证据 C-01
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（资产背景）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:24:00 UTC，资产 `ws-eng-05` 同时间窗存在已批准的维护、变更或备份活动，可解释部分日常管理行为。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：资产背景核查、关联事件扩查、显式反证检查
- **观测引用**：`obs-004, obs-005, obs-006, obs-007, obs-010`

### 证据 C-02
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:26:00 UTC，资产 `ws-eng-05` 与已批准的供应商或更新端点通信，更接近计划内流量。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：资产背景核查、关联事件扩查、显式反证检查
- **观测引用**：`obs-004, obs-005, obs-006, obs-007, obs-010`

## 4. 观测引用对照

| 观测编号 | 来源工具 | 事实摘要 | 关系 |
| -- | -- | -- | -- |
| `obs-001` | `search_seed_context` | 检索到 6 条偏可疑事件，围绕 sync-cdn-notify.net、203.0.113.77、10.70.3.45 展开。 | `supporting` |
| `obs-002` | `local_intel_lookup` | 本地情报库未命中该指示物。 | `context` |
| `obs-003` | `malware_profile_lookup` | malware_profile_lookup 返回了 8 条候选技术来源，可用于后续正文证据提取。 | `context` |
| `obs-004` | `expand_asset_scope` | 扩线检索到 4 条可疑关联事件，围绕 sync-cdn-notify.net、cdn-notify-edge.net、203.0.113.77 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-005` | `search_related_events` | 扩线检索到 10 条可疑关联事件，围绕 sync-cdn-notify.net、cdn-notify-edge.net、203.0.113.77 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-006` | `expand_asset_scope` | 扩线检索到 6 条可疑关联事件，围绕 sync-cdn-notify.net、cdn-notify-edge.net、203.0.113.77 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-007` | `search_related_events` | 扩线检索到 11 条可疑关联事件，围绕 sync-cdn-notify.net、cdn-notify-edge.net、203.0.113.77 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-008` | `fetch_page_content` | 成功读取技术页面正文，可继续抽取 claim 和实体。 | `context` |
| `obs-009` | `ground_candidate_event` | 候选事件 evt-811 缺少可独立验证的指示物，当前不并入主证据链，仅保留为边界线索。 | `context` |
| `obs-010` | `check_counterevidence` | 显式反证检查发现 2 条更接近维护、更新、补丁或备份背景的事件。 | `counterevidence` |

## 5. 正文章节引用映射

| 正文章节 | 主要 evidence / observation | 用途 |
| -- | -- | -- |
| 首页摘要 | E-01、E-02、validate_candidate_events | 当前状态 / 当前最强证据 / 当前最关键缺口 |
| 第 5 节 | E-01、E-02、E-03、E-04、E-05、E-06、E-07、C-01、C-02 | 主支撑证据 / 反证与替代解释 |
| 第 9 节 | validate_candidate_events | 证据链摘要 / 缺口说明 |
| 第 10 节 | E-01、E-02 | 结论 / 动作 / 复核重点 |

## 6. 证据来源与参考资料

| 来源类型 | 来源名称 | 用途 | 边界 |
| -- | -- | -- | -- |
| 上游检测 | t13d1516h2_2ba4190accc0_13fe129f5abc / Suspected multi-host beacon spread | 提供本案的调查起点与初始命中线索。 | 命中本身并不自动等于事件成立，仍需要后续本地事实补强。 |
| 内部观测 | trace_store.seed_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 外部结构化情报 | intel_tool | 补充基础设施、家族或公开背景解释。 | 外部情报不能单独替代本地事实，也不能直接把本案写成强归因。 |
| 内部观测 | trace_store.asset_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 内部观测 | trace_store.related_events | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 外部结构化情报 | https://malpedia.caad.fkie.fraunhofer.de/details/win.cobalt_strike | 补充基础设施、家族或公开背景解释。 | 外部情报不能单独替代本地事实，也不能直接把本案写成强归因。 |
| 外部结构化情报 | candidate_event:evt-811 | 补充基础设施、家族或公开背景解释。 | 外部情报不能单独替代本地事实，也不能直接把本案写成强归因。 |
| 背景核查 | trace_store.asset_context | 验证维护窗口、补丁、备份或共享基线等替代解释。 | 反证核查只能说明是否存在降级解释，不能替代主证据链。 |

## 7. 证据缺口与待补查询

| 缺口 | 限制的结论 | 当前是否可补 | 最值得补的查询 |
| -- | -- | -- | -- |
| 扩线得到的 4 条关联事件尚未完成独立验证，是否应并入主事件范围？ | 当前仍缺少对“扩线得到的 4 条关联事件尚未完成独立验证，是否应并入主事件范围？”的直接补证。 | 否 | 该缺口已多轮核查但仍未获得新的有效信息，更适合作为报告未决事项。 |
