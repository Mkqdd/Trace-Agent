# 首页摘要

- **结论：** 确认安全事件。
- **严重度：** 高危。
- **研判把握：** 高（94%），主证据链闭环，可交付事件级结论。
- **已确认范围：** `web-app-01` 已确认受影响并产生外联信标与执行线索；`app-db-01` 上存在已被确认的可疑外联信号且已支撑受影响认定；扩线候选资产 `web-app-03` 与若干候选域名/IP 尚待独立验证。
- **一句话结论：** 面向公网的 web 服务器 `web-app-01` 在利用尝试后解析并外联至 `cdn-auth-check.net`/`198.51.100.88`，随后出现 WebShell 落地、PowerShell 启动器执行、凭据访问及指向 `app-db-01` 的 SMB 横向移动线索；数据库服务器 `app-db-01` 随后同样外联同一信标基础设施，形成“Web 初始访问-信标-横向移动-二次信标”的已确认证据链。
- **立即动作：** 隔离 `web-app-01` 和 `app-db-01`；在边界和代理设备上封禁 `198.51.100.88` 和 `cdn-auth-check.net`；对候选事件逐条补做独立验证并在同时间窗内基于核心指标继续检索重复通信。

## 1. 事件结论与当前判断

本次事件的分析窗口为 2026-05-11 14:02:00 UTC 至 2026-05-11 14:55:00 UTC。种子告警落在内网主机 `web-app-01`，并由 JA4 指纹 `t13d1517h2_91adbe210bc1_7cae1281e991` 触发，告警附带的家族提示为“Confirmed web initial access to beacon chain”。围绕该种子衍生出九条已被确认为可疑或恶意的关联事件，共同构成了一条包含初始访问、信标通信、文件落地、可疑进程执行、凭据访问和横向移动线索的主证据链。整体证据闭合程度高，已足以将本事件定性为“确认安全事件”。

目前的主线结论为：`web-app-01` 作为面向公网的 Web 服务器，在遭到针对登录端点的利用尝试后不久解析了罕见域名 `cdn-auth-check.net` 并与之建立加密 TLS 信标通信，同时匹配了已知的 JA4 告警指纹。随后的主机侧证据包括 WebShell 文件落地、PowerShell 启动器执行、凭据存储访问，以及指向数据库服务器 `app-db-01` 的 SMB 连接。`app-db-01` 随后亦解析同一域名并外联至 `198.51.100.88`，形成跨资产的二次信标。上述事实链不依赖于任何单一推断，而是在时间线、对象关系和阶段标签上形成连贯闭合。当前结论已充分支撑“确认事件”交付，但尚不能据此得出攻击者完全控制、恶意软件完整执行细节、持久化机制或更广泛的横向移动闭环等更强结论，后者受到若干未闭合缺口和反证材料的限制。

## 2. 事件过程与关键时间线

分析窗口的起点为 2026-05-11 14:02:00 UTC，当时资产 `web-app-01` 收到针对登录端点的利用请求（`fact-event-evt-301`）。此后在同一时间窗口内观察到 WAF 遥测中更早出现的是被阻断的通用扫描器载荷（待验证背景事件，`fact-event-evt-302`），但这一信号本身并不改变主链方向。

主机侧活动在 14:07:30 UTC 出现明确转向：`web-app-01` 解析了域名 `cdn-auth-check.net`（`fact-event-evt-303`），随后在 14:12:00 UTC 触发种子告警，即命中 JA4 指纹的 TLS 信标通信，目标为 `198.51.100.88`，并与同一域名关联（`fact-event-evt-304`）。紧接着在 14:14:10 UTC，同一主机再次向该外部基础设施发起外联，构成典型的信标重连行为（`fact-event-evt-305`）。

落地和本地执行阶段在 14:16:30 UTC 至 14:21:00 UTC 之间展开。`web-app-01` 上出现了名为 `shell.aspx` 的 WebShell 文件，位于 `/var/www/html/.cache` 目录下（`fact-event-evt-306`），此后在 14:18:10 UTC 观察到 PowerShell 启动器在 `w3wp` 下执行并触及同一临时缓存路径（`fact-event-evt-307`）。14:21:00 UTC 则记录了从 Web 工作进程账户对凭据存储的访问，紧跟在 PowerShell 活动之后（`fact-event-evt-308`）。

横向移动线索出现在 14:24:20 UTC，此时 `web-app-01` 通过 SMB 连接到 `app-db-01`，对应 IP 为 `10.60.2.34`（`fact-event-evt-309`）。此后资产关系出现扩展：14:27:00 UTC，`app-db-01` 解析了同一个罕见域名 `cdn-auth-check.net`（此事件为候选扩线事件，尚未独立验证，`fact-event-evt-310`），并在 14:31:30 UTC 以确认的主证据形式直接外联至 `198.51.100.88`（`fact-event-evt-311`）。这构成了“Web 服务器-数据库服务器”之间的关联外联链。

后续可选扩线线索尚未进入已确认主线。14:34:40 UTC 出现从 `web-app-01` 到 `app-db-01` 的远程服务创建信号（候选事件，`fact-event-evt-312`）。另外，`web-app-03` 在 14:42:00 UTC 解析了同 IP 上的同级域名 `api-cdn-auth-check.net`（背景/替代解释事件，`fact-event-evt-313`），并在 14:45:20 UTC 通过扫描遥测厂商域名 `scanner-cdn.example` 连接至同一 IP（背景/替代解释事件，`fact-event-evt-314`）。这些边界信号有待独立验证，不能直接写入已确认传播范围。此外，14:50:00 UTC 出现计划内热修复部署（背景事件，`fact-event-evt-315`），以及 14:55:00 UTC 发现 `web-app-01` 上脚本块日志被禁用（候选事件，`fact-event-evt-316`），可能影响对 PowerShell 命令行细节的重建。

## 3. 关键证据判断

本报告将事件定性为“确认安全事件”的核心依据并非仅靠种子告警本身，而是在一个连贯时间窗口内累积了多条互相印证的已确认事实。最早的登录端点利用请求为初始突破提供了入口猜想，而随后立即发生的 DNS 解析和外联通信则将入口猜想转化为异常流量证据。JA4 指纹命中直接与“Confirmed web initial access to beacon chain”的家族提示对齐，同时该指纹本身在多起安全事件中被用作信标特征，进一步强化了恶意性判断。WebShell 文件 `shell.aspx` 的落地位置 `/var/www/html/.cache` 与 PowerShell 启动器触及的路径完全一致，说明文件投放和后续执行之间存在空间上的闭合关系。

在横向移动和信标扩展这一侧，`web-app-01` 向 `app-db-01` 发起的 SMB 连接是已确认事件而非候选线索，它建立了两个资产之间的接触点。更重要的事实来自 `app-db-01` 本身：在横向移动信号出现后，该数据库服务器不仅解析了同样的罕见域名 `cdn-auth-check.net`，而且随后直接外联至 `198.51.100.88`，这两项均为已确认主证据链的一部分。这种在第二跳资产上再现的相同信标行为将整个事件从“单主机外联”提升到“跨资产信标扩展”，为确认事件提供了多资产支撑。

尽管如此，当前证据尚不能进一步支撑“已观察到完全控制系统”“持久化机制已植入”或“已观察到实现了数据外传”等更强结论。从主机侧看，PowerShell 启动器虽然被执行，但脚本块日志已被禁用，使得命令行细节、载荷来源和执行范围无法得到完整确认；凭据访问行为虽发生，但尚未能证明凭据已被导出或重用。从网络侧看，`app-db-01` 的外联流量虽然与信标一致，但仍需进一步核查其来源进程和载荷落地情况，才能确认该外联是否确实由同一攻击者驱动。同样，围绕 `web-app-03` 的各项信号目前只能归入候选扩展或背景解释，不能作为已确认的受影响资产写入结论。因此，现有证据足以确认事件成立，但横向传播的闭环、持久化和攻击者完全控制等断言必须在进一步取证和验证后才能成立。

## 4. 影响范围、候选对象与外部基础设施

本报告已纳入确认受影响范围的资产包括 `web-app-01` 和 `app-db-01`。`web-app-01` 作为种子资产，在其上观察到完整的初始访问、信标通信、文件落地、执行和凭据访问，并作为横向移动的起点。`app-db-01` 虽未在种子告警中直接出现，但通过已确认的横向 SMB 连接与随后的信标外联确立了其受影响地位，目前应视为确认受影响资产进行隔离和取证。该资产既参与了横向移动接收，又复现了与 `web-app-01` 一致的外部基础设施通信模式，表明其已暴露在同一威胁活动中。

在候选范围侧，资产 `web-app-03` 目前仅具有微弱但需关注的线索，包括解析同级域名 `api-cdn-auth-check.net` 以及通过 `scanner-cdn.example` 连接至 `198.51.100.88`。这些信号虽然指向同一 IP 和相似域名体系，但尚未经过独立验证，也不具备类似 `web-app-01` 上的主机侧执行证据或信标重连行为，因此不能将其写成已确认受影响资产。另外，部分候选域名如 `0xdbgman.github.io`、`socket.io`、`vercel.app`、`www.microsoft.com`、`www.trendmicro.com` 等出现在扩展检索结果中，当前仅作为关联上下文保存，不代表它们属于关联外部基础设施。

外部基础设施方面，本事件的核心外联对象为 IP 地址 `198.51.100.88` 和域名 `cdn-auth-check.net`，两者应在边界和代理设备上排查与封禁。这些外部节点具备以下特点：在已确认主线事件中多次出现，关联信标重连，连接源涉及至少两个内部资产，并且该域名在大范围网络中属于罕见请求。其他外部域名如 `scanner-cdn.example` 属于背景上下文或扫描器遥测厂商基础设施，不构成当前事件的恶意核心，但仍可保留作为排查参考。所有外部 IP 和域名均不可视为“受影响资产”，而是外联对象、关联基础设施或封禁排查目标。

## 5. 反证、替代解释与未闭合缺口

当前证据集合中包含若干背景事件和替代解释，其作用在于限制结论的上限，而非推翻已确认主链。例如，`web-app-01` 在利用尝试之前已有 WAF 被阻断的通用扫描器载荷，这表明恶意请求可能具有扫描性质，也说明部分流量被边界阻断，但这并不能消除随后信标通信、文件落地和执行活动的恶意色彩。类似地，`web-app-03` 在同 IP 上的外联流量既可能是共享托管或扫描器遥测导致的良性通信，也可能是同一攻击者在更多资产上的有限痕迹，但仅凭单次 DNS 解析和通过扫描器域名的连接远不足以将其标记为受影响资产。同样，14:50 UTC 启动的计划内热修复部署只能解释窗口末端可能的服务重启行为，但不影响之前已发生的利用请求、信标通信和执行链。

存在两个未闭合缺口直接影响对更强结论的支撑。第一个缺口是围绕候选事件的独立验证尚未完成，包括 `app-db-01` 的 DNS 解析是否来自恶意进程、`web-app-01` 发起的远程服务创建是否成功落地，以及 `web-app-03` 的同类信号是否属于同一攻击者行为。这些事件若被证实，将扩大受影响范围并强化横向移动闭环；若被解释为良性或噪声，则只会限制边界而不会回退当前主链。第二个缺口是与反证和基线的显式核对尚未完成。虽然维护窗口和 WAF 阻断事件已作为背景事实被纳入分析，但尚未系统性地对比正常基线通信模式、过往维护记录和备份完整性，以确保没有将良性活动误判为恶意环节。这些缺口是后续取证和二次分析的重点方向，其解决程度将决定能否进一步给出攻击者持久化、完整执行链或更大范围传播等结论。

## 6. 处置建议与后续核查

- **隔离受影响资产并进行主机取证：** 重点对象为 `web-app-01` 和 `app-db-01`。应在这些资产上收集进程列表、计划任务、服务项、临时目录（特别是 `/var/www/html/.cache`）下的文件哈希、安全审计日志和应用日志，以确认是否存在持久化机制或其他落地载荷，并补全 PowerShell 命令行细节。
- **封禁外部基础设施并排查历史通信：** 在边界防火墙上对 `198.51.100.88` 和 `cdn-auth-check.net` 实施出站封禁，同时在代理、DNS 和网络流量日志中检索同时间窗或更大回溯窗口内涉及这些指标的所有通信，确认是否存在其他内网资产接触过同一基础设施。
- **验证候选扩线事件：** 针对 `fact-event-evt-310`（`app-db-01` 解析 `cdn-auth-check.net`）、`fact-event-evt-312`（`web-app-01` 向 `app-db-01` 远程服务创建）和 `fact-event-evt-313`/`fact-event-evt-314`（`web-app-03` 的同 IP 外联），逐条使用独立日志源进行确认比对，重点包括进程创建日志、服务安装事件、文件哈希以及 DNS 解析上下文，确认是否可并入主事件受影响范围。
- **补足主机端执行和数据访问证据：** 在 `web-app-01` 上重新打开并审计脚本块日志，同时从 Sysmon、安全事件日志和 EDR 遥测中检索 `w3wp` 及其子进程的参数、文件访问和注册表操作，验证 PowerShell 活动的完整上下文以及凭据存储访问的后续影响。
- **二次扩线检索与基线对比：** 以 JA4 指纹 `t13d1517h2_91adbe210bc1_7cae1281e991`、核心域名 `cdn-auth-check.net` 和核心 IP `198.51.100.88` 作为 关键关联指标，在全网流量与终端日志中检索同时间窗内的重复出现情况，并结合正常运营基线判断是否存在更大范围的隐蔽通信或横向移动。

---

# 事件调查技术附录

以下内容主要供分析、复盘和技术排查使用，不直接替代主报告结论。

## 1. IOC / IOA 清单

| 类型 | 值 | 角色 | 状态 |
| -- | -- | -- | -- |
| IP | 198.51.100.88 | 核心外部基础设施 | 已确认 |
| 域名 | cdn-auth-check.net | 核心外部基础设施 | 已确认 |
| 域名 | scanner-cdn.example | 背景/上下文指标 | 已确认 |
| 指纹 | t13d1517h2_91adbe210bc1_7cae1281e991 | 扩展查询候选 | 待确认 |
| 内网 IP | 10.60.1.14 | 关联内部地址 | 已确认 |
| 内网 IP | 10.60.1.23 | 关联内部地址 | 已确认 |
| 内网 IP | 10.60.2.34 | 关联内部地址 | 已确认 |
| 域名 | 0xdbgman.github.io | 扩展查询候选 | 待确认 |
| 域名 | api-cdn-auth-check.net | 扩展查询候选 | 待确认 |
| 域名 | redcanary.com | 扩展查询候选 | 待确认 |
| 域名 | socket.io | 扩展查询候选 | 待确认 |
| 域名 | socprime.com | 扩展查询候选 | 待确认 |
| 域名 | vercel.app | 扩展查询候选 | 待确认 |
| 域名 | www.microsoft.com | 扩展查询候选 | 待确认 |
| 域名 | www.picussecurity.com | 扩展查询候选 | 待确认 |
| 域名 | www.trendmicro.com | 扩展查询候选 | 待确认 |

## 2. 关键对象清单

| 对象 | 类型 | 当前角色 | 是否已纳入证据链 | 备注 |
| -- | -- | -- | -- | -- |
| web-app-01 | 资产 | 种子资产 | 是 | 种子告警首先落在该资产上。；当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。；事件聚类中仍需独立验证的候选资产。 |
| app-db-01 | 资产 | 已确认受影响资产 | 是 | 当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。；事件聚类中仍需独立验证的候选资产。 |
| web-app-03 | 资产 | 待确认关联资产 | 否 | 当前仍需独立验证是否真正受影响。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| 198.51.100.88 | IP | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选地址。 |
| cdn-auth-check.net | 域名 | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选域名。；候选事件涉及的域名，尚未进入已确认主链。 |
| scanner-cdn.example | 域名 | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| t13d1517h2_91adbe210bc1_7cae1281e991 | JA4 | 种子命中指纹 | 是 | 种子告警命中的关键通信指纹。 |
| Confirmed web initial access to beacon chain | 家族提示 | 家族/工具提示 | 是 | 辅助解释本案的家族或工具背景，不直接等于强归因。 |
| 10.60.1.14 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.60.1.23 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.60.2.34 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。；当前 pivot 里出现的扩线候选地址。；候选事件涉及的地址，尚待独立验证。 |
| 0xdbgman.github.io | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| api-cdn-auth-check.net | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| redcanary.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| socket.io | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| socprime.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| vercel.app | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.microsoft.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.picussecurity.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.trendmicro.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| t13d1517h2_91adbe210bc1_7cae1281e991 | 指纹 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选指纹。 |

## 3. 关键证据细目

### 证据 E-01
- **证据类型**：主支撑证据
- **证据类别**：调查触发证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-05-11 14:12:00 UTC，资产 `web-app-01` 围绕 198.51.100.88 / cdn-auth-check.net 的异常通信触发了种子告警。
- **为什么重要**：这是种子命中的直接落点，说明调查起点并不是孤立的指纹命中。
- **边界与限制**：单条种子命中本身不能独立证明事件成立，仍需要结合重复通信、资产背景或主机侧线索共同定性。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-005, obs-006, obs-007, obs-010`

### 证据 E-02
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-05-11 14:31:30 UTC，资产 `app-db-01` 再次与 198.51.100.88 / cdn-auth-check.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、资产背景核查、关联事件扩查
- **观测引用**：`obs-001, obs-004, obs-005, obs-006, obs-007, obs-008, obs-010`

### 证据 E-03
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-05-11 14:07:30 UTC，资产 `web-app-01` 先解析了域名 `cdn-auth-check.net`，为后续与同一基础设施的通信提供了准备。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-005, obs-006, obs-007, obs-010`

### 证据 E-04
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（主机）
- **证据强度**：高
- **事实描述**：在 2026-05-11 14:24:20 UTC，资产 `web-app-01` 出现指向 `10.60.2.34` 的横向操作告警，说明异常已经开始跨主机推进。
- **为什么重要**：它表明异常已经从外联推进到跨主机动作，比单纯重复通信更能支撑事件升级。
- **边界与限制**：当前仍缺少更细的命令行、落地文件、持久化或内存证据，因此可以确认存在执行线索，但还不能完整描述具体载荷。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-005, obs-006, obs-007, obs-010`

### 证据 E-05
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（主机）
- **证据强度**：高
- **事实描述**：在 2026-05-11 14:18:10 UTC，资产 `web-app-01` 在异常通信后出现可疑进程执行。
- **为什么重要**：它把异常从通信层推进到主机执行或其他更高风险阶段，对事件定性的权重明显更高。
- **边界与限制**：当前仍缺少更细的命令行、落地文件、持久化或内存证据，因此可以确认存在执行线索，但还不能完整描述具体载荷。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-005, obs-006, obs-007, obs-010`

### 证据 E-06
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：中
- **事实描述**：在 2026-05-11 14:42:00 UTC，资产 `web-app-03` 先解析了少见域名 `api-cdn-auth-check.net`。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：这条观测本身更适合用来补足上下文，而不是单独承担最终结论。
- **主要依据来源**：关联事件扩查、资产背景核查
- **观测引用**：`obs-006, obs-007, obs-008, obs-010`

### 证据 C-01
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（上下文）
- **证据强度**：高
- **事实描述**：在 2026-05-11 14:04:40 UTC，资产 `web-app-01` 同时间窗存在更接近计划内活动的背景线索。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-005, obs-006, obs-007, obs-010`

### 证据 C-02
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（网络）
- **证据强度**：高
- **事实描述**：在 2026-05-11 14:45:20 UTC，资产 `web-app-03` 存在共享基础设施背景，相关外部对象不能仅凭一次共现就升格为攻击基础设施。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：关联事件扩查、资产背景核查
- **观测引用**：`obs-005, obs-006, obs-007, obs-008, obs-010`

## 4. 观测引用对照

| 观测编号 | 来源工具 | 事实摘要 | 关系 |
| -- | -- | -- | -- |
| `obs-001` | `search_seed_context` | 检索到 9 条偏可疑事件，围绕 cdn-auth-check.net、198.51.100.88、10.60.2.34 展开。 | `supporting` |
| `obs-002` | `local_intel_lookup` | 本地情报库未命中该指示物。 | `context` |
| `obs-003` | `malware_profile_lookup` | malware_profile_lookup 返回了 8 条候选技术来源，可用于后续正文证据提取。 | `context` |
| `obs-004` | `expand_asset_scope` | 扩线检索到 2 条可疑关联事件，围绕 cdn-auth-check.net、198.51.100.88 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-005` | `search_related_events` | 扩线检索到 12 条可疑关联事件，围绕 cdn-auth-check.net、198.51.100.88、10.60.2.34 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-006` | `search_related_events` | 扩线检索到 12 条可疑关联事件，围绕 cdn-auth-check.net、198.51.100.88、10.60.2.34 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-007` | `search_related_events` | 扩线检索到 12 条可疑关联事件，围绕 cdn-auth-check.net、198.51.100.88、10.60.2.34 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-008` | `expand_asset_scope` | 扩线检索到 2 条可疑关联事件，围绕 cdn-auth-check.net、198.51.100.88 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-009` | `fetch_page_content` | 成功读取技术页面正文，可继续抽取 claim 和实体。 | `context` |
| `obs-010` | `search_related_events` | 扩线检索到 12 条可疑关联事件，围绕 cdn-auth-check.net、198.51.100.88、10.60.2.34 展开；当前先作为待验证候选线索。 | `candidate` |

## 5. 正文章节引用映射

| 正文章节 | 主要 evidence / observation | 用途 |
| -- | -- | -- |
| 首页摘要 | E-01、E-02、validate_candidate_events | 当前状态 / 当前最强证据 / 当前最关键缺口 |
| 第 5 节 | E-01、E-02、E-03、E-04、E-05、E-06、C-01、C-02 | 主支撑证据 / 反证与替代解释 |
| 第 9 节 | validate_candidate_events、check_counterevidence | 证据链摘要 / 缺口说明 |
| 第 10 节 | E-01、E-02 | 结论 / 动作 / 复核重点 |

## 6. 证据来源与参考资料

| 来源类型 | 来源名称 | 用途 | 边界 |
| -- | -- | -- | -- |
| 上游检测 | t13d1517h2_91adbe210bc1_7cae1281e991 / Confirmed web initial access to beacon chain | 提供本案的调查起点与初始命中线索。 | 命中本身并不自动等于事件成立，仍需要后续本地事实补强。 |
| 内部观测 | trace_store.seed_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 外部结构化情报 | intel_tool | 补充基础设施、家族或公开背景解释。 | 外部情报不能单独替代本地事实，也不能直接把本案写成强归因。 |
| 内部观测 | trace_store.asset_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 内部观测 | trace_store.related_events | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 外部结构化情报 | https://www.microsoft.com/en-us/security/blog/2026/03/11/contagious-interview-malware-delivered-through-fake-developer-job-interviews/ | 补充基础设施、家族或公开背景解释。 | 外部情报不能单独替代本地事实，也不能直接把本案写成强归因。 |

## 7. 证据缺口与待补查询

| 缺口 | 限制的结论 | 当前是否可补 | 最值得补的查询 |
| -- | -- | -- | -- |
| 扩线得到的 3 条关联事件尚未完成独立验证，是否应并入主事件范围？ | 当前仍缺少对“扩线得到的 3 条关联事件尚未完成独立验证，是否应并入主事件范围？”的直接补证。 | 是 | 扩线得到的 3 条关联事件尚未完成独立验证，是否应并入主事件范围？ |
| 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 | 当前不能完全排除维护窗口、共享基础设施或正常组件造成的替代解释。 | 是 | 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 |
