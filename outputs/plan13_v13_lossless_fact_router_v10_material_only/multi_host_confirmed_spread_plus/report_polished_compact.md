# 首页摘要

- **结论**：可疑事件，建议继续复核。
- **严重度**：中
- **研判把握**：中
- **已确认范围**：`ws-eng-02` 和 `ws-eng-05`
- **最强证据**：可疑 TLS 会话和 JA4 告警命中指向罕见外部基础设施。
- **关键缺口**：仍需围绕当前关键关联指标做一次显式扩线，确认是否存在同指标的更大范围复现。
- **立即动作**：对重点资产补采主机侧日志，确认是否存在执行、持久化或横向移动证据。
- **一句话结论**：当前只能依据已整理的交付判断和证据包保守生成报告材料。

## 调查起点与已知线索

可疑事件的调查始于种子告警，该告警自带家族/工具提示：Suspected multi-host beacon spread。2026年7月14日01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`，源地址为 `10.70.3.12`。随后，在01:10:00 UTC，`ws-eng-02` 发出了指向罕见外部基础设施的可疑 TLS 信标，涉及域名 `sync-cdn-notify.net` 和 IP 地址 `203.0.113.77`。这些事件构成了初步的调查起点，需要进一步研判。

## 调查范围与研判假设

当前调查范围包括已确认受影响的资产 `ws-eng-02` 和 `ws-eng-05`，以及待确认的资产 `ws-eng-09`。核心外部基础设施包括域名 `sync-cdn-notify.net` 和 IP 地址 `203.0.113.77`。这些对象的角色边界明确，外部基础设施只能作为外联对象或排查封禁对象，而不能直接认定为受影响资产。

## 事件过程与行为模式

事件的时间线如下：

- 2026年7月14日01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`，源地址为 `10.70.3.12`。
- 01:10:00 UTC，`ws-eng-02` 发出了指向罕见外部基础设施的可疑 TLS 信标，涉及域名 `sync-cdn-notify.net` 和 IP 地址 `203.0.113.77`。
- 01:14:20 UTC，`ws-eng-02` 重新连接到同一 C2 基础设施。
- 01:18:00 UTC，可疑的 `rundll32.exe` 在信标序列后从用户可写路径启动了 loader DLL。
- 01:21:10 UTC，从 `ws-eng-02` 到 `ws-eng-05` 的 PsExec 远程服务创建。
- 01:28:40 UTC，`ws-eng-05` 连接了原始信标基础设施。
- 01:32:10 UTC，`ws-eng-05` 解析了次级罕见域名 `cdn-notify-edge.net`。
- 01:36:00 UTC，`ws-eng-05` 通过 TLS 转向第二个外部 IP `198.51.100.44`。
- 01:39:20 UTC，WMI 远程进程创建从 `ws-eng-05` 到 `ws-eng-09`。
- 01:47:30 UTC，`ws-eng-09` 连接到次级基础设施。

这些事件展示了可疑行为的逐步推进，但需要进一步验证以确认其性质。

## 关键事实与证据判断

当前判断主要基于以下证据：

- 2026年7月14日01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`，源地址为 `10.70.3.12`。
- 01:10:00 UTC，`ws-eng-02` 发出了指向罕见外部基础设施的可疑 TLS 信标，涉及域名 `sync-cdn-notify.net` 和 IP 地址 `203.0.113.77`。
- 01:14:20 UTC，`ws-eng-02` 重新连接到同一 C2 基础设施。
- 01:18:00 UTC，可疑的 `rundll32.exe` 在信标序列后从用户可写路径启动了 loader DLL。
- 01:21:10 UTC，从 `ws-eng-02` 到 `ws-eng-05` 的 PsExec 远程服务创建。
- 01:28:40 UTC，`ws-eng-05` 连接了原始信标基础设施。

这些事件共同构成了当前的证据链，但需要进一步验证以确认其性质。

## 关联范围与候选边界

当前关联范围包括已确认受影响的资产 `ws-eng-02` 和 `ws-eng-05`，以及待确认的资产 `ws-eng-09` 和 `ws-lab-02`。核心外部基础设施包括域名 `sync-cdn-notify.net` 和 IP 地址 `203.0.113.77`。候选扩线事件包括：

- 01:32:10 UTC，`ws-eng-05` 解析了次级罕见域名 `cdn-notify-edge.net`。
- 01:36:00 UTC，`ws-eng-05` 通过 TLS 转向第二个外部 IP `198.51.100.44`。
- 01:39:20 UTC，WMI 远程进程创建从 `ws-eng-05` 到 `ws-eng-09`。
- 01:47:30 UTC，`ws-eng-09` 连接到次级基础设施。

这些候选扩线事件尚未独立验证，只能作为待确认扩线或边界线索。

## 反证、缺口与结论边界

当前结论受到以下缺口和背景事件的限制：

- 仍需围绕当前关键关联指标做一次显式扩线，确认是否存在同指标的更大范围复现。
- 扩线得到的 5 条关联事件尚未完成独立验证，是否应并入主事件范围？
- 仍需显式检查维护窗口、补丁、备份或共享基线等反证。
- 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。

背景事件包括：

- 01:24:00 UTC，`ws-eng-05` 开始批准的 SCCM 补丁暂存。
- 01:26:00 UTC，`ws-eng-05` 从批准的 Microsoft 端点下载了 Windows 更新内容。
- 01:44:10 UTC，`ws-eng-09` 解析了次级域名 `cdn-notify-edge.net`。
- 01:46:20 UTC，`ws-lab-02` 通过不同供应商域名访问了同一托管 IP。

这些背景事件只能用于收窄边界，不能单独推翻主链。

## 结论与后续动作

当前事件的结论是可疑事件，建议继续复核。接下来的行动建议包括：

- 对重点资产补采主机侧日志，确认是否存在执行、持久化或横向移动证据。
- 继续围绕相同域名、目标 IP 和 JA4 通信指纹搜索更宽时间窗内的关联事件。
- 优先核验新扩出的关联事件是否存在独立命中，不要只因为共享基础设施就直接并入主证据链。
- 把关联资产 `ws-eng-09` 纳入复核清单，确认它们是共享基础设施背景还是真实受影响对象。

以上行动建议旨在进一步验证当前证据，并确认事件的真实性质。

技术细节、IOC/IOA、关键对象清单、观测引用、证据细目和待补缺口详见技术附录。