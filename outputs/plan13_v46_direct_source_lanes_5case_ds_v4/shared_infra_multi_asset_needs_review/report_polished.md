# 首页摘要

- **结论**：本次事件构成可疑的安全事件簇，但受限于主机侧遥测缺失和共享基础设施背景，暂无法升级为确认安全事件，当前状态为待人工复核。
- **严重度**：中危。
- **研判把握**：较高。围绕种子告警已形成由三个 TLS 会话构成的可疑时间线，但同时存在多个可解释为正常业务的背景信号，限制了更强结论的得出。
- **已确认范围**：ws-legal-03 是该事件簇中已确认的唯一受影响资产；外部共享基础设施 IP 198.51.100.131 及域名 auth-queue-sync.net 被确认为关键外联对象。
- **一句话结论**：ws-legal-03 在约十分钟内向同一共享托管 IP 上的罕见域名 auth-queue-sync.net 发起了三次 JA4 指纹匹配的 TLS 会话，行为模式符合可疑信标特征，但缺乏命令行、执行链或持久化等主机侧证据，且存在 SSO 扩展更新、正常 Azure AD 流量及多租户共享 IP 等替代解释，因此事件性质仍待进一步复核。
- **立即动作**：对 ws-legal-03 启动主机侧取证补采，重点获取告警窗口内的进程命令行、网络连接对应进程和持久化项；同时在 DNS 与网络流量日志中围绕域名 auth-queue-sync.net 和 IP 198.51.100.131 展开更宽时间窗的回溯搜索。

## 1. 事件结论与当前判断

本次调查围绕一台名为 ws-legal-03 的内部工作站在 2026-06-20 10:12:00 UTC 触发的一条 TLS 层 JA4 指纹告警展开，告警提示其为“Suspicious shared-infra beacon requiring review”。告警对应的 TLS 会话指向罕见域名 auth-queue-sync.net，该域名解析到处于共享托管环境的外部 IP 198.51.100.131。告警本身并不是针对已知恶意软件家族的强匹配，而是针对通信行为特征和基础设施上下文的综合风险评估，因此家族/工具提示只能作为事件研判的辅助参考，不能直接被当作确定性归因。

从告警发生后的十分钟内，同一个工作站 ws-legal-03 又连续两次向相同的 auth-queue-sync.net 域名和共享 IP 发起短的 TLS 会话，形成了集中的、周期性的外联模式。这一系列事件已构成可疑事件簇，足以支撑将 ws-legal-03 纳入确认受影响范围，并将 auth-queue-sync.net 与 198.51.100.131 列为需要对外排查和封禁考虑的基础设施对象。但在主机侧，告警窗口内并未采集到任何命令行日志，无法验证发起这些网络连接的进程身份、是否存在恶意代码执行以及是否伴随持久化或横向移动行为。同时，ws-legal-03 在同一时间段内存在一次经过批准的 SSO 浏览器扩展更新，并在相近窗口中出现了正常的 login.microsoftonline.com 身份流量，这些背景活动为网络层的可疑信号提供了值得关注的替代解释。因此，当前事件仍处于“可疑事件，建议继续复核”阶段，判断把握较高，但交付门槛尚未闭合。

## 2. 事件过程与关键时间线

以下时间线以 2026-06-20 UTC 为分析窗口，重点突出 ws-legal-03 的外联行为演变及其对事件研判的推动作用。

- **2026-06-20 10:04:30 UTC** - 作为背景信号，ws-legal-03 在种子告警之前通过 DNS 解析了罕见域名 auth-queue-sync.net。该域名被映射到一个共享托管 IP 上。这一解析行为本身并非告警，但它为后续 TLS 会话的发起提供了时间承接和域名准备证据，提示 ws-legal-03 与目标域名的接触并非在告警瞬间才突然发生。
- **2026-06-20 10:12:00 UTC** - 种子 JA4 告警在这一时间点触发。ws-legal-03 向 auth-queue-sync.net 的共享托管 IP 198.51.100.131 发起了一条异常的 TLS 会话，命中指纹 t13d190900_7711aa22bb33_9911ccddee77。该告警不直接等同于已确认恶意通信，但它将此前单纯的域名解析行为升级为网络层可疑事件，标志着事件簇的正式起点。
- **2026-06-20 10:16:20 UTC** - 在种子告警仅四分钟后，ws-legal-03 再次向同一 auth-queue-sync.net 端点发起重连。这一行为进一步强化了信标式通信模式的存在，因为短时间内重复对外连接同一罕见域名和共享 IP，且发生在告警之后，说明工作站在告警后并未停止该行为，这与一次性误触或用户习得性浏览的典型表现存在差异。
- **2026-06-20 10:21:40 UTC** - ws-legal-03 继续向同一域名和共享 IP 发起第三次短的 TLS 会话，重新使用了同一 JA4 相关路径。至此，ws-legal-03 已在十分钟内形成三次同域名、同 IP 的集中外联，进一步巩固了预配置信标或周期性回连的可疑判断，但对主机侧执行上下文的完全缺乏，使得此阶段尚不能确认恶意软件已成功加载或运行。
- **2026-06-20 10:24:00 UTC** - 在此之后，针对 ws-legal-03 的端点遥测信息确认，该资产在相关时间窗内不具备命令行日志覆盖。这一事实直接限制了从网络层向主机层闭合执行链的可能性，意味着当前已知信标行为无法被关联到具体的进程、脚本或持久化机制，事件评判的上限也因此被限制在“网络层可疑通信”层面。

后续在窗口末尾，ws-legal-03 还解析了另一个罕见的队列主题域名 login-queue-cdn.net，并对该域名的非种子 IP 203.0.113.131 产生了一条孤立的 TLS 连接。这些行为在当前阶段只能被记录为待验证的后续候选节点，不能用于扩大已确认范围，因为缺少主机侧佐证，且该域名与种子域名不在同一 IP 上，尚无法确认是否属于同一活动簇。

## 3. 关键证据判断

种子告警所命中的 JA4 指纹 t13d190900_7711aa22bb33_9911ccddee77 是网络层加密通信的客户端 TLS 握手特征，其价值在于提供了一种可回溯、可复现的通信模式，而不是直接等同于任何已知恶意软件家族。该指纹在 ws-legal-03 随后两次重连中沿用了同一通信路径，说明种子告警并非孤立的误报，而是具有一定稳定性的通信行为。结合域名 auth-queue-sync.net 罕见且映射至共享托管 IP 的特性，这套组合为“可疑信标”的判断提供了合理的网络侧信号。

反对仓促升级为确认安全事件的关键证据局限来自两方面。一方面，在 2026-06-20 10:24:00 UTC 明确记录的 telemetry-gap 表明，ws-legal-03 在相关时间窗内没有命令行日志覆盖。这意味着无法确认是哪个进程或加载模块产生了这些 TLS 会话，也无法判断是否存在 psExec、WMI、rundll32 或落地脚本等远程执行技术。因此，虽然网络层已经观察到连串连接，但主机执行链几乎完全空白，任何试图将事件定性为“已确认可疑执行活动”或“已确认存在横向推进线索”的结论都缺乏支持，只能严格限定为“网络层可疑连接”。另一方面，种子告警所附带的家族提示属于共享基础设施信标的泛化风险提示，它本身不包含特定 APT 组织或明确恶意软件族系的强匹配标记，其作用是指引分析方向而非输出归因结论。只有在该提示与同一窗口内的主机执行证据、武器化载荷或持久化项相互印证时，才能进一步提升其权重。

综合来看，现有证据足以支撑 ws-legal-03 产生值得调查的可疑外联行为这一事件级判断，但证据覆盖的关键阶段仅停留在 DNS 解析和 TLS 连接，缺少能佐证执行、控制或横向移动的证据层。因此，当前事件的有意义部分集中在网络边界的外联指标上，而非对其背后主机已遭完全控制的确认。

## 4. 影响范围、候选对象与外部基础设施

已确认的受影响范围仅限于种子工作站 ws-legal-03，该资产通过三次 TLS 会话集中关联了同一关键外部基础设施。域名字符串 auth-queue-sync.net 和共享托管 IP 198.51.100.131 构成本事件的核心外部基础设施对象，需要作为后续边界排查和封禁评估的外联目标处理。在此范围内，ws-legal-03 的确认依据在于其网络行为已形成异常连接簇，而不是基于主机控制或持久化的证据。

在候选对象方面，另外两台工作站 ws-legal-09 和 ws-legal-12 曾在本窗口内以不同业务域名触及同一共享 IP 198.51.100.131。ws-legal-09 在 2026-06-20 10:39:00 UTC 与 10:42:00 UTC 先后访问并解析了 cdn-auth-queue.net，同时在 10:46:10 UTC 还通过 identity-cache.vendor.example 这一经批准的供应商域名连接至同一共享 IP。ws-legal-12 在 10:49:30 UTC 则通过 legal-docs-cdn.example 这一与法律文件审查相关的 SaaS 域名连往同一 IP。这三条连接虽然都触碰了种子 IP，但对于 ws-legal-09，其网络信号较弱且同时使用了已批准的供应商域名，背景倾向明显；对于 ws-legal-12，其流量更是落在日常业务 SaaS 路径上。目前没有任何主机侧证据能将它们的触碰同一 IP 的行为与种子告警的可疑信标特征绑定，因此 ws-legal-09 和 ws-legal-12 只能作为待验证的候选关联资产，不能直接并入已确认受影响范围。

外部基础设施 198.51.100.131 在被动基础设施上下文中被认定为同一周内存放了多个不相关的客户域名，而仅在本窗口内便对应了至少四个不同域名（auth-queue-sync.net、cdn-auth-queue.net、identity-cache.vendor.example、legal-docs-cdn.example）。这种多租户共享托管的现象，在缺乏发起进程侧佐证的情况下，限制了将域名访问直接等同于攻击者基础设施操作的能力。然而，auth-queue-sync.net 对告警的专注关联程度，仍足以让它作为出口侧排查和边界封禁的优先关注对象。

## 5. 反证、替代解释与未闭合缺口

当前事件中存在一组值得正视的背景信号和替代解释，这些信号虽不能单独推翻可疑信标的判断，但有效收窄了更强结论的成立空间。ws-legal-03 在 2026-06-20 10:55:00 UTC 被记录到一次经过批准的 SSO 浏览器扩展更新，这一更新与第一次可疑 TLS 会话在时间上重叠，说明存在以下可能：该 TLS 会话是 SSO 工作流中某组件基于共享 CDN 环境的更新行为，而非恶意信标。此外，在 2026-06-20 11:01:20 UTC，同一台工作站还出现了正常的 login.microsoftonline.com 身份流量，证明了该窗口内存在正常的 Azure AD 认证活动。在 ws-legal-09 和 ws-legal-12 侧，identity-cache.vendor.example 和 legal-docs-cdn.example 等域名的触碰同样表明，相关业务 SaaS 服务可能共用了同一个共享 IP，这为部分连接迹象提供了基础设施层面的背景解释。外部的被动基础设施记录更直接确认，IP 198.51.100.131 在同一周内承载了多个彼此无关的客户域名，进一步弱化了将所有触碰该 IP 的行为直接视为恶意信标的推理基础。

多项未闭合缺口继续限制事件结论向更深阶段延伸。最关键的是主机侧执行、持久化和横向移动证据完全缺失，在没有命令行、落地文件或父子进程等直接证据的情况下，网络层的可疑连线不能自动等同为恶意软件加载或攻击者操作。其次，围绕同一域名、同一目标 IP 和同一 JA4 指纹在更广时间窗以及更大规模资产范围内的复现状况尚未展开显式扩线搜索，使得当前事件规模可能被低估或高估。此外，关联资产 ws-legal-09 和 ws-legal-12 是否真正受到恶意行为影响仍悬而未决，因为这些资产同时受到遥测覆盖缺口限制和业务使用共享基础设施这两方面因素的影响。还有，种子自带的家族提示尚未经过外部情报验证，不能排除它指向的只是共享基础设施信标的一般性风险而非具体恶意家族。最后，在维护窗口、计划补丁和合法基线等反证方向上，也缺少系统性排查，进一步制约了对整体风险面的判断。

## 6. 处置建议与后续核查

以下建议均围绕明确对象、日志来源和验证目标展开，旨在弥补关键缺口并收窄事件评估的不确定性。

- **对 ws-legal-03 启动针对性的主机侧日志补采与执行链回溯**：鉴于 2026-06-20 10:04:30 至 11:18:00 UTC 之间该资产缺乏命令行遥测，应通过 EDR 回补该窗口内所有进程创建事件、子进程关系、加载 DLL、注册表持久化项以及计划任务和服务的变更日志。重点排查发起与 198.51.100.131 通信的进程及其父进程，验证是否存在 rundll32、psExec、WMI 或脚本宿主等可疑执行模式，并找出对应文件路径和命令行参数。
- **围绕外联指标实施更宽时间窗和更大范围的回溯搜索**：在 DNS 日志和网络流量全量记录中，以域名 auth-queue-sync.net、cdn-auth-queue.net、login-queue-cdn.net 以及目标 IP 198.51.100.131 和 203.0.113.131 为条件，向前回溯至少 30 天、向后扩展至当前，搜索是否存在其他内部资产发起的连接请求。同步针对种子 JA4 指纹 t13d190900_7711aa22bb33_9911ccddee77 在流量分析平台中做全企业范围的关联查询，验证是否存在相同指纹向同一共享 IP 或相关域名的更大规模复现。
- **对候选资产 ws-legal-09 和 ws-legal-12 实施区分背景与恶意的验证**：核查 ws-legal-09 与 ws-legal-12 在 2026-06-20 窗口内的 EDR 遥测是否存在与浏览器进程或受信应用无关的程序注入、异常子进程派生或持久化痕迹。验证 identity-cache.vendor.example 和 legal-docs-cdn.example 在企业环境中的正常基线分布情况，以区分共享基础设施背景使用是否与业务需求匹配。保持这两个资产为候选状态，除非出现命令行风险信号及独立的恶意行为指示，否则不建议将其升级至已确认受影响范围。
- **在边界及代理侧实施外联控制与阶段性封禁评估**：基于 auth-queue-sync.net 和 IP 198.51.100.131 是当前最直接的外联指标，应在 Web 代理和出口防火墙中临时封禁对该域名的解析及对该 IP 的直达连接，同时保持对其他候选域名（如 cdn-auth-queue.net、login-queue-cdn.net）的主动监控而不立即封禁。在完成扩线与主机侧调查后再决定是否将封禁扩大。
- **开展外部情报校验与反证排查**：对 JA4 指纹和家族提示进行公开威胁情报查询，确认是否有已知攻击活动关联记录。与此同时，核实分析窗口内是否存在计划内补丁、SCCM 部署、Windows Update 或共享基线活动，若检索到相关记录，应将其纳入最终结论判断，进一步收窄事件恶意性假设所能到达的上限。

---

# 事件调查技术附录

以下内容主要供分析、复盘和技术排查使用，不直接替代主报告结论。

## 1. IOC / IOA 清单

| 类型 | 值 | 角色 | 状态 |
| -- | -- | -- | -- |
| IP | 198.51.100.131 | 核心外部基础设施 | 已确认 |
| 域名 | auth-queue-sync.net | 核心外部基础设施 | 已确认 |
| IP | 20.190.160.22 | 背景/上下文指标 | 已确认 |
| 域名 | identity-cache.vendor.example | 背景/上下文指标 | 已确认 |
| 域名 | login.microsoftonline.com | 背景/上下文指标 | 已确认 |
| 指纹 | t13d190900_7711aa22bb33_9911ccddee77 | 扩展查询候选 | 待确认 |
| 内网 IP | 10.44.1.18 | 关联内部地址 | 已确认 |
| 内网 IP | 10.44.1.29 | 关联内部地址 | 已确认 |
| 内网 IP | 10.44.1.42 | 关联内部地址 | 已确认 |
| IP | 203.0.113.131 | 扩展查询候选 | 待确认 |
| 域名 | cdn-auth-queue.net | 扩展查询候选 | 待确认 |
| 域名 | legal-docs-cdn.example | 扩展查询候选 | 待确认 |
| 域名 | login-queue-cdn.net | 扩展查询候选 | 待确认 |

## 2. 关键对象清单

| 对象 | 类型 | 当前角色 | 是否已纳入证据链 | 备注 |
| -- | -- | -- | -- | -- |
| ws-legal-03 | 资产 | 种子资产 | 是 | 种子告警首先落在该资产上。；当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| ws-legal-09 | 资产 | 待确认关联资产 | 否 | 当前仍需独立验证是否真正受影响。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| ws-legal-12 | 资产 | 待确认关联资产 | 否 | 当前仍需独立验证是否真正受影响。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| 198.51.100.131 | IP | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选地址。 |
| auth-queue-sync.net | 域名 | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| 20.190.160.22 | IP | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选地址。 |
| identity-cache.vendor.example | 域名 | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| login.microsoftonline.com | 域名 | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| t13d190900_7711aa22bb33_9911ccddee77 | JA4 | 种子命中指纹 | 是 | 种子告警命中的关键通信指纹。 |
| Suspicious shared-infra beacon requiring review | 家族提示 | 家族/工具提示 | 是 | 辅助解释本案的家族或工具背景，不直接等于强归因。 |
| 10.44.1.18 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.44.1.29 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.44.1.42 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 203.0.113.131 | IP | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选地址。 |
| cdn-auth-queue.net | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| legal-docs-cdn.example | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| login-queue-cdn.net | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| t13d190900_7711aa22bb33_9911ccddee77 | 指纹 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选指纹。 |

## 3. 关键证据细目

### 证据 E-01
- **证据类型**：主支撑证据
- **证据类别**：调查触发证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-20 10:12:00 UTC，资产 `ws-legal-03` 出现了一条与 198.51.100.131 / auth-queue-sync.net 相关的关键观测。
- **为什么重要**：这是种子命中的直接落点，说明调查起点并不是孤立的指纹命中。
- **边界与限制**：单条种子命中本身不能独立证明事件成立，仍需要结合重复通信、资产背景或主机侧线索共同定性。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-02
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-20 10:21:40 UTC，资产 `ws-legal-03` 再次与 198.51.100.131 / auth-queue-sync.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-03
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-20 10:16:20 UTC，资产 `ws-legal-03` 再次与 198.51.100.131 / auth-queue-sync.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-04
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：中
- **事实描述**：在 2026-06-20 10:42:00 UTC，资产 `ws-legal-09` 先解析了少见域名 `cdn-auth-queue.net`。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：这条观测本身更适合用来补足上下文，而不是单独承担最终结论。
- **主要依据来源**：关联事件扩查、显式反证检查
- **观测引用**：`obs-002, obs-003, obs-004, obs-005`

### 证据 E-05
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：中
- **事实描述**：在 2026-06-20 10:49:30 UTC，资产 `ws-legal-12` 出现了一条与 198.51.100.131 / legal-docs-cdn.example 相关的关键观测。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：这条观测本身更适合用来补足上下文，而不是单独承担最终结论。
- **主要依据来源**：关联事件扩查、显式反证检查
- **观测引用**：`obs-002, obs-003, obs-004, obs-005`

### 证据 C-01
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-20 10:46:10 UTC，资产 `ws-legal-09` 与 198.51.100.131 / identity-cache.vendor.example 的访问更接近计划内更新、维护或共享基础设施背景。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：关联事件扩查、显式反证检查
- **观测引用**：`obs-002, obs-003, obs-004, obs-005`

### 证据 C-02
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（上下文）
- **证据强度**：高
- **事实描述**：在 2026-06-20 10:52:00 UTC，资产 `相关资产` 与 198.51.100.131 / auth-queue-sync.net 的访问更接近计划内更新、维护或共享基础设施背景。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：关联事件扩查
- **观测引用**：`obs-002, obs-003`

## 4. 观测引用对照

| 观测编号 | 来源工具 | 事实摘要 | 关系 |
| -- | -- | -- | -- |
| `obs-001` | `search_seed_context` | 检索到 3 条偏可疑事件，围绕 auth-queue-sync.net、198.51.100.131 展开。 | `supporting` |
| `obs-002` | `search_related_events` | 扩线检索到 3 条可疑关联事件，围绕 auth-queue-sync.net、198.51.100.131 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-003` | `search_related_events` | 扩线检索到 3 条可疑关联事件，围绕 auth-queue-sync.net、198.51.100.131 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-004` | `expand_asset_scope` | 检索到 1 条更接近维护、更新或正常基线的事件。 | `counterevidence` |
| `obs-005` | `expand_asset_scope` | 检索到 1 条更接近维护、更新或正常基线的事件。 | `counterevidence` |

## 5. 正文章节引用映射

| 正文章节 | 主要 evidence / observation | 用途 |
| -- | -- | -- |
| 首页摘要 | E-01、E-02、check_counterevidence | 当前状态 / 当前最强证据 / 当前最关键缺口 |
| 第 5 节 | E-01、E-02、E-03、E-04、E-05、C-01、C-02 | 主支撑证据 / 反证与替代解释 |
| 第 9 节 | check_counterevidence、structure_evidence、ground_external_context | 证据链摘要 / 缺口说明 |
| 第 10 节 | E-01、E-02 | 结论 / 动作 / 复核重点 |

## 6. 证据来源与参考资料

| 来源类型 | 来源名称 | 用途 | 边界 |
| -- | -- | -- | -- |
| 上游检测 | t13d190900_7711aa22bb33_9911ccddee77 / Suspicious shared-infra beacon requiring review | 提供本案的调查起点与初始命中线索。 | 命中本身并不自动等于事件成立，仍需要后续本地事实补强。 |
| 内部观测 | trace_store.seed_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 内部观测 | trace_store.related_events | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 背景核查 | trace_store.asset_context | 验证维护窗口、补丁、备份或共享基线等替代解释。 | 反证核查只能说明是否存在降级解释，不能替代主证据链。 |

## 7. 证据缺口与待补查询

| 缺口 | 限制的结论 | 当前是否可补 | 最值得补的查询 |
| -- | -- | -- | -- |
| 是否存在主机侧执行、持久化或横向移动证据？ | 当前尚未观测到稳定的执行阶段证据，因此还不能确认主机侧执行、持久化或横向移动是否已经成立。 | 否 | 存在理论上相关的工具，但它们近期已经进入低收益状态。 |
| 仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。 | 当前仍缺少对“仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。”的直接补证。 | 否 | 存在理论上相关的工具，但它们近期已经进入低收益状态。 |
| 关联资产 ws-legal-09、ws-legal-12 是否真正受影响？ | 当前仍缺少对“关联资产 ws-legal-09、ws-legal-12 是否真正受影响？”的直接补证。 | 否 | 存在理论上相关的工具，但它们近期已经进入低收益状态。 |
| 补充 seed 家族、指纹或外部基础设施的公开技术背景，明确它只能作为背景还是能支撑基础设施解释。 | 当前不适合仅凭背景情报完成基础设施或家族级强归因。 | 是 | 补充 seed 家族、指纹或外部基础设施的公开技术背景，明确它只能作为背景还是能支撑基础设施解释。 |
| 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 | 当前不能完全排除维护窗口、共享基础设施或正常组件造成的替代解释。 | 是 | 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 |
| 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。 | 当前仍缺少对“仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。”的直接补证。 | 是 | 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。 |
| seed 自带的家族提示 Suspicious shared-infra beacon requiring review 是否有更多外部证据支撑？ | 当前仍缺少对“seed 自带的家族提示 Suspicious shared-infra beacon requiring review 是否有更多外部证据支撑？”的直接补证。 | 是 | seed 自带的家族提示 Suspicious shared-infra beacon requiring review 是否有更多外部证据支撑？ |
