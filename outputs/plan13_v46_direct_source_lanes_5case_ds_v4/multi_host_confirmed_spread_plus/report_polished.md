# 首页摘要

- **结论**：确认安全事件
- **严重度**：高危
- **研判把握**：高（94%）
- **已确认范围**：内网资产 `ws-eng-02` 在分析窗口内与核心外部基础设施 `sync-cdn-notify.net` / `203.0.113.77` 建立持续加密信标会话，并观察到主机侧可疑执行及向 `ws-eng-05` 的横向推进线索；内网资产 `ws-eng-05` 随后也联系相同信标基础设施。`ws-eng-02`、`ws-eng-05` 已纳入确认受影响范围。
- **一句话结论**：事件簇已从单点异常推进到具备多资产范围与持久化通信特征的高风险阶段，证据足以交付事件级结论，但攻击者完全控制、远控链路闭环等更强结论受未闭合缺口和背景因素限制。
- **立即动作**：立即隔离或重点监控 `ws-eng-02` 和 `ws-eng-05`；在边界与代理设备对 `203.0.113.77` 和 `sync-cdn-notify.net` 执行封禁并回溯通信记录。

## 1. 事件结论与当前判断

本次调查交付的结论为确认安全事件，研判把握度达到高（94%）。判断基础不再是孤立告警，而是覆盖 DNS 解析、加密信标重连、主机侧可疑执行、横向推进线索以及第二资产接力通信的完整事件簇。种子告警自带的家族/工具提示 `Suspected multi-host beacon spread` 与后续观察到的多资产、多阶段行为模式一致，进一步支撑了事件判断。虽然现有证据尚未覆盖攻击者完整控制闭环、持久化驻留或数据外泄等更强结论，但信标通信的持续性（尤其是告警后的重连）以及第二资产主动联系同一基础设施这两个特征，足以说明威胁活动并非偶发异常，而是有序推进的高风险事件。

本地情报库未命中指示物，表明本次事件所依赖的外部基础设施或通信模式较新或未收录于现有情报源；这可能意味攻击者在当前时间段使用了未公开的基础设施。技术候选来源分析返回了包括 Process Injection (T1055)、Command and Control (TA0011) 和 Exfiltration Over C2 Channel (T1041) 在内的多项候选技术方向，提供了后续证据提取与检测规则优化的参考范围。

## 2. 事件过程与关键时间线

- **2026-07-14 01:04:00 UTC** -- 内网资产 `ws-eng-02`（源 IP `10.70.3.12`）对稀有域名 `sync-cdn-notify.net` 发起 DNS 解析。这一解析事件发生在可疑 TLS 会话之前，构成 C2 通信的准备阶段，提示该资产在种下信标前已尝试获取 C2 服务器的地址。

- **2026-07-14 01:10:00 UTC** -- `ws-eng-02` 通过 TLS 协议与 `sync-cdn-notify.net` / `203.0.113.77` 建立会话，命中种子 JA4 指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc` 并触发告警。该告警直接确立了资产与稀有外部基础设施之间的加密信标联系，是整个事件簇的锚点。

- **2026-07-14 01:14:20 UTC** -- 告警仅 4 分钟后，`ws-eng-02` 再次与同一 C2 基础设施建立 TLS 连接。持续重连排除了单次异常网络抖动的可能，说明信标通信具备持续性，资产可能已处于指令等待或任务下发阶段。

- **2026-07-14 01:18:00 UTC** -- `ws-eng-02` 上触发可疑进程事件：`rundll32.exe` 从用户可写路径加载了一个 DLL 加载线索。由于事实中未提供完整命令行参数、落地 DLL 路径、父进程信息和持久化痕迹，该事件目前只能定性为“可疑执行线索”或“待核查执行链”，不能直接断定为已确认的恶意软件可疑加载线索，但它紧跟在信标序列之后，显著提升了主机侧受影响的怀疑。

- **2026-07-14 01:21:10 UTC** -- `ws-eng-02` 通过 PsExec 向 `ws-eng-05` 发起远程服务创建。这表明横向推进已经发生，但不能作为“已确认存在横向推进线索到 ws-eng-05 并被完全控制”：目前仅有远程服务创建告警，缺乏源自 `ws-eng-05` 的载荷落地、执行结果或独立的主机侧命中作为闭环。

- **2026-07-14 01:28:40 UTC** -- 资产 `ws-eng-05`（源 IP `10.70.3.45`）在与 `ws-eng-02` 发生横向推送事件仅 7 分钟后，也主动联系了相同的信标基础设施 `sync-cdn-notify.net` / `203.0.113.77`。这一接力通信消除了“PsExec 只是管理操作误报”的怀疑，是 `ws-eng-05` 已实际参与信标活动、需纳入确认受影响范围的关键依据。

## 3. 关键证据判断

种子告警并不仅仅给出了一个可疑 TLS 会话，它提供了 DNS 先导解析、告警后重连和后续主机上下文形成的连贯序列。第一个时间线事实（01:04:00 稀有域名解析）把整个事件从前置侦察阶段就锚定在了 `ws-eng-02` 上，它回答了“异常通信从何开始”这一问题。紧接着，01:10:00 的 JA4 指纹命中稀有外部基础设施，说明通信并非与常见商业 CDN 或云服务的偶发交互；该指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc` 作为识别特征，后续还被用于扩线检索，验证了该信标模式具备可重复的通信特征，而不仅仅是单一流量异常。

上述信标逻辑最关键的强化点是 01:14:20 的重连。在真实管理活动中，告警后的立即断连反而更符合操作员反应；而重现连接意味着主机侧存在自动化或持久化指令，这是区分“偶发命中”与“存续威胁”的标志性差异。01:18:00 的 `rundll32.exe` 加载器线索虽不能独立支撑可疑执行活动结论，但其时序安排在信标序列之后，使“主机存在非预期执行行为”的置信度显著上升。横向推送事件（01:21:10 的 PsExec）则进一步把事件从单资产问题拉升到了多资产范围的嫌疑，说明威胁操作者可能已在利用 `ws-eng-02` 作为跳板。

`ws-eng-05` 在 01:28:40 自行联系同一 C2 基础设施，是在没有其他随机因素干扰下对横向影响最直接的确认。它不是来源于同一个源 IP 的地址转换或日志拼接，而是从 `10.70.3.45` 这个独立 IP 发起的加密连接；同时，初始事件簇检索结果共发现 6 条偏可疑事件围绕 `sync-cdn-notify.net`、`203.0.113.77` 和 `10.70.3.45` 展开，说明关联性强且不是单点证据。由于本地情报库未命中指示物，本案无法借助既定威胁情报标签自动归因，需要以自建指标形式回溯和封禁。

## 4. 影响范围、候选对象与外部基础设施

已确认受影响范围目前覆盖内网资产 `ws-eng-02` 和 `ws-eng-05`，外部基础设施对象为 `sync-cdn-notify.net` 及其解析 IP `203.0.113.77`。`ws-eng-02` 是种子告警所在资产，提供了 DNS 解析、信标、主机可疑执行和横向推进源的四重角色；`ws-eng-05` 最初仅因横向推送告警被聚焦，但在自行联系同一信标基础设施后，已从“疑似受影响资产”升格为“已确认受影响资产”。两个资产当前的证据没有覆盖到完整的攻击者控制闭环、持久化机制或数据外泄行为，因此报告将两者的状态定位在“已纳入确认受影响范围、需要隔离与取证”，而并不宣称现有证据显示完全控制这些主机。

围绕当前事件簇，扩线检索得到了 9 条可疑关联事件，其中涉及时域相近的候选域名 `cdn-notify-edge.net` 和 IP `198.51.100.44`。2026-07-14 01:32:10 UTC，`ws-eng-05` 解析了 `cdn-notify-edge.net`；01:36:00 UTC 该资产又通过 TLS 联系了该域名对应的 IP `198.51.100.44`。这两个事件目前均被列为候选边界或背景线索：域名已完成 grounding、但未命中独立情报，因此暂不并入主证据链。任何将它们写成“蔓延至第二套基础设施”的结论都将超出当前证据，现阶段只能作为需要独立验证的扩线方向出现。

此外，`ws-eng-05` 在 01:39:20 UTC 还触发了从自身向 `10.70.3.90`（疑似 `ws-eng-09`）的 WMI 远程进程创建。该事件被识别为背景或替代解释事件，因为它缺乏可独立验证的指示物（例如目标进程名称、目标主机后续独立命中或外联行为），不能据此断言横向移动已从 `ws-eng-02` 向第三台资产蔓延。报告将其与 `10.70.3.90` 一并列为边界线索，要求通过后续复核确认其性质。在扩展查询中出现的其他域名，如 `attack.mitre.org`、`d-nb.info`、`hunt.io`、`www.vectra.ai` 等，均为检索过程中出现的候选域名，其自身状态并未完成 grounding，不应被视为本次事件的已确认指标，仅作为待确认候选。

## 5. 反证、替代解释与未闭合缺口

本次调查中，2026-07-14 01:24:00 UTC 和 01:26:00 UTC 在 `ws-eng-05` 上出现了两条备受关注的背景事件：先是该资产在同一小时内启动了经批准的 SCCM 补丁推送，随后从微软批准的终结点 `download.windowsupdate.com` / `13.107.246.45` 下载了 Windows 更新内容。这两个事件不能单独推翻主链结论--因为 `ws-eng-05` 联系 `sync-cdn-notify.net` 这一事实并不因存在补丁活动就自动变成良性，尤其是该信标行为是从同一个源 IP `10.70.3.45` 发起的，而非来自旁路或代理重定向。但它们确实限制了某些更强结论的成立空间，例如不能在所有主机活动上都默认攻击者驱动：SCCM 维护活动本身解释了同时间段内的部分管理行为，当未来分析 `ws-eng-05` 上的进程、服务或网络连接时，必须将维护窗口下的正常变动与潜在恶意行为区分开。

另一组候选事件（针对 `cdn-notify-edge.net` 和 `198.51.100.44`）同样不能直接并入主链，但它们无法被忽略。01:32:10 的域名解析和 01:36:00 的 TLS 连接目前被定性为背景或替代解释事件，因为 grounding 未命中独立情报，且此域名并未在种子告警中出现。这意味着存在一种可能，即 `ws-eng-05` 在到达原始信标主机后进一步启动了第二组外联，但存在当前无法确定是攻击者基础设施扩展、还是某种非恶意通信（例如投递链中的前置域名或应用层回调）等竞争解释。这构成两条互不矛盾的缺口：第一，仍需对这些候选事件补做独立验证以判定是否并入事件范围；第二，仍需以 `sync-cdn-notify.net`、`203.0.113.77`、JA4 指纹等核心 关键关联指标 为目标执行显式扩线，确认是否存在同指标在更大范围（更多资产、更长时间窗）的复现。

对于 `ws-eng-02` 上的 `rundll32.exe` 执行，虽然已确认时间线上的路径可疑，但缺少完整命令行、加载 DLL 哈希、父进程链和磁盘落地路径，这一缺口当前约束我们只能写到“可疑执行线索”而不是“已确认可疑加载器成功执行”。同样，PsExec 横向推送虽然提供了远程服务创建的证据，但目标主机后续是否绑定了新的 C2 通信或其它独立恶意行为，未形成闭环，这意味着横向移动“已确认”的只是动作，而不是攻击者在 `ws-eng-05` 上建立存续这一结论。WMI 远程进程创建事件同样因缺少可独立验证的指示物而未被合并，构成对“攻击者是否已向更多资产渗透”的上限约束。

## 6. 处置建议与后续核查

- **隔离与监控已确认受影响资产**：立即对 `ws-eng-02` 与 `ws-eng-05` 实施网络层隔离或纳入重点监控组。在 EDR/进程日志中检索以下字段：`rundll32.exe` 的完整命令行参数、加载 DLL 路径、父进程名称和进程链；重点关注用户可写路径（如 `%TEMP%`、`%APPDATA%`、`Download` 等）下的 DLL、脚本或可执行文件落地，并以文件哈希在本地文件系统和全网范围内做回溯搜索，目标是明确是否存在可疑加载线索、持久化项或额外载荷落地。

- **对外部基础设施执行封禁与回溯**：在边界防火墙、正向/反向代理和 DNS 解析设备上，针对核心外部基础设施 `203.0.113.77` 和 `sync-cdn-notify.net` 实施双向封禁；同时回溯当前调查窗口内所有源 IP 向这些指标发起的 DNS 查询、TCP/TLS 连接流日志。如回溯到其他内网资产存在同样通信，应将其列为扩线候选资产并执行隔离评估。对候选域名 `cdn-notify-edge.net` 和 IP `198.51.100.44` 采取监控模式，在 DNS 日志和代理日志中检索解析与连接记录，捕获是否还有其他内部主机触达。若发现独立命中并及时补充 grounding 验证，则更新封禁清单。

- **验证横向推送的闭环**：在 `ws-eng-05` 和 `ws-eng-02` 的主机日志中，检索 PsExec 创建的远程服务名称、对应的服务二进制路径和事件 ID（如 Windows 安全日志 4697、系统日志 7045）；核查该服务在目标主机上是否实际上执行、以及是否产生了子进程或后续网络连接。对候选的 WMI 远程进程创建事件（源 IP `10.70.3.45`、目标 `10.70.3.90`），在目标主机 `10.70.3.90` 上检索该时段前后的进程创建日志（EventID 4688、592 或 EDR 记录），验证是否确实出现由 WMI 拉起的可疑进程，并检查该进程是否产生后续横向、持久化或外联行为。

- **扩线与独立验证**：以种子指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc`、域名 `sync-cdn-notify.net`、IP `203.0.113.77` 为 关键关联指标，在分析窗口前后扩展检索所有资产的 TLS 及 DNS 记录，验证是否存在更多主机命中同一指纹或联系同一基础设施。对已发现的 9 条扩线关联事件逐条补做独立验证，重点确认它们是否命中本地指纹、IOC 或外部威胁情报；若验证通过，则应扩充受影响范围并相应调整隔离与封禁措施。

- **区分维护窗口活动与恶意行为**：在 `ws-eng-05` 上，将 SCCM 补丁推送时段与所有可疑事件交叉比对，将已知的补丁相关进程、网络连接（如 `download.windowsupdate.com` / `13.107.246.45`）列入允许基线，避免后续调查将正常管理活动误认为横向或外联行为；同时，任何同在维护窗口内的异常外联、异常进程加载仍需独立排查，不应因存在维护窗口而自动排除。

---

# 事件调查技术附录

以下内容主要供分析、复盘和技术排查使用，不直接替代主报告结论。

## 1. IOC / IOA 清单

| 类型 | 值 | 角色 | 状态 |
| -- | -- | -- | -- |
| IP | 203.0.113.77 | 核心外部基础设施 | 已确认 |
| 域名 | sync-cdn-notify.net | 核心外部基础设施 | 已确认 |
| IP | 13.107.246.45 | 背景/上下文指标 | 已确认 |
| 域名 | download.windowsupdate.com | 背景/上下文指标 | 已确认 |
| 指纹 | t13d1516h2_2ba4190accc0_13fe129f5abc | 扩展查询候选 | 待确认 |
| 内网 IP | 10.70.3.12 | 关联内部地址 | 已确认 |
| 内网 IP | 10.70.3.45 | 关联内部地址 | 已确认 |
| 内网 IP | 10.70.3.90 | 关联内部地址 | 仅上下文 |
| IP | 198.51.100.44 | 扩展查询候选 | 已验证待确认 |
| 域名 | attack.mitre.org | 扩展查询候选 | 待确认 |
| 域名 | cdn-notify-edge.net | 扩展查询候选 | 已验证待确认 |
| 域名 | d-nb.info | 扩展查询候选 | 待确认 |
| 域名 | deepstrike.io | 扩展查询候选 | 待确认 |
| 域名 | hunt.io | 扩展查询候选 | 待确认 |
| 域名 | www.aha.org | 扩展查询候选 | 待确认 |
| 域名 | www.hhs.gov | 扩展查询候选 | 待确认 |
| 域名 | www.ironnet.com | 扩展查询候选 | 待确认 |
| 域名 | www.quorumcyber.com | 扩展查询候选 | 待确认 |
| 域名 | www.safeaeon.com | 扩展查询候选 | 待确认 |
| 域名 | www.sciencedirect.com | 扩展查询候选 | 待确认 |
| 域名 | www.vectra.ai | 扩展查询候选 | 待确认 |

## 2. 关键对象清单

| 对象 | 类型 | 当前角色 | 是否已纳入证据链 | 备注 |
| -- | -- | -- | -- | -- |
| ws-eng-02 | 资产 | 种子资产 | 是 | 种子告警首先落在该资产上。；当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| ws-eng-05 | 资产 | 已确认受影响资产 | 是 | 当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。；事件聚类中仍需独立验证的候选资产。；候选事件缺少可独立验证指示物，当前只作为上下文边界保留。；已完成 grounding 但暂未获得独立支撑，当前作为边界线索保留。 |
| 203.0.113.77 | IP | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选地址。 |
| sync-cdn-notify.net | 域名 | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选域名。 |
| 13.107.246.45 | IP | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选地址。 |
| download.windowsupdate.com | 域名 | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| t13d1516h2_2ba4190accc0_13fe129f5abc | JA4 | 种子命中指纹 | 是 | 种子告警命中的关键通信指纹。 |
| Suspected multi-host beacon spread | 家族提示 | 家族/工具提示 | 是 | 辅助解释本案的家族或工具背景，不直接等于强归因。 |
| 10.70.3.12 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.70.3.45 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。；当前 pivot 里出现的扩线候选地址。 |
| 10.70.3.90 | 内网 IP | 关联内部地址 | 否 | 当前 pivot 里出现的扩线候选地址。；候选事件缺少可独立验证指示物，当前只作为上下文边界保留。 |
| 198.51.100.44 | IP | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选地址。；已完成 grounding 但暂未获得独立支撑，当前作为边界线索保留。 |
| attack.mitre.org | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| cdn-notify-edge.net | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。；候选事件涉及的域名，尚未进入已确认主链。；已完成 grounding 但暂未获得独立支撑，当前作为边界线索保留。 |
| d-nb.info | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| deepstrike.io | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| hunt.io | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.aha.org | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.hhs.gov | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.ironnet.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.quorumcyber.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.safeaeon.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.sciencedirect.com | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| www.vectra.ai | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| t13d1516h2_2ba4190accc0_13fe129f5abc | 指纹 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选指纹。 |

## 3. 关键证据细目

### 证据 E-01
- **证据类型**：主支撑证据
- **证据类别**：调查触发证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:10:00 UTC，资产 `ws-eng-02` 围绕 203.0.113.77 / sync-cdn-notify.net 的异常通信触发了种子告警。
- **为什么重要**：这是种子命中的直接落点，说明调查起点并不是孤立的指纹命中。
- **边界与限制**：单条种子命中本身不能独立证明事件成立，仍需要结合重复通信、资产背景或主机侧线索共同定性。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-004`

### 证据 E-02
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:28:40 UTC，资产 `ws-eng-05` 再次与 203.0.113.77 / sync-cdn-notify.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-004`

### 证据 E-03
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:04:00 UTC，资产 `ws-eng-02` 先解析了少见域名 `sync-cdn-notify.net`，为后续与同一基础设施的通信提供了准备。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-004`

### 证据 E-04
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（主机）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:18:00 UTC，资产 `ws-eng-02` 在异常通信后出现 `rundll32.exe` 可疑启动，伴随 DLL 加载，路径位于用户可写目录。
- **为什么重要**：它把异常从通信层推进到主机执行或其他更高风险阶段，对事件定性的权重明显更高。
- **边界与限制**：当前仍缺少更细的命令行、落地文件、持久化或内存证据，因此可以确认存在执行线索，但还不能完整描述具体载荷。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-004`

### 证据 E-05
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:21:10 UTC，资产 `ws-eng-02` 出现指向 `10.70.3.45` 的远程服务创建，说明异常已经开始跨主机推进。
- **为什么重要**：它表明异常已经从外联推进到跨主机动作，比单纯重复通信更能支撑事件升级。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-004`

### 证据 E-06
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（主机）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:39:20 UTC，资产 `ws-eng-05` 出现指向 `10.70.3.90` 的远程进程创建。
- **为什么重要**：这说明异常已经从种子资产延伸到其他主机，事件范围不再局限于单点。
- **边界与限制**：当前仍缺少更细的命令行、落地文件、持久化或内存证据，因此可以确认存在执行线索，但还不能完整描述具体载荷。
- **主要依据来源**：关联事件扩查、候选对象情报验证
- **观测引用**：`obs-004, obs-005`

### 证据 E-07
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:36:00 UTC，资产 `ws-eng-05` 再次与 198.51.100.44 / cdn-notify-edge.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：关联事件扩查、候选对象情报验证
- **观测引用**：`obs-004, obs-006`

### 证据 C-01
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（资产背景）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:24:00 UTC，资产 `ws-eng-05` 同时间窗存在已批准的维护、变更或备份活动，可解释部分日常管理行为。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：关联事件扩查
- **观测引用**：`obs-004`

### 证据 C-02
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:26:00 UTC，资产 `ws-eng-05` 与已批准的供应商或更新端点通信，更接近计划内流量。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：关联事件扩查
- **观测引用**：`obs-004`

## 4. 观测引用对照

| 观测编号 | 来源工具 | 事实摘要 | 关系 |
| -- | -- | -- | -- |
| `obs-001` | `search_seed_context` | 检索到 6 条偏可疑事件，围绕 sync-cdn-notify.net、203.0.113.77、10.70.3.45 展开。 | `supporting` |
| `obs-002` | `local_intel_lookup` | 本地情报库未命中该指示物。 | `context` |
| `obs-003` | `technical_source_search` | technical_source_search 返回了 15 条候选技术来源，可用于后续正文证据提取。 | `context` |
| `obs-004` | `search_related_events` | 扩线检索到 9 条可疑关联事件，围绕 sync-cdn-notify.net、cdn-notify-edge.net、203.0.113.77 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-005` | `ground_candidate_event` | 候选事件 evt-811 缺少可独立验证的指示物，当前不并入主证据链，仅保留为边界线索。 | `context` |
| `obs-006` | `ground_candidate_event` | 候选事件 evt-810 已完成 local_intel grounding，但暂未命中独立情报，当前不直接并入主证据链。 | `context` |

## 5. 正文章节引用映射

| 正文章节 | 主要 evidence / observation | 用途 |
| -- | -- | -- |
| 首页摘要 | E-01、E-02、expand_cluster_scope | 当前状态 / 当前最强证据 / 当前最关键缺口 |
| 第 5 节 | E-01、E-02、E-03、E-04、E-05、E-06、E-07、C-01、C-02 | 主支撑证据 / 反证与替代解释 |
| 第 9 节 | expand_cluster_scope、check_counterevidence、validate_candidate_events | 证据链摘要 / 缺口说明 |
| 第 10 节 | E-01、E-02 | 结论 / 动作 / 复核重点 |

## 6. 证据来源与参考资料

| 来源类型 | 来源名称 | 用途 | 边界 |
| -- | -- | -- | -- |
| 上游检测 | t13d1516h2_2ba4190accc0_13fe129f5abc / Suspected multi-host beacon spread | 提供本案的调查起点与初始命中线索。 | 命中本身并不自动等于事件成立，仍需要后续本地事实补强。 |
| 内部观测 | trace_store.seed_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 外部结构化情报 | intel_tool | 补充基础设施、家族或公开背景解释。 | 外部情报不能单独替代本地事实，也不能直接把本案写成强归因。 |
| 内部观测 | trace_store.related_events | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 外部结构化情报 | candidate_event:evt-811 | 补充基础设施、家族或公开背景解释。 | 外部情报不能单独替代本地事实，也不能直接把本案写成强归因。 |
| 外部结构化情报 | candidate_event:evt-810 | 补充基础设施、家族或公开背景解释。 | 外部情报不能单独替代本地事实，也不能直接把本案写成强归因。 |

## 7. 证据缺口与待补查询

| 缺口 | 限制的结论 | 当前是否可补 | 最值得补的查询 |
| -- | -- | -- | -- |
| 仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。 | 当前仍缺少对“仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。”的直接补证。 | 是 | 仍需围绕当前关键关联指标开展一轮扩线核查，确认是否存在同指标的更大范围复现。 |
| 扩线得到的 1 条关联事件尚未完成独立验证，是否应并入主事件范围？ | 当前仍缺少对“扩线得到的 1 条关联事件尚未完成独立验证，是否应并入主事件范围？”的直接补证。 | 否 | 该缺口已多轮核查但仍未获得新的有效信息，更适合作为报告未决事项。 |
| 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 | 当前不能完全排除维护窗口、共享基础设施或正常组件造成的替代解释。 | 是 | 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 |
