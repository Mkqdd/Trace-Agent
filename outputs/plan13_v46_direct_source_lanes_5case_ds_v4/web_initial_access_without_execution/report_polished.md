# 首页摘要

- **结论：** 本次分析确认 **web-app-02** 发生一起可疑的安全事件，存在对外信标（Beacon）行为，评估为待人工复核的可疑事件簇。
- **严重度：** **中危**。
- **研判把握：** 较高（事件时间线闭合、核心信标行为明确，但缺乏主机侧执行闭环和更大范围横向确认）。
- **已确认范围：** 资产 **web-app-02**（10.61.2.20）已确认发生可疑外联，外部基础设施 **auth-edge-panel.net** 与 **198.51.100.177** 已确认被用于通信。
- **一句话结论：** **web-app-02** 在遭受针对 Admin Portal 的利用请求后，向外联基础设施建立可疑 TLS 会话，匹配 Web Shell 类信标特征；事件成立，但攻击执行链、横向移动及关联资产 **web-app-05** 的影响仍需进一步取证核查。
- **立即动作：** 隔离 **web-app-02** 并进行主机取证；全网封禁 **auth-edge-panel.net** 及 **198.51.100.177**；对关联候选资产 **web-app-05** 启动核查。

## 1. 事件结论与当前判断

本次调查围绕一台名为 `web-app-02`（源地址 10.61.2.20）的面向公众的 Web 服务器展开。该服务器在受到针对 Admin Portal 的利用请求攻击后，主动解析并向外联域名 `auth-edge-panel.net`（解析到 198.51.100.177）建立了匹配 `Possible web beacon without host corroboration` 特征的 TLS 会话。综合考虑攻击前序、信标特征以及信标后的重连行为，我们判断该事件为一起“可疑事件，建议继续复核”的安全事件。

当前判断的核心在于，攻击链的初期阶段--外部利用请求、DNS 解析以及可疑信标通信--已在时间线上形成稳定闭环，足以支撑事件成立。然而，由于主机侧脚本日志未开启、未发现恶意子进程以及对横向移动和关联资产的验证尚未完成，我们目前无法对攻击者在主机内部的控制深度和横向移动范围做出更强结论。因此，现行结论严格收敛在“可疑外联信标”层面，不建议提升为已确认的全面失陷。

## 2. 事件过程与关键时间线

以下时间线基于已确认事实，展示了事件从初始利用到信标重连的完整发展过程。

- **2026-06-24 14:50:00 UTC**  
  `web-app-02` 首次收到针对 Admin Portal 的利用请求，这标志着攻击者开始探测或尝试利用面向公众的服务。这是事件链的起点，表明外部威胁行为者已将 `web-app-02` 视为目标。

- **2026-06-24 14:54:00 UTC**  
  `web-app-02` 再次收到针对 Admin Portal 的利用请求，且使用了与第一波不同的源 ASN 和攻击载荷家族。这说明攻击具有持续性，且可能存在多个攻击者或同一攻击者使用多样化武器库，加大了事件对抗的复杂性。

- **2026-06-24 14:58:30 UTC**  
  `web-app-02` 在遭受利用请求后不久，主动执行 DNS 解析，请求了 `auth-edge-panel.net`。这一行为是事件转折点，它将外部攻击与随后的可疑外联联系起来，暗示利用请求可能成功触发了某种机制，导致服务器需要联系 C2 基础设施。

- **2026-06-24 15:02:00 UTC**  
  种子告警（Seed Alert）触发。`web-app-02` 与 `auth-edge-panel.net` 解析到的 IP `198.51.100.177` 之间建立了 TLS 连接，其 JA4 指纹 `t13d1517h2_22adbe210bc1_11cae1281e22` 命中了“疑似 Web Shell 信标”规则。该时刻是事件的核心支撑点，将先前所有可疑活动聚合并确认为一个具体的、可疑特征明确的安全事件。

- **2026-06-24 15:06:40 UTC**  
  在种子告警发生后，`web-app-02` 再次连接到完全相同的外部基础设施 `auth-edge-panel.net` / `198.51.100.177`。这次重连确认了该外联非一次性偶发行为，而是具有持续性的信标通信或控制指令回传，显著强化了事件的恶意性质。

## 3. 关键证据判断

证据判断的核心在于如何从碎片化的告警和日志中，构建起一条从攻击到信标的逻辑链，并评估其权重。本次事件中，支持“可疑信标行为”这一结论的证据链是坚实且连续的。在 `14:50:00 UTC` 至 `14:54:00 UTC` 期间，`web-app-02` 连续遭受两波特征迥异的针对 Admin Portal 的利用请求，这构成了明确的初始攻击行为。其后，在 `14:58:30 UTC`，该服务器便立即解析了此前未出现过的域名 `auth-edge-panel.net`，这种时间上的紧密相继性，强烈暗示 DNS 解析行为是被外部攻击所触发的结果，而非正常的业务或系统请求。

随后在 `15:02:00 UTC` 种子告警的触发，将这一系列行为的威胁等级从“可疑”升级为“可疑特征明确”。该告警不仅识别出 TLS 通信的 JA4 指纹符合“疑似 Web Shell 信标”模式，还明确指出了关联的资产和基础设施，成为整条证据链的中心节点。最后，在 `15:06:40 UTC` 发生的对同一基础设施的重连，彻底否定了告警偶发或误报的可能性，证明了攻击链的延续性和通信的稳定性。这使得我们能够以较高把握断定，`web-app-02` 确实已与外部 C2 基础设施建立了可疑信道。

然而，证据链在确认主机内部活动和横向影响时显得乏力。在 `15:15:30 UTC` 窗口内的主机遥测显示，Web 工作进程没有产生可疑的子进程活动，`15:34:00 UTC` 更新后的 WAF 规则也成功拦截了后续针对 Admin Portal 的攻击载荷。这两点虽然证明了安全控制措施部分生效，但并未解除风险--它们无法证明在告警前攻击载荷是否已成功执行并使主机上线。相比之下，更具限制性的证据是 `15:50:00 UTC` 的事实：`web-app-02` 并未启用脚本块日志记录。这个遥测缺口直接导致了我们无法获取攻击成功后的主机侧命令行、脚本执行细节，成为当前判断无法深入执行链、持久化等阶段的最大瓶颈。因此，当前证据足以判定事件为“可疑事件簇”，但不足以支撑“已确认权限维持”或“已确认存在横向推进线索”的更强结论。

## 4. 影响范围、候选对象与外部基础设施

当前已确认受到影响的资产仅为 `web-app-02`（10.61.2.20）。该资产在整个事件中扮演了受攻击目标和信标源的双重角色。所有已确认可疑的网络行为，包括与 C2 基础设施的 TLS 通信，均源于此节点。因此，`web-app-02` 是本次事件应急处置和深度取证的唯一确认重点。围绕 `web-app-02` 的所有观测事实构成了事件的“已确认范围”。与之直接交互的外部基础设施 `auth-edge-panel.net` 和 `198.51.100.177` 被定位为攻击者的控制端或中继设施，是应立即执行网络侧封禁和回溯搜索的关键对象。

在确认范围之外，存在一些需要进一步验证的候选对象，它们不能简单地并入已确认影响范围。资产 `web-app-05`（10.61.2.55）是风险最高的待确认目标。在 `15:30:00 UTC`，`web-app-05` 也解析了与 C2 设施位于同一基础设施上的兄弟域名 `cdn-auth-edge.net`，随后在 `15:32:20 UTC` 发起了短暂的单次 TLS 连接。这使其成为一个严重的候选问题：该行为可能是共享基础架构背景下的无害扫描仪流量，也可能意味着已观察到通过某种方式（如凭证复用、横向移动）将蔓延到了其他服务器。同样，资产 `web-app-04`（10.61.2.44）虽然也访问了同一 IP `198.51.100.177`，但其域名 `scanner-cdn.example` 和上下文明确指出这是一个扫描仪遥测供应商的流量，更倾向于一个良性背景解释，但这仍需通过日志核查来最终确认，以排除被恶意域名伪装的可能。对域 `cdn-auth-edge.net` 和 JA4 指纹 `t13d1517h2_22adbe210bc1_11cae1281e22` 作为扩线候选，应在网络流量日志中进行大范围、长周期的搜索，以确定是否存在更广泛的、尚未被发现的受影响资产。

## 5. 反证、替代解释与未闭合缺口

事件的复杂性在于它发生在一个存在合法运维活动、动态攻击载荷和基础设施共享的环境中，这些背景信息提供了必须审慎对待的反证或替代解释，限制了做出更强结论的能力。一个重要的干扰项是，`web-app-02` 在事件发生的同一时段（与信标行为重叠）内，正处在一次计划内的 Admin Portal 补丁部署周期中。这个维护窗口引发了一种替代可能：即部分网络行为或系统状态变化可能与补丁部署有关。尽管如此，维护活动通常不需要解析陌生的、在邮件告警中标记为可疑的信标域名，因此这种解释虽然存在，却不能完全解释或否定核心的信标通信事件。

同时，存在一批由其他网络节点 `web-app-04` 对同一 IP `198.51.100.177` 的访问，其流量被标记为发送到 `scanner-cdn.example` 的扫描仪遥测。这强烈暗示 `198.51.100.177` 是一个共享的托管基础设施，而非仅为本次攻击独家使用的 IP。这种共享背景削弱了单纯基于 IP 回溯的归因强度，但并未否定 `auth-edge-panel.net` 这一特定域名及其关联信标特征的恶意性。它告诉我们，处置时需严格区分不同访问目的，不能将所有访问该 IP 的资产一刀切。

目前，事件分析面临着几个关键的未闭合缺口，这些缺口直接决定了事件研判的上限。首要缺口是 **主机侧执行证据缺失**。由于 `web-app-02` 未启用脚本块日志记录，我们无法回答“攻击载荷是否在主机上成功执行”这一核心问题。主机遥测报告未发现可疑子进程，这虽然是正面信息，但不能作为没有执行行为的证据。第二个显著缺口是 **`web-app-05` 的受影响状态不明**。该资产的行为是一个亟待验证的红色信号，它的验证结果可能会大幅扩展当前的事件范围，也可能使其被归类为无害的背景流量。此外，围绕 Seed Alert 带来的家族提示“Possible web beacon without host corroboration”和关键 JA4 指纹，缺乏公开的情报溯源支持，这使得我们无法从外部视角加固或反驳当前判断。因此，所有结论都必须建立在“内部证据链自洽，但缺乏外部报告支持和内部主机闭环”这一基础上。

## 6. 处置建议与后续核查

以下行动建议旨在闭合关键缺口、验证候选威胁并控制已知风险。所有动作需有序执行，优先处理已确认影响的资产和关键基础设施。

- **隔离并深度取证 `web-app-02`**  
  - **对象**：资产 `web-app-02` (10.61.2.20)。  
  - **动作**：立即将其从网络隔离，但保持开机以保留内存证据。  
  - **核查目标**：部署或启用增强型的 EDR 日志采集与 Sysmon，重点核查围绕告警时间 *2026-06-24 14:50:00 UTC 至 15:50 UTC* 的所有进程创建、网络连接、服务创建和计划任务。重点查找关联到 `auth-edge-panel.net` 或 `198.51.100.177` 的子进程，以及任何可疑的脚本引擎（如 `wscript`, `powershell`, `rundll32.exe`）调用。目标是闭合 `gap:execution_gap`，判断是否存在执行、持久化或横向移动。

- **全网范围内封禁并回溯 IOC**  
  - **对象**：域名 `auth-edge-panel.net`，IP `198.51.100.177`，以及兄弟域名 `cdn-auth-edge.net`。  
  - **动作**：在边界防火墙、Web 代理和 DNS 服务上创建静态阻断规则。  
  - **核查目标**：在 DNS 日志、代理日志和 EDR 网络事件中，以 `198.51.100.177`、`auth-edge-panel.net`、`cdn-auth-edge.net` 以及 JA4 指纹 `t13d1517h2_22adbe210bc1_11cae1281e22` 为关键字，回溯过去 30 天的历史记录，以发现是否还有其他资产存在类似外联行为，回应 `gap:expand_cluster_scope`。

- **启动对候选资产 `web-app-05` 和 `web-app-04` 的专项核查**  
  - **对象**：资产 `web-app-05` (10.61.2.55) 和 `web-app-04` (10.61.2.44)。  
  - **动作**：不要直接升级为受影响资产。  
  - **核查目标**：对于 `web-app-05`，需获取其在 `2026-06-24 15:30:00 UTC` 前后的主机日志，确认解析 `cdn-auth-edge.net` 和发起 TLS 连接的进程及其父进程，并与 `web-app-02` 的事件时间进行精准比对。对于 `web-app-04`，验证其访问 `198.51.100.177` 的流量是否确实指向扫描仪供应商 `scanner-cdn.example`，并确认相关进程的合法性。目标是解决 `gap:related_assets_review`。

- **弥补 `web-app-02` 的遥测缺陷并审查维护窗口交互**  
  - **对象**：资产 `web-app-02`。  
  - **动作**：立即开启 PowerShell 脚本块日志记录、命令行审计和关键目录的文件完整性监控。  
  - **核查目标**：调取计划补丁部署的详细日志，与安全事件时间线进行精确比对。确认补丁操作的具体进程和网络连接，明确分离安全事件和合规运维活动，回应 `gap:check_counterevidence`。

- **深化威胁情报关联与外部背景补充**  
  - **对象**：JA4 指纹 `t13d1517h2_22adbe210bc1_11cae1281e22`，域名 `auth-edge-panel.net`。  
  - **动作**：向威胁情报平台查询上述指标。  
  - **核查目标**：确认是否存在公开报告将该指纹或域名与特定 APT 组织或特定 Web Shell 家族关联，从而为内部事件提供外部佐证，以回应 `gap:ground_external_context` 和 `gap:validate_family_hint`。

---

# 事件调查技术附录

以下内容主要供分析、复盘和技术排查使用，不直接替代主报告结论。

## 1. IOC / IOA 清单

| 类型 | 值 | 角色 | 状态 |
| -- | -- | -- | -- |
| IP | 198.51.100.177 | 核心外部基础设施 | 已确认 |
| 域名 | auth-edge-panel.net | 核心外部基础设施 | 已确认 |
| 域名 | scanner-cdn.example | 背景/上下文指标 | 已确认 |
| 指纹 | t13d1517h2_22adbe210bc1_11cae1281e22 | 扩展查询候选 | 待确认 |
| 内网 IP | 10.61.2.20 | 关联内部地址 | 已确认 |
| 内网 IP | 10.61.2.44 | 关联内部地址 | 已确认 |
| 内网 IP | 10.61.2.55 | 关联内部地址 | 已确认 |
| 域名 | cdn-auth-edge.net | 扩展查询候选 | 待确认 |

## 2. 关键对象清单

| 对象 | 类型 | 当前角色 | 是否已纳入证据链 | 备注 |
| -- | -- | -- | -- | -- |
| web-app-02 | 资产 | 种子资产 | 是 | 种子告警首先落在该资产上。；当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| web-app-05 | 资产 | 待确认关联资产 | 否 | 当前仍需独立验证是否真正受影响。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| 198.51.100.177 | IP | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选地址。 |
| auth-edge-panel.net | 域名 | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选域名。 |
| scanner-cdn.example | 域名 | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| t13d1517h2_22adbe210bc1_11cae1281e22 | JA4 | 种子命中指纹 | 是 | 种子告警命中的关键通信指纹。 |
| Possible web beacon without host corroboration | 家族提示 | 家族/工具提示 | 是 | 辅助解释本案的家族或工具背景，不直接等于强归因。 |
| 10.61.2.20 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.61.2.44 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.61.2.55 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| web-app-04 | 资产 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选资产。 |
| cdn-auth-edge.net | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。 |
| t13d1517h2_22adbe210bc1_11cae1281e22 | 指纹 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选指纹。 |

## 3. 关键证据细目

### 证据 E-01
- **证据类型**：主支撑证据
- **证据类别**：调查触发证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-24 15:02:00 UTC，资产 `web-app-02` 围绕 198.51.100.177 / auth-edge-panel.net 的异常通信触发了种子告警。
- **为什么重要**：这是种子命中的直接落点，说明调查起点并不是孤立的指纹命中。
- **边界与限制**：单条种子命中本身不能独立证明事件成立，仍需要结合重复通信、资产背景或主机侧线索共同定性。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-02
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-24 14:58:30 UTC，资产 `web-app-02` 对外服务入口出现可疑利用请求。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-03
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-24 15:06:40 UTC，资产 `web-app-02` 再次与 198.51.100.177 / auth-edge-panel.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-04
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-24 14:54:00 UTC，资产 `web-app-02` 对外服务入口出现可疑利用请求。
- **为什么重要**：它补足了外联之前的高风险上下文，使后续可疑通信不再显得孤立。
- **边界与限制**：它解释了事件为何进入高风险状态，但单独仍不能证明漏洞利用已经成功执行。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-05
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-06-24 14:50:00 UTC，资产 `web-app-02` 对外服务入口出现可疑利用请求。
- **为什么重要**：它补足了外联之前的高风险上下文，使后续可疑通信不再显得孤立。
- **边界与限制**：它解释了事件为何进入高风险状态，但单独仍不能证明漏洞利用已经成功执行。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 E-06
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：中
- **事实描述**：在 2026-06-24 15:30:00 UTC，资产 `web-app-05` 先解析了少见域名 `cdn-auth-edge.net`。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：这条观测本身更适合用来补足上下文，而不是单独承担最终结论。
- **主要依据来源**：关联事件扩查、资产背景核查
- **观测引用**：`obs-002, obs-003, obs-004, obs-005`

### 证据 E-07
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：中
- **事实描述**：在 2026-06-24 15:32:20 UTC，资产 `web-app-05` 出现了一条与 198.51.100.177 / cdn-auth-edge.net 相关的关键观测。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：这条观测本身更适合用来补足上下文，而不是单独承担最终结论。
- **主要依据来源**：关联事件扩查、资产背景核查
- **观测引用**：`obs-002, obs-003, obs-004, obs-005`

### 证据 C-01
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（资产背景）
- **证据强度**：高
- **事实描述**：在 2026-06-24 15:15:30 UTC，资产 `web-app-02` 同时间窗存在已批准的维护、变更或备份活动，可解释部分日常管理行为。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

### 证据 C-02
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（资产背景）
- **证据强度**：高
- **事实描述**：在 2026-06-24 15:18:00 UTC，资产 `web-app-02` 同时间窗存在已批准的维护、变更或备份活动，可解释部分日常管理行为。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：种子事件窗观测、关联事件扩查
- **观测引用**：`obs-001, obs-002, obs-003`

## 4. 观测引用对照

| 观测编号 | 来源工具 | 事实摘要 | 关系 |
| -- | -- | -- | -- |
| `obs-001` | `search_seed_context` | 检索到 5 条偏可疑事件，围绕 auth-edge-panel.net、198.51.100.177 展开。 | `supporting` |
| `obs-002` | `search_related_events` | 扩线检索到 5 条可疑关联事件，围绕 auth-edge-panel.net、198.51.100.177 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-003` | `search_related_events` | 扩线检索到 5 条可疑关联事件，围绕 auth-edge-panel.net、198.51.100.177 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-004` | `expand_asset_scope` | 检索到 2 条上下文事件，当前主要用于扩边界和补足范围。 | `context` |
| `obs-005` | `expand_asset_scope` | 检索到 2 条上下文事件，当前主要用于扩边界和补足范围。 | `context` |

## 5. 正文章节引用映射

| 正文章节 | 主要 evidence / observation | 用途 |
| -- | -- | -- |
| 首页摘要 | E-01、E-02、check_counterevidence | 当前状态 / 当前最强证据 / 当前最关键缺口 |
| 第 5 节 | E-01、E-02、E-03、E-04、E-05、E-06、E-07、C-01、C-02 | 主支撑证据 / 反证与替代解释 |
| 第 9 节 | check_counterevidence、structure_evidence、ground_external_context | 证据链摘要 / 缺口说明 |
| 第 10 节 | E-01、E-02 | 结论 / 动作 / 复核重点 |

## 6. 证据来源与参考资料

| 来源类型 | 来源名称 | 用途 | 边界 |
| -- | -- | -- | -- |
| 上游检测 | t13d1517h2_22adbe210bc1_11cae1281e22 / Possible web beacon without host corroboration | 提供本案的调查起点与初始命中线索。 | 命中本身并不自动等于事件成立，仍需要后续本地事实补强。 |
| 内部观测 | trace_store.seed_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 内部观测 | trace_store.related_events | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 内部观测 | trace_store.asset_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |

## 7. 证据缺口与待补查询

| 缺口 | 限制的结论 | 当前是否可补 | 最值得补的查询 |
| -- | -- | -- | -- |
| 是否存在主机侧执行、持久化或横向移动证据？ | 当前尚未观测到稳定的执行阶段证据，因此还不能确认主机侧执行、持久化或横向移动是否已经成立。 | 否 | 存在理论上相关的工具，但它们近期已经进入低收益状态。 |
| 仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。 | 当前仍缺少对“仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。”的直接补证。 | 否 | 存在理论上相关的工具，但它们近期已经进入低收益状态。 |
| 关联资产 web-app-05 是否真正受影响？ | 当前仍缺少对“关联资产 web-app-05 是否真正受影响？”的直接补证。 | 否 | 该缺口已多轮核查但仍未获得新的有效信息，更适合作为报告未决事项。 |
| 补充 seed 家族、指纹或外部基础设施的公开技术背景，明确它只能作为背景还是能支撑基础设施解释。 | 当前不适合仅凭背景情报完成基础设施或家族级强归因。 | 是 | 补充 seed 家族、指纹或外部基础设施的公开技术背景，明确它只能作为背景还是能支撑基础设施解释。 |
| 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 | 当前不能完全排除维护窗口、共享基础设施或正常组件造成的替代解释。 | 是 | 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 |
| 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。 | 当前仍缺少对“仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。”的直接补证。 | 是 | 仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。 |
| seed 自带的家族提示 Possible web beacon without host corroboration 是否有更多外部证据支撑？ | 当前仍缺少对“seed 自带的家族提示 Possible web beacon without host corroboration 是否有更多外部证据支撑？”的直接补证。 | 是 | seed 自带的家族提示 Possible web beacon without host corroboration 是否有更多外部证据支撑？ |
