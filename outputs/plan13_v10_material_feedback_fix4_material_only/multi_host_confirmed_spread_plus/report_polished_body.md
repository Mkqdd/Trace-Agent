# 首页摘要
- **结论**：可疑事件，建议继续复核
- **严重度**：中
- **研判把握**：中
- **已确认范围**：ws-eng-02, ws-eng-05
- **最强证据**：资产 `ws-eng-02` 在短时间内多次连接到罕见的外部基础设施，并且有可疑的 TLS 会话和 rundll32.exe 执行行为。
- **关键缺口**：仍有 3 条候选扩线事件没有完成独立验证，尚未执行显式反证检查。
- **立即动作**：对重点资产补采主机侧日志，确认是否存在执行、持久化或横向移动证据。
- **一句话结论**：当前事件存在可疑行为，但需要进一步验证以确定其影响范围和性质。

## 调查起点与已知线索

此次调查始于一个可疑事件，该事件最初由资产 `ws-eng-02` 触发。在 2026 年 7 月 14 日 01:04:00 UTC，该资产解析了一个罕见的域名 `sync-cdn-notify.net`，随后在 01:10:00 UTC 发生了可疑的 TLS 会话。这一系列行为引起了我们的注意，因为它们可能表明存在恶意活动。此外，该资产还与罕见的外部基础设施 `203.0.113.77` 进行了多次连接，进一步增加了事件的可疑性。因此，我们决定继续深入调查，以确认是否存在更广泛的威胁。

## 关键对象与角色边界

在此次调查中，我们识别了多个关键对象及其角色。资产 `ws-eng-02` 作为种子资产，是事件的起点。资产 `ws-eng-05` 已被确认为受影响资产，因为它出现了可疑的横向移动行为。资产 `ws-eng-09` 目前仍需独立验证是否真正受到影响，因此被列为待确认关联资产。此外，域名 `sync-cdn-notify.net` 和 IP 地址 `203.0.113.77` 被认定为核心外部基础设施，因为它们在事件过程中反复出现。这些对象的角色和相互关系构成了当前调查的核心范围。

## 关键事实与证据判断

当前判断主要基于以下几个关键事实：

- 2026 年 7 月 14 日 01:04:00 UTC，资产 `ws-eng-02` 解析了罕见的域名 `sync-cdn-notify.net`，随后在 01:10:00 UTC 发生了可疑的 TLS 会话。
- 01:14:20 UTC，该资产再次连接到相同的 C2 基础设施。
- 01:18:00 UTC，可疑的 rundll32.exe 从用户可写的路径启动了一个加载器 DLL。
- 01:21:10 UTC，通过 PsExec 从 `ws-eng-02` 到 `ws-eng-05` 创建了远程服务，这可能是横向移动的迹象。

这些事实共同支持了当前的可疑事件判断。然而，这些事实不能单独推出更强的结论，需要结合其他证据进行综合分析。例如，尽管存在可疑的 TLS 会话和 rundll32.exe 执行行为，但我们还需要进一步验证这些行为的具体性质和影响范围。

## 反证、缺口与结论边界

当前结论受到几个关键缺口的限制：

- 尚未完成对 3 条候选扩线事件的独立验证，这些事件是否应并入主事件范围仍需确认。
- 尚未执行显式反证检查，如维护窗口、补丁、备份或共享基线等。
- 需要围绕当前 关键关联指标 做一次显式扩线，确认是否存在同指标的更大范围复现。

这些缺口限制了我们得出更强结论的能力。例如，我们无法确定这些可疑行为是否构成实际的威胁，或者是否仅仅是误报。因此，当前结论仅限于可疑事件，建议继续复核。

## 结论与后续动作

最终判断为可疑事件，建议继续复核。当前事件存在可疑行为，但需要进一步验证以确定其影响范围和性质。接下来的行动包括：

- 对重点资产补采主机侧日志，确认是否存在执行、持久化或横向移动证据。
- 继续围绕相同域名、目标 IP 和 JA4 通信指纹搜索更宽时间窗内的关联事件。
- 优先核验新扩出的关联事件是否存在独立命中，不要只因为共享基础设施就直接并入主证据链。
- 把关联资产 `ws-eng-09` 纳入复核清单，确认它们是共享基础设施背景还是真实受影响对象。

技术细节、IOC/IOA、关键对象清单、观测引用、证据细目和待补缺口详见技术附录。