# 首页摘要

- **结论**：确认安全事件。
- **严重度**：高。
- **研判把握**：高。
- **已确认范围**：ws-eng-02、ws-eng-05。
- **最强证据**：可疑 TLS 会话、可疑 JA4 告警、可疑 TLS 信标。
- **关键缺口**：扩线得到的 3 条关联事件尚未完成独立验证。
- **立即动作**：优先隔离或重点监控资产：ws-eng-02、ws-eng-05；在边界和代理设备上排查并封禁外部基础设施：203.0.113.77、sync-cdn-notify.net。
- **一句话结论**：当前只能依据已整理的交付判断和证据包保守生成报告材料。

## 调查起点与已知线索

本次调查始于 2026 年 7 月 14 日 01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`，源地址为 `10.70.3.12`。随后，在 01:10:00 UTC，该工作站命中了指向罕见外部基础设施 `sync-cdn-notify.net / 203.0.113.77` 的可疑 TLS 信标。这些初始线索表明，事件具有进一步调查的价值。在 01:14:20 UTC，`ws-eng-02` 重新连接到了同一 C2 基础设施，进一步支持了这一判断。这些事件构成了本次调查的入口线索，提供了初步的通信连续性和高风险行为的支撑。

## 调查范围与研判假设

本次调查的锚点是种子资产 `ws-eng-02`，该资产已被确认受到影响。另一已确认受影响资产是 `ws-eng-05`。此外，`ws-eng-09` 被列为待确认关联资产，当前仍需独立验证是否真正受影响。核心外部基础设施包括 IP 地址 `203.0.113.77` 和域名 `sync-cdn-notify.net`，这些对象是外联和封禁排查的重点，而非内部受影响资产。这些对象的角色边界清晰，确保了调查范围的合理界定。

## 事件过程与行为模式

事件过程显示，2026 年 7 月 14 日 01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`，随后在 01:10:00 UTC 命中了可疑 TLS 信标。在 01:14:20 UTC，该工作站重新连接到同一 C2 基础设施。这些事件支撑了通信连续性的判断，使单点告警具备事件级复核价值。在 01:18:00 UTC，可疑的 `rundll32.exe` 从用户可写路径启动了 loader DLL，进一步增加了高风险行为的嫌疑。然而，这些行为不能直接推出完全控制或恶意载荷已确认。在 01:21:10 UTC，从 `ws-eng-02` 到 `ws-eng-05` 的 PsExec 远程服务创建，表明了潜在的横向移动线索。这些事件展示了事件如何推进，形成了可解释的行为模式。

## 关键事实与证据判断

关键事实包括 2026 年 7 月 14 日 01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`，随后在 01:10:00 UTC 命中了可疑 TLS 信标。在 01:14:20 UTC，该工作站重新连接到同一 C2 基础设施，支撑了通信连续性的判断。在 01:18:00 UTC，可疑的 `rundll32.exe` 从用户可写路径启动了 loader DLL，进一步增加了高风险行为的嫌疑。然而，这些行为不能直接推出完全控制或恶意载荷已确认。在 01:21:10 UTC，从 `ws-eng-02` 到 `ws-eng-05` 的 PsExec 远程服务创建，表明了潜在的横向移动线索。这些事件支撑了当前的判断，但不能推出更强的结论。

## 关联范围与候选边界

已确认受影响对象包括 `ws-eng-02` 和 `ws-eng-05`，这些对象作为确认范围或主链锚点。待确认对象包括 `ws-eng-09`、`ws-lab-02` 和 `cdn-notify-edge.net`，这些对象尚未独立验证，只能作为候选范围，不能并入已确认受影响资产。核心外部基础设施包括 IP 地址 `203.0.113.77` 和域名 `sync-cdn-notify.net`，这些对象是外联和封禁排查的重点，而非内部受影响资产。这些对象的角色边界清晰，确保了调查范围的合理界定。

## 反证、缺口与结论边界

当前最关键缺口在于扩线得到的 3 条关联事件尚未完成独立验证。背景或替代解释事件包括 2026 年 7 月 14 日 01:24:00 UTC，资产 `ws-eng-05` 开始批准的 SCCM 补丁暂存，可能解释部分常规管理活动。在 01:26:00 UTC，从批准的 Microsoft 端点下载了 Windows 更新内容。在 01:36:00 UTC，第二台工作站在连接原始信标主机后，通过 TLS 转向第二个外部 IP。这些事件提供替代解释或共享基础设施边界，但不能单独推翻主链。这些缺口和背景事件限制了更强结论的撰写。

## 结论与后续动作

最终结论是确认安全事件，事件簇已经从单点异常推进到高风险阶段或保守收敛后的多资产范围，足以支撑事件成立。种子告警自带家族/工具提示：Suspected multi-host beacon spread。已观察到关联资产或共享基础设施上的扩展信号，但尚需继续确认是否属于同一事件。主支撑证据、反证检查与报告材料均已到位，可按确认事件交付。立即动作包括优先隔离或重点监控资产：ws-eng-02、ws-eng-05；在边界和代理设备上排查并封禁外部基础设施：203.0.113.77、sync-cdn-notify.net。短期动作包括对扩线得到的关联事件逐条补做独立验证，确认它们是否也命中本地指纹、IOC 或外部情报。持续动作包括以 seed 指标和事件簇中的域名/IP 为关键关联指标，继续检索同时间窗内的重复通信。这些行动建议对应当前证据和边界，确保后续核查和处置动作的有效性。

技术细节、IOC/IOA、关键对象清单、观测引用、证据细目和待补缺口详见技术附录。