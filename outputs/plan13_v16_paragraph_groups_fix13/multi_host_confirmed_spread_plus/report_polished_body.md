# 首页摘要

- **结论**：确认安全事件。
- **严重度**：高。
- **研判把握**：高。
- **已确认范围**：ws-eng-02、ws-eng-05。
- **最强证据**：可疑 TLS 会话、可疑 TLS 信标、PsExec 远程服务创建。
- **关键缺口**：扩线得到的 3 条关联事件尚未完成独立验证。
- **立即动作**：优先隔离或重点监控资产：ws-eng-02、ws-eng-05；在边界和代理设备上排查并封禁外部基础设施：203.0.113.77、sync-cdn-notify.net。
- **一句话结论**：当前只能依据已整理的交付判断和证据包保守生成报告材料。

## 调查起点与已知线索

本次调查始于 2026 年 7 月 14 日 01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`。随后，在 01:10:00 UTC，该工作站命中了指向罕见外部基础设施 `203.0.113.77` 的可疑 TLS 信标。这些初始线索表明了可疑活动的存在，促使进一步调查。

在 01:14:20 UTC，`ws-eng-02` 重新连接到了同一 C2 基础设施。这一行为进一步支持了可疑活动的持续性。这些事件构成了本次调查的入口线索，提供了足够的理由进行深入分析。

## 调查范围与研判假设

已确认受影响的资产包括 `ws-eng-02` 和 `ws-eng-05`。这些资产已被纳入主证据链，显示了可疑活动的存在。而 `ws-eng-09` 作为待确认关联资产，目前仍需独立验证是否真正受到影响，因此不能并入已确认受影响资产范围。

核心外部基础设施包括 IP 地址 `203.0.113.77` 和域名 `sync-cdn-notify.net`。这些对象是外联和封禁排查的重点，但不应被视为内部受影响资产。

## 事件过程与行为模式

事件过程始于 2026 年 7 月 14 日 01:04:00 UTC，`ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`。随后，在 01:10:00 UTC，该工作站命中了指向罕见外部基础设施 `203.0.113.77` 的可疑 TLS 信标。这些事件展示了通信的连续性。

在 01:18:00 UTC，可疑的 `rundll32.exe` 从用户可写路径启动了 loader DLL。紧接着，在 01:21:10 UTC，`ws-eng-02` 到 `ws-eng-05` 发生了 PsExec 远程服务创建，显示出高风险行为。

在 01:28:40 UTC，`ws-eng-05` 连接了原始信标基础设施，进一步证实了横向移动的可能性。这些事件展示了可疑活动的连续性和扩展性。

## 关键事实与证据判断

关键事实包括 2026 年 7 月 14 日 01:04:00 UTC，`ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`，以及 01:10:00 UTC 命中了可疑 TLS 信标的事件。这些事件展示了通信的连续性，支持了可疑活动的存在。

在 01:18:00 UTC，可疑的 `rundll32.exe` 从用户可写路径启动了 loader DLL，显示出高风险行为。随后，在 01:21:10 UTC，`ws-eng-02` 到 `ws-eng-05` 发生了 PsExec 远程服务创建，进一步证实了横向移动的可能性。

归纳判断显示，检索到 6 条偏可疑事件，围绕 `sync-cdn-notify.net`、`203.0.113.77` 和 `10.70.3.45` 展开。扩线检索到 4 条可疑关联事件，围绕 `sync-cdn-notify.net`、`cdn-notify-edge.net` 和 `203.0.113.77` 展开，当前先作为待验证候选线索。

## 关联范围与候选边界

已确认受影响的资产包括 `ws-eng-02` 和 `ws-eng-05`。这些资产已被纳入主证据链，显示了可疑活动的存在。而 `ws-eng-09` 作为待确认关联资产，目前仍需独立验证是否真正受到影响，因此不能并入已确认受影响资产范围。

候选扩线节点包括 2026 年 7 月 14 日 01:32:10 UTC，`ws-eng-05` 解析了罕见域名 `cdn-notify-edge.net`。此外，01:47:30 UTC，`ws-eng-09` 连接到次级基础设施，显示出潜在的横向移动。然而，这些事件尚未完成独立验证，因此只能作为待确认扩线或边界线索。

核心外部基础设施包括 IP 地址 `203.0.113.77` 和域名 `sync-cdn-notify.net`。这些对象是外联和封禁排查的重点，但不应被视为内部受影响资产。

## 反证、缺口与结论边界

当前最关键缺口在于扩线得到的 3 条关联事件尚未完成独立验证。这些事件包括 2026 年 7 月 14 日 01:32:10 UTC，`ws-eng-05` 解析了罕见域名 `cdn-notify-edge.net`，以及 01:47:30 UTC，`ws-eng-09` 连接到次级基础设施。这些事件尚未独立验证，因此不能并入主事件范围。

背景或替代解释事件包括 01:24:00 UTC，`ws-eng-05` 开始批准的 SCCM 补丁暂存，可能解释部分常规管理活动。此外，01:26:00 UTC，`ws-eng-05` 从批准的 Microsoft 端点下载了 Windows 更新内容。这些事件只能用于收窄边界，不能单独推翻主链。

共享基础设施解释包括 01:46:20 UTC，`ws-lab-02` 通过不同供应商域名访问了同一托管 IP，提示该次级 IP 可能是共享基础设施。这些解释只能用于收窄边界，不能单独推翻主链。

## 结论与后续动作

最终结论是确认安全事件。当前证据显示可疑活动的存在，但仍有关键缺口需要解决。立即动作包括优先隔离或重点监控资产 `ws-eng-02` 和 `ws-eng-05`，以及在边界和代理设备上排查并封禁外部基础设施 `203.0.113.77` 和 `sync-cdn-notify.net`。

短期核查建议包括对扩线得到的关联事件逐条补做独立验证，确认它们是否也命中本地指纹、IOC 或外部情报。此外，以种子指标和事件簇中的域名/IP 为关键关联指标，继续检索同时间窗内的重复通信。

持续复核建议包括围绕当前关键关联指标做一次显式扩线，确认是否存在同指标的更大范围复现。这些行动建议对应当前证据和边界，旨在进一步验证和确认事件范围。

技术细节、IOC/IOA、关键对象清单、观测引用、证据细目和待补缺口详见技术附录。