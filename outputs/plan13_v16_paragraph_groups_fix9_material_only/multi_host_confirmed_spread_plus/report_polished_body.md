# 首页摘要

- **结论**：确认安全事件
- **严重度**：高
- **研判把握**：高
- **已确认范围**：ws-eng-02、ws-eng-05
- **最强证据**：可疑 TLS 会话、恶意 JA4 信标、可疑执行线索
- **关键缺口**：扩线得到的 3 条关联事件尚未完成独立验证
- **立即动作**：优先隔离或重点监控资产：ws-eng-02、ws-eng-05；在边界和代理设备上排查并封禁外部基础设施：203.0.113.77、sync-cdn-notify.net
- **一句话结论**：当前只能依据已整理的交付判断和证据包保守生成报告材料。

## 调查起点与已知线索

这些事实说明本节为什么需要展开。种子告警首先落在资产 `ws-eng-02` 上，该资产在 2026-07-14 01:04:00 UTC 解析了罕见域名 `sync-cdn-notify.net`。随后，在 01:10:00 UTC，该工作站命中了指向罕见外部基础设施 `203.0.113.77` 的可疑 TLS 信标。这些初始线索表明，该资产可能受到了恶意活动的影响，因此需要进一步调查。

这些事实说明本节为什么需要展开。在 01:14:20 UTC，`ws-eng-02` 重新连接到了同一 C2 基础设施。这一行为进一步支持了该资产受到恶意活动影响的可能性。这些事实说明了为什么需要继续研判，因为可疑的网络活动和重复连接到同一基础设施的行为表明了潜在的恶意活动。

## 调查范围与研判假设

这些对象或事件需要保留在候选范围，不能并入已确认受影响资产。已确认受影响资产包括 `ws-eng-02` 和 `ws-eng-05`，而 `ws-eng-09` 仍需独立验证是否真正受影响。这些对象或事件需要保留在候选范围，不能并入已确认受影响资产。

这些基础设施是外联和封禁排查对象，不是内部受影响资产。核心外部基础设施包括 IP 地址 `203.0.113.77` 和域名 `sync-cdn-notify.net`。这些对象是当前主证据链中反复出现的关键对象，但它们只是外联和封禁排查对象，而不是内部受影响资产。

## 事件过程与行为模式

2026-07-14 01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`。随后，在 01:10:00 UTC，该工作站命中了指向罕见外部基础设施 `203.0.113.77` 的可疑 TLS 信标。在 01:14:20 UTC，该工作站重新连接到了同一 C2 基础设施。这些事件展示了可疑的网络活动和重复连接到同一基础设施的行为，表明了潜在的恶意活动。

在 01:18:00 UTC，可疑的 `rundll32.exe` 在信标序列后从用户可写路径启动了 `loader DLL`。随后，在 01:21:10 UTC，从 `ws-eng-02` 到 `ws-eng-05` 创建了 PsExec 远程服务。这些执行与远程服务线索提高了当前判断的权重，但还不能直接推出完全控制或恶意载荷已确认。

在 01:28:40 UTC，资产 `ws-eng-05` 连接了原始信标基础设施 `sync-cdn-notify.net / 203.0.113.77`。在 01:46:20 UTC，资产 `ws-lab-02` 通过不同供应商域名访问了同一托管 IP `198.51.100.44`，提示该次级 IP 可能是共享基础设施。这些节点提供维护、更新或共享基础设施背景，帮助限定主链解释。

在 01:32:10 UTC，资产 `ws-eng-05` 解析了次级罕见域名 `cdn-notify-edge.net`。在 01:36:00 UTC，第二台工作站通过 TLS 转向第二个外部 IP `198.51.100.44`。在 01:39:20 UTC，WMI 远程进程创建从 `ws-eng-05` 到 `ws-eng-09`。这些节点提供维护、更新或共享基础设施背景，帮助限定主链解释。

在 01:44:10 UTC，第三台工作站 `ws-eng-09` 解析了扩线后才出现的次级域名 `cdn-notify-edge.net`。这些节点提供维护、更新或共享基础设施背景，帮助限定主链解释。

## 关键事实与证据判断

这些事件展示了可疑的网络活动和重复连接到同一基础设施的行为，表明了潜在的恶意活动。

在 01:04:00 UTC，资产 `ws-eng-02` 解析了罕见域名 `sync-cdn-notify.net`。

随后，在 01:10:00 UTC，该工作站命中了指向罕见外部基础设施 `203.0.113.77` 的可疑 TLS 信标。

在 01:14:20 UTC，该工作站重新连接到了同一 C2 基础设施。 这些事件展示了可疑的网络活动和重复连接到同一基础设施的行为，表明了潜在的恶意活动。 在 01:18:00 UTC，可疑的 `rundll32.exe` 在信标序列后从用户可写路径启动了 `loader DLL`。 随后，在 1:21:10 UTC，从 `ws-eng-02` 到 `ws-eng-05` 创建了 PsExec 远程服务。 这些执行与远程服务线索提高了当前判断的权重，但还不能直接推出完全控制或恶意载荷已确认。 归纳判断只能辅助解释证据链，不能替代关键事件事实。 检索到 6 条偏可疑事件，围绕 `sync-cdn-notify.net`、`203.0.113.77`、`10.70.3.45` 展开。 扩线检索到 4 条可疑关联事件，围绕 `sync-cdn-notify.net`、`cdn-notify-edge.net`、`203.0.113.77` 展开；当前先作为待验证候选线索。 这些归纳判断只能辅助解释证据链，不能替代关键事件事实。

## 关联范围与候选边界

这些对象或事件需要保留在候选范围，不能并入已确认受影响资产。

已确认受影响资产包括 `ws-eng-02` 和 `ws-eng-05`，而 `ws-eng-09` 仍需独立验证是否真正受影响。

这些对象或事件需要保留在候选范围，不能并入已确认受影响资产。

这些基础设施是外联和封禁排查对象，不是内部受影响资产。 核心外部基础设施包括 IP 地址 `203.0.113.77` 和域名 `sync-cdn-notify.net`。 这些对象是当前主证据链中反复出现的关键对象，但它们只是外联和封禁排查对象，而不是内部受影响资产。 这些对象或事件需要保留在候选范围，不能并入已确认受影响资产。 候选事件包括 01:32:10 UTC 的 `ws-eng-05` 解析次级罕见域名 `cdn-notify-edge.net`，以及 01:47:30 UTC 的 `ws-eng-09` 接触次级基础设施。 这些对象或事件需要保留在候选范围，不能并入已确认受影响资产。

## 反证、缺口与结论边界

这些未闭合问题限制更强结论和更宽范围写法。扩线得到的 3 条关联事件尚未完成独立验证，是否应并入主事件范围？仍需显式检查维护窗口、补丁、备份或共享基线等反证。仍需把当前事件事实沉淀为 claim 或可引用实体，避免报告只剩原始事件聚合。仍需围绕当前关键关联指标做一次显式扩线，确认是否存在同指标的更大范围复现。这些未闭合问题限制更强结论和更宽范围写法。

背景事件提供替代解释或共享基础设施边界，但不能单独推翻主链。在 01:24:00 UTC，资产 `ws-eng-05` 开始批准的 SCCM 补丁暂存，可能解释部分常规管理活动。在 01:26:00 UTC，从批准的 Microsoft 端点下载了 Windows 更新内容。在 01:36:00 UTC，第二台工作站通过 TLS 转向第二个外部 IP。这些背景或候选边界事件提供替代解释或共享基础设施边界，但不能单独推翻主链。

背景事件提供替代解释或共享基础设施边界，但不能单独推翻主链。在 01:44:10 UTC，第三台工作站 `ws-eng-09` 解析了扩线后才出现的次级域名 `cdn-notify-edge.net`。在 01:46:20 UTC，资产 `ws-lab-02` 通过不同供应商域名访问了同一托管 IP `198.51.100.44`，提示该次级 IP 可能是共享基础设施。这些背景或候选边界事件提供替代解释或共享基础设施边界，但不能单独推翻主链。

## 结论与后续动作

这些建议对应当前证据边界，应写成后续核查和处置动作。确认安全事件；事件簇已经从单点异常推进到高风险阶段或保守收敛后的多资产范围，足以支撑事件成立。种子告警自带家族/工具提示：Suspected multi-host beacon spread。已观察到关联资产或共享基础设施上的扩展信号，但尚需继续确认是否属于同一事件。主支撑证据、反证检查与报告材料均已到位，可按确认事件交付。优先隔离或重点监控资产：ws-eng-02、ws-eng-05。在边界和代理设备上排查并封禁外部基础设施：203.0.113.77、sync-cdn-notify.net。这些建议对应当前证据边界，应写成后续核查和处置动作。

这些建议对应当前证据边界，应写成后续核查和处置动作。对扩线得到的关联事件逐条补做独立验证，确认它们是否也命中本地指纹、IOC 或外部情报。以 seed 指标和事件簇中的域名 / IP 为关键关联指标，继续检索同时间窗内的重复通信。这些建议对应当前证据边界，应写成后续核查和处置动作。

技术细节、IOC/IOA、关键对象清单、观测引用、证据细目和待补缺口详见技术附录。