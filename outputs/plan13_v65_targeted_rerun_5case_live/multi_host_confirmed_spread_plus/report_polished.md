# 首页摘要

- **结论：** 确认安全事件
- **严重度：** 高危
- **研判把握：** 94%
- **已确认范围：** 资产 `ws-eng-02` 与 `ws-eng-05` 已纳入确认受影响范围，核心外联基础设施为 `sync-cdn-notify.net` 与 `203.0.113.77`
- **一句话结论：** 以种子告警为起点，已构建出一条包含信标外联、可疑执行与跨主机远程服务创建的多阶段事件链，并形成多主机扩散迹象
- **立即动作：** 隔离资产 `ws-eng-02` 和 `ws-eng-05`，在边界设备上封禁 `203.0.113.77` 和 `sync-cdn-notify.net`，对候选扩线事件启动独立验证

## 1. 事件结论与当前判断

本次调查分析窗口为 2026-07-14 01:04:00 UTC 至 2026-07-14 01:39:20 UTC，触发种子告警为针对资产 `ws-eng-02` 发出的“Suspected multi-host beacon spread”可疑多主机信标扩散告警。经证据图与假设板组织论证，当前确认存在一条包含信标外联、可疑进程执行和横向移动的可疑活动链，且该活动已从最初的单台工作站扩展到至少两台资产。种子告警中命中的 JA4 指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc` 和恶意外联域名 `sync-cdn-notify.net` 构成了事件簇的核心关联指标。

事件簇已经从单点异常推进到高风险阶段的多资产范围，足以支撑事件成立。当前主支撑证据和范围已达到确认事件交付门槛，因此得出“确认安全事件”结论。剩余候选扩线事件、反证及背景材料作为报告边界呈现，不削弱已确认事件的定性。

## 2. 事件过程与关键时间线

事件起点位于 2026-07-14 01:04:00 UTC，种子资产 `ws-eng-02` 对罕见域名 `sync-cdn-notify.net` 发起 DNS 解析请求，该行为被标记为可疑命令与控制通信的前兆。六分钟后，即 01:10:00 UTC，一条 JA4 指纹匹配的告警确认 `ws-eng-02` 与相同域名及其解析地址 `203.0.113.77` 之间建立了可疑 TLS 信标连接，此为整个事件的主支撑告警事件。

在告警触发后，资产 `ws-eng-02` 于 01:14:20 UTC 重新连接至同一命令与控制基础设施，进一步证实了通信的持续性与非偶然性。随后的主机侧行为进一步加剧了事件严重性：在 01:18:00 UTC，`ws-eng-02` 上出现可疑的 `rundll32.exe` 进程，从一个用户可写路径加载了加载器 DLL，该执行线索紧跟在信标序列之后发生，构成了可疑执行链的佐证。

横向移动迹象出现在 01:21:10 UTC，此时 `ws-eng-02` 利用 PsExec 向另一台资产发起远程服务创建操作，目标地址为 `10.70.3.45`。该动作直接关联到第二台工作站的出现：在 01:28:40 UTC，资产 `ws-eng-05` 也开始与原始信标基础设施 `sync-cdn-notify.net` 和 `203.0.113.77` 进行可疑通信，其源地址正是前述横向移动的目标 `10.70.3.45`，由此形成了从种子资产到第二资产的清晰传播链条。

在 01:32:10 UTC 和 01:36:00 UTC，资产 `ws-eng-05` 还出现了对第二个罕见域名 `cdn-notify-edge.net` 及其解析 IP `198.51.100.44` 的 DNS 查询与 TLS 外联事件，这些事件目前处于待验证候选状态，可作为扩线线索跟进。分析窗口内最后一条事件发生在 01:39:20 UTC，`ws-eng-05` 通过 WMI 向 `10.70.3.90` 发起远程进程创建，该事件已定位但当前仅以边界线索保留，不具备独立验证条件。

在同一时间窗口内，资产 `ws-eng-05` 还观测到与 SCCM 补丁暂存、Windows 更新下载等维护活动相关的网络流量，这些背景或候选边界事件将作为评估替代解释的关键材料在后续章节中讨论。

## 3. 关键证据判断

当前确认安全事件的结论基于一条由多项已确认事实构建的证据主链。这条主链的起点是种子资产 `ws-eng-02` 对 `sync-cdn-notify.net` 的稀有域名解析，在时间上与后续发生的 JA4 指纹命中告警紧密衔接，且该告警明确将通信定性为恶意信标行为。告警后的重连行为表明该外联并非短暂误报或配置错误，而是具备持续性的命令与控制通信。主机层面，在信标通信活跃期之后紧跟着出现了通过 `rundll32.exe` 加载可疑 DLL 的执行事件，尽管当前缺少命令行参数和落地文件的深度取证，该事件在时间序列和行为匹配上与信标阶段高度相关，构成一条指向可疑执行活动的可疑线索。

横向移动的证据则将事件从单机风险升级为多主机扩散。`ws-eng-02` 通过 PsExec 向 `10.70.3.45` 发起远程服务创建，而该目标地址随后被确认为资产 `ws-eng-05`。在远程服务创建事件发生数分钟后，`ws-eng-05` 即开始与完全相同的信标基础设施进行通信，这一因果链条和时序关系很难单纯用巧合或独立感染解释。综合上述 DNS、告警、重连、执行和远程服务创建事件，可以合理判断本次活动至少跨越 `ws-eng-02` 和 `ws-eng-05` 两台工作站，构成了疑似多主机信标扩散的态势。

然而，目前证据尚不足以支撑更强结论。虽然我们观测到了从 `ws-eng-02` 到 `ws-eng-05` 的远程服务创建及后续信标行为，但缺少直接证明 `ws-eng-05` 上恶意载荷成功落地、执行并产生持久化的主机侧证据，例如计划任务、服务安装或注册表驻留项。同样地，对于 `rundll32.exe` 所加载的 DLL 缺少静态分析、内存镜像或网络连接进程链的完整映证，因此无法在报告中断言该加载器已成功实施载荷注入或数据窃取。候选对象 `10.70.3.90` 由于仅有单一的 WMI 远程进程创建事件且无独立验证指示物，目前不能纳入已确认受影响范围。

## 4. 影响范围、候选对象与外部基础设施

已确认为本次事件中受影响的资产为 `ws-eng-02` 和 `ws-eng-05`，它们构成了当前事件簇的确认范围。种子资产 `ws-eng-02` 是可疑活动的起点，其上观测到完整的 DNS 解析、信标告警、重连和可疑执行行为。资产 `ws-eng-05` 因被 PsExec 远程服务创建事件指向，且随后开始向同一命令与控制基础设施发起信标通信，故纳入确认受影响范围，需要隔离和取证。在确认受影响范围之外，存在若干待验证候选资产与指标。资产 `ws-eng-05` 在 01:39:20 UTC 向 `10.70.3.90` 发起的 WMI 远程进程创建事件，由于缺乏可独立验证的辅助指示物且未被主证据链中其他事实交叉验证，当前仅作为边界线索保留，不构成确认受影响资产。

此外，`ws-eng-05` 对域名 `cdn-notify-edge.net` 和 IP `198.51.100.44` 的 DNS 查询与 TLS 通信同样被归类为候选扩线事件，这些对象已完成初步的局部情报查证但未命中独立的外部情报，因此尚不能直接确认其恶意属性，需要进一步排查和验证。

在核心外部基础设施方面，事件主链明确指向了 `203.0.113.77` 和 `sync-cdn-notify.net`，这两个对象在种子告警和两台资产的信标通信中反复出现，属于关联基础设施和排查封禁对象。背景对象如 `download.windowsupdate.com` 和 `13.107.246.45`，则在 `ws-eng-05` 的维护窗口中出现，与 Windows 更新相关，当前作为共享或背景基础设施处理，用于解释合法管理流量，不将其视为攻击设施的一部分。待确认的 JA4 指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc` 作为种子命中指纹，其本身已是确认的主链对象，但作为独立指标在全网范围内复现的概率和分布仍需进一步扩线排查。

## 5. 反证、替代解释与未闭合缺口

在围绕种子告警和主证据链构建事件结论的同时，我们识别并评估了可能削弱或限制当前判断强度的反证和替代解释。资产 `ws-eng-05` 在分析窗口内出现了一系列与系统维护相关的活动，包括 SCCM 补丁暂存于 01:24:00 UTC 开启，以及 01:26:00 UTC 从认可 Microsoft 端点 `download.windowsupdate.com` 下载更新内容。这些活动的时间窗口与 `ws-eng-05` 首次出现信标外联的时刻部分重叠，因此无法完全排除部分远程进程创建或网络流量与计划内维护操作相关的可能性。然而，这些维护活动的存在仅能说明观察窗口内混合了合法与可疑流量，并不能解释为何一台刚被种子资产远程操作的工作站会在维护窗口期间精准地连接至已知恶意信标基础设施。因此，该替代解释的作用在于收窄结论的边界，即我们在判断 `ws-eng-05` 的受影响程度时需要注意区分维护流量与恶意流量，而不应将其用作否定事件发生的反证。

当前分析仍存在若干未闭合缺口，这些缺口限制了报告可以得出的更强结论。仍然缺少针对整个候选扩线范围的显式验证，特别是 `cdn-notify-edge.net` 和 `198.51.100.44` 是否会在同时间窗内被内部更多资产解析或连接，这使我们无法判断当前观测到的扩散范围是否为全貌。同时，我们对 `Suspected multi-host beacon spread` 这一家族提示的外部技术背景理解不足，尚不能判断该活动是否对应已知攻击组织或工具集，因此当前报告无法完成攻击归因。主机侧的缺口同样限制了对内网横向移动程度的判断，因为尚未获得确认表明 `ws-eng-05` 上已建立持久化机制或已完成数据外传。总结而言，这些缺口意味着虽然我们有充分证据宣告安全事件成立，但暂时不能得出“已观察到获得完全控制”“横向移动已闭环”或“数据已被窃取”等更强结论。

## 6. 处置建议与后续核查

- **隔离受影响资产：** 对象为 `ws-eng-02` 和 `ws-eng-05`。立即将这两台工作站从生产网络中断开，通过端点检测与响应平台或系统事件日志，收集其进程树、服务列表、计划任务和网络连接快照，目标是捕捉持久化机制、横向移动工具或未被当前分析覆盖的恶意载荷。
- **封禁外部基础设施：** 对象为 `203.0.113.77` 和 `sync-cdn-notify.net`。在边界防火墙、安全Web网关和DNS递归解析器上添加封禁策略，通过代理和DNS查询日志回溯同时间窗内所有对上述指标的外联记录，验证是否存在其他遗漏的受影响资产。
- **验证候选扩线事件：** 对象为 `ws-eng-05` 涉及的 `cdn-notify-edge.net`、`198.51.100.44` 以及向 `10.70.3.90` 的 WMI 远程进程创建事件。利用本地威胁情报平台和外部公开情报源，逐一查证这些对象是否与已知可疑活动关联，同时检查 `ws-eng-05` 和 `10.70.3.90` 的主机日志以核验进程创建事件的上下文，目标是将候选线索升级为确认证据或排除误报。
- **扩大排查范围：** 以 `sync-cdn-notify.net`、`203.0.113.77` 和 JA4 指纹 `t13d1516h2_2ba4190accc0_13fe129f5abc` 为关键度量指标。在SIEM或日志聚合平台中检索分析窗口内全量资产对这些指标的DNS、TLS和网络流日志记录，目标为确认当前扩散范围是否仅限于两台已确认资产，或存在尚未发现的第三台受影响设备。
- **强化主机取证：** 对象为 `ws-eng-02`。提取 `rundll32.exe` 进程加载的 DLL 文件进行静态分析，并调取该进程启动时的完整命令行参数，结合内存转储或执行后文件系统时间线，明确该可疑执行是否成功导致了后续的代码注入、凭据窃取或横向移动准备。

---

# 事件调查技术附录

以下内容主要供分析、复盘和技术排查使用，不直接替代主报告结论。

## 1. IOC / IOA 清单

| 类型 | 值 | 角色 | 状态 |
| -- | -- | -- | -- |
| IP | 203.0.113.77 | 核心外部基础设施 | 已确认 |
| 域名 | sync-cdn-notify.net | 核心外部基础设施 | 已确认 |
| IP | 13.107.246.45 | 背景/上下文指标 | 已确认 |
| 域名 | download.windowsupdate.com | 背景/上下文指标 | 已确认 |
| 指纹 | t13d1516h2_2ba4190accc0_13fe129f5abc | 扩展查询候选 | 待确认 |
| 内网 IP | 10.70.3.12 | 关联内部地址 | 已确认 |
| 内网 IP | 10.70.3.45 | 关联内部地址 | 已确认 |
| 内网 IP | 10.70.3.90 | 关联内部地址 | 仅上下文 |
| IP | 198.51.100.44 | 扩展查询候选 | 已验证待确认 |
| 域名 | cdn-notify-edge.net | 扩展查询候选 | 已验证待确认 |

## 2. 关键对象清单

| 对象 | 类型 | 当前角色 | 是否已纳入证据链 | 备注 |
| -- | -- | -- | -- | -- |
| ws-eng-02 | 资产 | 种子资产 | 是 | 种子告警首先落在该资产上。；当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| ws-eng-05 | 资产 | 已确认受影响资产 | 是 | 当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。；事件聚类中仍需独立验证的候选资产。；候选事件缺少可独立验证指示物，当前只作为上下文边界保留。；已完成 grounding 但暂未获得独立支撑，当前作为边界线索保留。 |
| 203.0.113.77 | IP | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选地址。 |
| sync-cdn-notify.net | 域名 | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选域名。 |
| 13.107.246.45 | IP | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选地址。 |
| download.windowsupdate.com | 域名 | 背景/上下文指标 | 是 | 用于补充边界或背景解释的对象。；当前 pivot 里出现的扩线候选域名。 |
| t13d1516h2_2ba4190accc0_13fe129f5abc | JA4 | 种子命中指纹 | 是 | 种子告警命中的关键通信指纹。 |
| Suspected multi-host beacon spread | 家族提示 | 家族/工具提示 | 是 | 辅助解释本案的家族或工具背景，不直接等于强归因。 |
| 10.70.3.12 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。 |
| 10.70.3.45 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。；当前 pivot 里出现的扩线候选地址。 |
| 10.70.3.90 | 内网 IP | 关联内部地址 | 否 | 当前 pivot 里出现的扩线候选地址。；候选事件缺少可独立验证指示物，当前只作为上下文边界保留。 |
| 198.51.100.44 | IP | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选地址。；已完成 grounding 但暂未获得独立支撑，当前作为边界线索保留。 |
| cdn-notify-edge.net | 域名 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选域名。；候选事件涉及的域名，尚未进入已确认主链。；已完成 grounding 但暂未获得独立支撑，当前作为边界线索保留。 |
| t13d1516h2_2ba4190accc0_13fe129f5abc | 指纹 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选指纹。 |

## 3. 关键证据细目

### 证据 E-01
- **证据类型**：主支撑证据
- **证据类别**：调查触发证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:10:00 UTC，资产 `ws-eng-02` 围绕 203.0.113.77 / sync-cdn-notify.net 的异常通信触发了种子告警。
- **为什么重要**：这是种子命中的直接落点，说明调查起点并不是孤立的指纹命中。
- **边界与限制**：单条种子命中本身不能独立证明事件成立，仍需要结合重复通信、资产背景或主机侧线索共同定性。
- **主要依据来源**：种子事件窗观测
- **观测引用**：`obs-001`

### 证据 E-02
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:28:40 UTC，资产 `ws-eng-05` 再次与 203.0.113.77 / sync-cdn-notify.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、资产背景核查
- **观测引用**：`obs-001, obs-002`

### 证据 E-03
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:04:00 UTC，资产 `ws-eng-02` 先解析了少见域名 `sync-cdn-notify.net`，为后续与同一基础设施的通信提供了准备。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测
- **观测引用**：`obs-001`

### 证据 E-04
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（主机）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:18:00 UTC，资产 `ws-eng-02` 在异常通信后出现 `rundll32.exe` 可疑启动，伴随 DLL 加载，路径位于用户可写目录。
- **为什么重要**：它把异常从通信层推进到主机执行或其他更高风险阶段，对事件定性的权重明显更高。
- **边界与限制**：当前仍缺少更细的命令行、落地文件、持久化或内存证据，因此可以确认存在执行线索，但还不能完整描述具体载荷。
- **主要依据来源**：种子事件窗观测
- **观测引用**：`obs-001`

### 证据 E-05
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:21:10 UTC，资产 `ws-eng-02` 出现指向 `10.70.3.45` 的远程服务创建，说明异常已经开始跨主机推进。
- **为什么重要**：它表明异常已经从外联推进到跨主机动作，比单纯重复通信更能支撑事件升级。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测
- **观测引用**：`obs-001`

### 证据 E-06
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（主机）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:39:20 UTC，资产 `ws-eng-05` 出现指向 `10.70.3.90` 的远程进程创建。
- **为什么重要**：这说明异常已经从种子资产延伸到其他主机，事件范围不再局限于单点。
- **边界与限制**：当前仍缺少更细的命令行、落地文件、持久化或内存证据，因此可以确认存在执行线索，但还不能完整描述具体载荷。
- **主要依据来源**：资产背景核查、候选对象情报验证
- **观测引用**：`obs-002, obs-003`

### 证据 E-07
- **证据类型**：主支撑证据
- **证据类别**：范围确认依据（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:36:00 UTC，资产 `ws-eng-05` 再次与 198.51.100.44 / cdn-notify-edge.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：它把事件范围从种子资产推进到其他资产，使当前结论不再只停留在单点异常。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：资产背景核查、候选对象情报验证
- **观测引用**：`obs-002, obs-004`

### 证据 C-01
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（资产背景）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:24:00 UTC，资产 `ws-eng-05` 同时间窗存在已批准的维护、变更或备份活动，可解释部分日常管理行为。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：资产背景核查
- **观测引用**：`obs-002`

### 证据 C-02
- **证据类型**：反证或替代解释
- **证据类别**：反证与替代解释（网络）
- **证据强度**：高
- **事实描述**：在 2026-07-14 01:26:00 UTC，资产 `ws-eng-05` 与已批准的供应商或更新端点通信，更接近计划内流量。
- **为什么重要**：这条观测更支持背景解释或替代解释，而不是继续放大当前主假设。
- **边界与限制**：这类背景解释能够帮助收窄判断，但如果后续出现脱离基线的重复通信、执行线索或跨资产异常，它本身并不能永久关闭事件。
- **主要依据来源**：资产背景核查
- **观测引用**：`obs-002`

## 4. 观测引用对照

| 观测编号 | 来源工具 | 事实摘要 | 关系 |
| -- | -- | -- | -- |
| `obs-001` | `search_seed_context` | 检索到 6 条偏可疑事件，围绕 sync-cdn-notify.net、203.0.113.77、10.70.3.45 展开。 | `supporting` |
| `obs-002` | `expand_asset_scope` | 扩线检索到 4 条可疑关联事件，围绕 sync-cdn-notify.net、cdn-notify-edge.net、203.0.113.77 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-003` | `ground_candidate_event` | 候选事件 evt-811 缺少可独立验证的指示物，当前不并入主证据链，仅保留为边界线索。 | `context` |
| `obs-004` | `ground_candidate_event` | 候选事件 evt-810 已完成 local_intel grounding，但暂未命中独立情报，当前不直接并入主证据链。 | `context` |

## 5. 正文章节引用映射

| 正文章节 | 主要 evidence / observation | 用途 |
| -- | -- | -- |
| 首页摘要 | E-01、E-02、expand_cluster_scope | 当前状态 / 当前最强证据 / 当前最关键缺口 |
| 第 5 节 | E-01、E-02、E-03、E-04、E-05、E-06、E-07、C-01、C-02 | 主支撑证据 / 反证与替代解释 |
| 第 9 节 | expand_cluster_scope、check_counterevidence、validate_candidate_events | 证据链摘要 / 缺口说明 |
| 第 10 节 | E-01、E-02 | 结论 / 动作 / 复核重点 |

## 6. 证据来源与参考资料

| 来源类型 | 来源名称 | 用途 | 边界 |
| -- | -- | -- | -- |
| 上游检测 | t13d1516h2_2ba4190accc0_13fe129f5abc / Suspected multi-host beacon spread | 提供本案的调查起点与初始命中线索。 | 命中本身并不自动等于事件成立，仍需要后续本地事实补强。 |
| 内部观测 | trace_store.seed_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 内部观测 | trace_store.asset_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 外部结构化情报 | candidate_event:evt-811 | 补充基础设施、家族或公开背景解释。 | 外部情报不能单独替代本地事实，也不能直接把本案写成强归因。 |
| 外部结构化情报 | candidate_event:evt-810 | 补充基础设施、家族或公开背景解释。 | 外部情报不能单独替代本地事实，也不能直接把本案写成强归因。 |

## 7. 证据缺口与待补查询

| 缺口 | 限制的结论 | 当前是否可补 | 最值得补的查询 |
| -- | -- | -- | -- |
| 仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。 | 当前仍缺少对“仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。”的直接补证。 | 是 | 仍需围绕当前关键关联指标开展一轮扩线核查，确认是否存在同指标的更大范围复现。 |
| 扩线得到的 1 条关联事件尚未完成独立验证，是否应并入主事件范围？ | 当前仍缺少对“扩线得到的 1 条关联事件尚未完成独立验证，是否应并入主事件范围？”的直接补证。 | 否 | 该缺口已多轮核查但仍未获得新的有效信息，更适合作为报告未决事项。 |
| 补充 seed 家族、指纹或外部基础设施的公开技术背景，明确它只能作为背景还是能支撑基础设施解释。 | 当前不适合仅凭背景情报完成基础设施或家族级强归因。 | 否 | 该缺口已多轮核查但仍未获得新的有效信息，更适合作为报告未决事项。 |
| 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 | 当前不能完全排除维护窗口、共享基础设施或正常组件造成的替代解释。 | 是 | 仍需显式检查维护窗口、补丁、备份或共享基线等反证。 |
