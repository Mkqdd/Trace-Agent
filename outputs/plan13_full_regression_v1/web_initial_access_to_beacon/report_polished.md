# 首页摘要
- **结论**：确认安全事件
- **严重度**：高
- **研判把握**：高
- **已确认范围**：web-app-01
- **一句话结论**：事件已经从初始异常发展到高风险阶段，影响范围可能扩大，需要立即采取行动以防止进一步损害。
- **立即动作**：优先隔离或重点监控资产：web-app-01；在边界和代理设备上排查并封禁外部基础设施：198.51.100.88、cdn-auth-check.net。

## 1. 事件背景与已知线索
本次安全事件始于2026年5月11日14:02:00 UTC，当时公共访问的Web服务器`web-app-01`接收到针对登录端点的可疑利用请求。随后，该服务器在短时间内解析了一个罕见的域名`cdn-auth-check.net`，并触发了种子告警，匹配到了一个可能的web-shell信标。这些事件表明了潜在的入侵尝试，并且提供了恶意行为存在的直接证据。

## 2. 范围界定与调查假设
事件范围包括确认受到影响的资产`web-app-01`，以及作为核心外部基础设施的IP地址`198.51.100.88`和域名`cdn-auth-check.net`。初步调查假设认为，这些外部基础设施可能是恶意活动的一部分，需要进一步调查以确认其影响范围。

## 3. 对象覆盖策略与关键实体
关键实体包括种子资产`web-app-01`，以及核心外部基础设施`198.51.100.88`和`cdn-auth-check.net`。这些对象在事件中反复出现，是主要的调查焦点。

## 4. 事件机制分解
事件机制从初始异常开始，逐步演变为可疑活动，最终确认了恶意行为的存在。可疑活动包括针对登录端点的利用尝试，解析罕见域名，以及匹配到web-shell信标的种子告警。这些活动表明了恶意软件的执行阶段，进一步证实了事件的严重性。

## 5. 关键证据与异常事实
关键证据包括针对登录端点的可疑利用请求、解析罕见域名`cdn-auth-check.net`、匹配到web-shell信标的种子告警，以及后续的TLS连接和PowerShell启动器的执行。这些证据相互支撑，共同指向了恶意行为的存在。然而，这些事件仅表明了恶意软件的执行阶段，但无法单独确定其对整个网络的影响。此外，仍需进一步调查以确认是否存在更大的影响范围。

## 6. 时序特征与行为模式
决定性的时序特征显示，事件从2026年5月11日14:02:00 UTC开始，经历了可疑利用请求、解析罕见域名、匹配到web-shell信标、重新连接到外部基础设施，以及PowerShell启动器的执行。这一系列行为模式表明了恶意软件的执行过程。

## 7. 传播与关联分析
传播分析显示，事件从初始异常逐步扩展到可疑活动，最终确认了恶意行为的存在。可疑活动涉及解析罕见域名和匹配到web-shell信标的种子告警，这些活动表明了恶意软件的执行阶段。然而，这些事件仅表明了恶意软件的执行阶段，但无法单独确定其对整个网络的影响。此外，仍需进一步调查以确认是否存在更大的影响范围。

## 8. 影响分析
影响分析表明，事件已经从初始异常发展到高风险阶段，影响范围可能扩大。尽管这些事件提供了恶意行为的直接证据，但仍需进一步调查以确定其影响范围。目前，已确认受影响的资产为`web-app-01`，需要重点关注。

## 9. 证据链摘要与观测缺口
证据链包括针对登录端点的可疑利用请求、解析罕见域名`cdn-auth-check.net`、匹配到web-shell信标的种子告警，以及后续的TLS连接和PowerShell启动器的执行。这些证据相互支撑，共同指向了恶意行为的存在。然而，这些事件仅表明了恶意软件的执行阶段，但无法单独确定其对整个网络的影响。此外，仍需进一步调查以确认是否存在更大的影响范围。

## 10. 结论与后续建议
- **立即处置**：优先隔离或重点监控资产：web-app-01；在边界和代理设备上排查并封禁外部基础设施：198.51.100.88、cdn-auth-check.net。
- **短期核查**：以 seed 指标和事件簇中的域名 / IP 为 pivot，继续检索同时间窗内的重复通信。
- **持续复核**：仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。

## 11. 技术附录提示
技术附录包含 IOC、对象清单、证据细目和观测映射。

---

# 事件调查技术附录

以下内容主要供分析、复盘和技术排查使用，不直接替代主报告结论。

## 1. IOC / IOA 清单

| 类型 | 值 | 角色 | 状态 |
| -- | -- | -- | -- |
| IP | 198.51.100.88 | 核心外部基础设施 | 已确认 |
| 域名 | cdn-auth-check.net | 核心外部基础设施 | 已确认 |
| 指纹 | t13d1517h2_91adbe210bc1_7cae1281e991 | 扩展查询候选 | 待确认 |
| 内网 IP | 10.60.1.14 | 关联内部地址 | 已确认 |

## 2. 关键对象清单

| 对象 | 类型 | 当前角色 | 是否已纳入证据链 | 备注 |
| -- | -- | -- | -- | -- |
| web-app-01 | 资产 | 种子资产 | 是 | 种子告警首先落在该资产上。；当前已纳入主证据链的资产。；事件中出现过的资产对象。；当前 pivot 里出现的扩线候选资产。 |
| 198.51.100.88 | IP | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选地址。 |
| cdn-auth-check.net | 域名 | 核心外部基础设施 | 是 | 当前主证据链中反复出现的关键对象。；当前 pivot 里出现的扩线候选域名。 |
| t13d1517h2_91adbe210bc1_7cae1281e991 | JA4 | 种子命中指纹 | 是 | 种子告警命中的关键通信指纹。 |
| Possible web-shell beacon | 家族提示 | 家族/工具提示 | 是 | 辅助解释本案的家族或工具背景，不直接等于强归因。 |
| 10.60.1.14 | 内网 IP | 关联内部地址 | 是 | 事件链中出现的内部地址。；当前 pivot 里出现的扩线候选地址。 |
| t13d1517h2_91adbe210bc1_7cae1281e991 | 指纹 | 扩展查询候选 | 否 | 当前 pivot 里出现的扩线候选指纹。 |

## 3. 关键证据细目

### 证据 E-01
- **证据类型**：主支撑证据
- **证据类别**：调查触发证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-05-11 14:12:00 UTC，资产 `web-app-01` 围绕 198.51.100.88 / cdn-auth-check.net 的异常通信触发了种子告警。
- **为什么重要**：这是种子命中的直接落点，说明调查起点并不是孤立的指纹命中。
- **边界与限制**：单条种子命中本身不能独立证明事件成立，仍需要结合重复通信、资产背景或主机侧线索共同定性。
- **主要依据来源**：种子事件窗观测、关联事件扩查、资产背景核查、显式反证检查
- **观测引用**：`obs-001, obs-002, obs-003, obs-004`

### 证据 E-02
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-05-11 14:07:30 UTC，资产 `web-app-01` 先解析了少见域名 `cdn-auth-check.net`，为后续与同一基础设施的通信提供了准备。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查、资产背景核查、显式反证检查
- **观测引用**：`obs-001, obs-002, obs-003, obs-004`

### 证据 E-03
- **证据类型**：主支撑证据
- **证据类别**：连续性支撑证据（网络）
- **证据强度**：高
- **事实描述**：在 2026-05-11 14:18:10 UTC，资产 `web-app-01` 再次与 198.51.100.88 / cdn-auth-check.net 通信，说明同一基础设施上的异常仍在持续。
- **为什么重要**：同一基础设施在种子资产或关联资产上再次出现，使单点告警扩展成可复核的连续事件链。
- **边界与限制**：仅凭网络侧重复通信仍可能在少数情况下被正常组件、同步组件或运维脚本解释，因此仍需要结合资产背景或主机侧结果。
- **主要依据来源**：种子事件窗观测、关联事件扩查、资产背景核查、显式反证检查
- **观测引用**：`obs-001, obs-002, obs-003, obs-004`

### 证据 E-04
- **证据类型**：主支撑证据
- **证据类别**：风险升级证据（主机）
- **证据强度**：高
- **事实描述**：在 2026-05-11 14:23:00 UTC，资产 `web-app-01` 出现了高风险主机侧动作。
- **为什么重要**：它把异常从通信层推进到主机执行或其他更高风险阶段，对事件定性的权重明显更高。
- **边界与限制**：当前仍缺少更细的命令行、落地文件、持久化或内存证据，因此可以确认存在执行线索，但还不能完整描述具体载荷。
- **主要依据来源**：种子事件窗观测、关联事件扩查、资产背景核查、显式反证检查
- **观测引用**：`obs-001, obs-002, obs-003, obs-004`

## 4. 观测引用对照

| 观测编号 | 来源工具 | 事实摘要 | 关系 |
| -- | -- | -- | -- |
| `obs-001` | `search_seed_context` | 检索到 5 条偏可疑事件，围绕 cdn-auth-check.net、198.51.100.88 展开。 | `supporting` |
| `obs-002` | `search_related_events` | 扩线检索到 5 条可疑关联事件，围绕 cdn-auth-check.net、198.51.100.88 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-003` | `expand_asset_scope` | 扩线检索到 5 条可疑关联事件，围绕 cdn-auth-check.net、198.51.100.88 展开；当前先作为待验证候选线索。 | `candidate` |
| `obs-004` | `check_counterevidence` | 显式反证检查覆盖了 5 条同资产上下文，未发现足以降级当前判断的明确背景解释。 | `context` |
| `obs-005` | `extract_claim_candidates_from_page` | 从内部调查摘要中整理出 8 条 claim，用于结构化报告而不是新增外部证据。 | `context` |

## 5. 正文章节引用映射

| 正文章节 | 主要 evidence / observation | 用途 |
| -- | -- | -- |
| 首页摘要 | E-01、E-02、expand_cluster_scope | 当前状态 / 当前最强证据 / 当前最关键缺口 |
| 第 5 节 | E-01、E-02、E-03、E-04 | 主支撑证据 / 反证与替代解释 |
| 第 9 节 | expand_cluster_scope | 证据链摘要 / 缺口说明 |
| 第 10 节 | E-01、E-02 | 结论 / 动作 / 复核重点 |

## 6. 证据来源与参考资料

| 来源类型 | 来源名称 | 用途 | 边界 |
| -- | -- | -- | -- |
| 上游检测 | t13d1517h2_91adbe210bc1_7cae1281e991 / Possible web-shell beacon | 提供本案的调查起点与初始命中线索。 | 命中本身并不自动等于事件成立，仍需要后续本地事实补强。 |
| 内部观测 | trace_store.seed_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 内部观测 | trace_store.related_events | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 内部观测 | trace_store.asset_context | 支撑时间线、范围和关键事实。 | 仅覆盖当前调查纳入的本地流量与上下文数据。 |
| 背景核查 | trace_store.asset_context | 验证维护窗口、补丁、备份或共享基线等替代解释。 | 反证核查只能说明是否存在降级解释，不能替代主证据链。 |
| 内部整理 | internal_digest | 把零散观测沉淀为可引用的事实、实体与报告素材。 | 内部整理不会新增外部事实，只负责结构化已有调查结果。 |

## 7. 证据缺口与待补查询

| 缺口 | 限制的结论 | 当前是否可补 | 最值得补的查询 |
| -- | -- | -- | -- |
| 仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。 | 当前仍缺少对“仍需围绕当前 pivot 做一次显式扩线，确认是否存在同指标的更大范围复现。”的直接补证。 | 否 | 该缺口已多轮核查但仍未获得新的有效信息，更适合作为报告未决事项。 |
