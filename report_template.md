# Incident-Agent 事件级溯源报告模板（V2）

## 说明

这份文档不再把报告定位成“单条告警分析卡片”，而是定位成一份真正面向事件调查的溯源报告模板。它更适合我们现在的 `incident-agent` 主线，也更贴近安全分析员真正关心的问题：

1. 这起事件到底发生了什么。
2. 事件是如何从种子告警扩展成一个完整故事的。
3. 目前已经确认了哪些资产、哪些外部基础设施、哪些攻击阶段。
4. 哪些证据支撑当前结论，哪些背景证据在提醒我们不要过度定性。
5. 现在应该优先做什么，后面还缺什么。

这版模板的核心原则如下：

1. **事件优先，不是字段优先**：报告先服务于“讲清楚事件”，再考虑字段填充完整度。
2. **事实与结论分离**：观测事实、研判判断、反证/替代解释分别描述，不把推断写成事实。
3. **证据要写厚，不必压缩成一句话**：关键证据允许写成一小段，把“看到了什么、为什么重要、局限在哪里”讲明白。
4. **边界和不确定性必须显式写出**：报告不只说明“为什么升级”，也要说明“为什么还没确认更多”。
5. **结构优先，便于 agent 生成**：主干结构固定，可选附录按证据可得性决定是否输出，不强迫模型编造缺失字段。

当前建议的实际产物不是单个文件，而是两层交付：

- `report.md`
  - 面向阅读者的主报告，只保留结论、事件经过、关键判断依据、范围边界和动作建议
- `report_appendix.md`
  - 面向分析员的技术附录，承载技术时间线、IOC / IOA、观测对照和更细的证据细目

## 与旧版模板的差异

这版模板主动删掉或下沉了以下容易失真、长期写空、或暂时不适合自动化固定生成的栏目：

- 责任人、完成时限、通知状态这类工单字段
- 每次都必须出现的精细网络统计项，例如总字节数、上下行字节数、持续时长
- 每次都必须出现的主机取证细项，例如注册表、计划任务、父子进程树
- 每次都必须出现的查询语句

这些内容不是不重要，而是改为“有则写、无则省略”，放进附录或扩展块，而不是作为主报告的刚性骨架。

## 生成约束

如果后续让 agent 自动生成报告，建议遵守下面的约束：

1. 报告只能基于 `incident.json` 中已经存在的事实，不允许在成文阶段引入新的 IOC、新结论或新阶段。
2. 每条关键结论都应能映射到 `observation_id` 或证据编号。
3. 关键证据至少要回答三个问题：
   - 观测到了什么事实？
   - 这条事实为什么支持或反驳当前假设？
   - 它的局限性是什么？
4. 不可获得的信息直接省略，不得为了“报告好看”而补写。
5. 主报告与技术附录分离：面向阅读者的叙事放在 `report.md`，原始摘要、工具来源、观测对照和细节清单下沉到 `report_appendix.md`。

---

# 第一部分：推荐模板

## 1. 执行摘要

本节只回答“结论是什么、为什么值得关注、现在最该做什么”。

- **事件结论**：
- **当前状态**：`confirmed` / `needs_review` / `monitor_only`
- **严重度**：
- **置信度**：
- **一句话摘要**：
- **建议立即执行的动作**：

建议写法：

- 不重复展开全部细节，只保留最关键的事件闭环和处置动作。
- 如果是降级或观察场景，要明确写出“为什么没有升级为确认事件”。

---

## 2. 事件范围与边界

本节回答“这起事件目前覆盖到哪里了，还没有覆盖到哪里”。

- **Seed alert / 起点**：
- **时间窗口**：
- **种子资产**：
- **疑似受影响资产**：
- **核心外部指示物**：
- **当前攻击阶段**：
- **当前事件边界**：
- **尚未确认的范围**：

建议写法：

- 不只列资产和 IOC，还要写清“事件簇是否只收敛在单资产、是否存在横向扩散、是否已经覆盖入侵入口与后续执行”。
- 如果存在背景资产或共享基础设施，也要在这里提前声明，避免后文误读。

---

## 3. 核心时间线

本节用最少但最关键的节点把事件串起来。推荐字段如下：

| 时间 | 事件 | 作用 | 观测引用 |
| -- | -- | -- | -- |
|  |  | `种子告警` / `主证据` / `反证` / `执行线索` / `处置动作` | `obs-xxx` |

建议写法：

- 时间线不是原始日志堆叠，只保留对结论有推进作用的事件。
- 每个节点都应该能说明它在推进哪一步判断。

---

## 4. 核心证据链

本节是整份报告的重点。不要把证据压缩成标签或一句话，要把“事实、含义、局限”写清楚。

### 4.1 主支撑证据

每条关键证据建议使用以下结构：

#### 证据 E-01

- **证据类别**：网络 / 主机 / 情报 / 资产背景 / 历史扩线
- **证据强度**：高 / 中 / 低
- **事实描述**：
- **为什么支持当前主假设**：
- **局限性与边界**：
- **观测引用**：

建议写法：

- 一条关键证据可以写成 3 到 5 句，而不是只写一个短语。
- 如果几条观测共同支撑同一个判断，可以合并成一条较厚的证据块。

### 4.2 反证与替代解释

本节必须保留，即使结论是确认事件也一样。

#### 证据 C-01

- **反证或替代解释**：
- **相关事实**：
- **为什么它不足以推翻主结论，或为什么它足以让我们降级**：
- **观测引用**：

建议写法：

- 这里不是为了“唱反调”，而是为了说明模型为什么没有过度自信。
- `needs_review` 和 `monitor_only` 场景下，这一节通常比主支撑证据更关键。

### 4.3 背景情报与家族上下文（可选）

只有在确实能帮助理解事件时才写，且必须与事件绑定证据分开。

- **相关家族/工具提示**：
- **背景情报摘要**：
- **与本事件的关联方式**：
- **不能据此直接推出的结论**：
- **观测引用 / 来源引用**：

建议写法：

- 背景情报用于“帮助解释”，不是用于“替代事件证据”。
- 如果只有 seed rule 名称而没有更多绑定证据，不要把它写成强归因结论。

---

## 5. 综合研判

本节回答“为什么最后得出这个状态，而不是别的状态”。

- **主假设**：
- **备选解释**：
- **为什么当前结论成立**：
- **为什么没有升级或为什么没有降级**：
- **仍未解决的问题**：
- **下一次复核时最值得补的证据**：

建议写法：

- `confirmed` 场景下，要解释“闭环是怎么形成的”。
- `needs_review` 场景下，要解释“为什么可疑但证据仍不够”。
- `monitor_only` 场景下，要解释“为什么更像背景流量或计划内活动”。

---

## 6. 处置建议

本节只保留真正可执行的动作，不需要为了好看而写满。

### 6.1 立即动作（0 到 2 小时）

- [HIGH]
- [HIGH]

### 6.2 短期排查（24 小时内）

- [HIGH]
- [MEDIUM]

### 6.3 后续改进（可选）

- [MEDIUM]
- [LOW]

建议写法：

- 每条动作都尽量与本案的证据链或不确定性直接对应。
- 如果是降级场景，动作应偏向“确认背景、保留样本、监控复发”，而不是一律隔离封禁。

---

## 7. 技术附录（单独文件）

技术附录建议单独输出为 `report_appendix.md`，不要求每次都全部写满。

### 7.1 IOC / IOA 清单

| 类型 | 值 | 与本案关系 |
| -- | -- | -- |
| IP |  |  |
| 域名 |  |  |
| URL |  |  |
| JA3 / JA4 / 指纹 |  |  |
| Hash / 文件 / 进程 |  |  |

### 7.2 观测引用对照

| 观测编号 | 来源工具 | 摘要 |
| -- | -- | -- |
| `obs-001` |  |  |
| `obs-002` |  |  |

### 7.3 扩展上下文（可选）

只有在确实拿到了这些信息时才展开：

- 更细的五元组或流量统计
- 主机侧详细取证结果
- 检索语句或查询轨迹
- 资产画像、变更窗口、维护计划

---

# 第二部分：示例报告

下面给出一份更贴近 `incident-agent` 输出目标的示例。这个示例不是追求“字段特别多”，而是追求三件事：

1. 事件闭环是否讲清楚了。
2. 关键证据是否写得足够明白。
3. 反证、边界和未决问题是否被正面写出来了。

---

# 事件级溯源报告

## 1. 执行摘要

- **事件结论**：确认事件
- **当前状态**：`confirmed`
- **严重度**：高危
- **置信度**：94 / 100
- **一句话摘要**：公网 Web 服务器 `web-app-01` 在可疑 exploit 请求后解析稀有域名 `cdn-auth-check.net`，随后与 `198.51.100.88` 形成重复 TLS 外联，并在外联开始后出现 `w3wp -> powershell` 执行线索，当前已经形成“初始利用 -> beacon -> 执行”的事件闭环。
- **建议立即执行的动作**：隔离 `web-app-01`，封禁 `198.51.100.88` 与 `cdn-auth-check.net`，并以相同外部基础设施为 pivot 继续扩线。

## 2. 事件范围与边界

- **Seed alert / 起点**：2026-05-11 14:12:00 UTC，`web-app-01` 命中 “Possible web-shell beacon” JA4 规则。
- **时间窗口**：2026-05-11 14:02:00 UTC 至 2026-05-11 14:23:00 UTC。
- **种子资产**：`web-app-01`（10.60.1.14）。
- **疑似受影响资产**：当前确认收敛在 `web-app-01`，暂未发现第二台资产命中同一批基础设施。
- **核心外部指示物**：`198.51.100.88`、`cdn-auth-check.net`、`JA4 t13d1517h2_91adbe210bc1_7cae1281e991`。
- **当前攻击阶段**：`initial-access`、`command-and-control`、`execution`。
- **当前事件边界**：现有证据已经足够把本案从“单条告警”提升为“单资产已成立事件”，并且闭环主要落在 Web 服务器利用后的外联与执行阶段。
- **尚未确认的范围**：还没有证据证明存在横向移动、批量受影响资产或明确的数据外传行为，入侵入口虽然高度怀疑与 exploit 请求相关，但仍缺少更细的成功利用细节。

## 3. 核心时间线

| 时间 | 事件 | 作用 | 观测引用 |
| -- | -- | -- | -- |
| 2026-05-11 14:02:00 UTC | 面向登录接口的可疑 exploit 请求出现在公网 Web 服务器上 | `主证据` | `obs-001` |
| 2026-05-11 14:07:30 UTC | `web-app-01` 解析稀有域名 `cdn-auth-check.net`，返回 `198.51.100.88` | `主证据` | `obs-001` |
| 2026-05-11 14:12:00 UTC | 种子告警命中相同外部基础设施与可疑 JA4 指纹 | `种子告警` | `obs-001` |
| 2026-05-11 14:18:10 UTC | 同一资产再次连向相同域名和外部 IP | `主证据` | `obs-001` |
| 2026-05-11 14:23:00 UTC | `w3wp` 上下文中出现 PowerShell 启动行为 | `执行线索` | `obs-002` |

## 4. 核心证据链

### 4.1 主支撑证据

#### 证据 E-01

- **证据类别**：网络 + 初始访问上下文
- **证据强度**：高
- **事实描述**：种子告警出现前，`web-app-01` 先收到针对登录接口的可疑 exploit 请求，随后解析此前未见业务基线支撑的稀有域名 `cdn-auth-check.net`，并将其解析到 `198.51.100.88`。这说明外部基础设施并不是只在告警瞬间偶发出现，而是在告警前就已经与潜在利用链条发生关联。
- **为什么支持当前主假设**：如果只有一次 JA4 命中，我们仍然可能把它看作单点噪声；但 exploit 请求与后续 DNS 准备动作把告警前后的上下文串了起来，说明该外部基础设施更像是利用后的后续通信目标，而不是普通第三方服务。
- **局限性与边界**：仅凭 exploit 请求本身还不能证明漏洞一定成功利用，DNS 解析也不能单独证明恶意；它们的价值在于与后续重复外联和执行线索结合后，显著抬高了事件成立概率。
- **观测引用**：`obs-001`

#### 证据 E-02

- **证据类别**：网络行为
- **证据强度**：高
- **事实描述**：2026-05-11 14:12:00 UTC 的种子告警命中 “Possible web-shell beacon” 规则后，`web-app-01` 在 14:18:10 UTC 再次连向同一域名和同一外部 IP，且通信围绕同一批指示物持续复现。对事件而言，这已经不是“某个指纹偶然命中”，而是“同一资产围绕同一外部基础设施形成连续会话”。
- **为什么支持当前主假设**：重复外联把单条告警扩展成事件链，是本案从“可疑告警”走向“确认事件”的核心一步。只要通信对象、时间窗口和资产主体都高度一致，这种复现就比单条 seed alert 有更强的定性能力。
- **局限性与边界**：网络侧重复通信仍可能在极少数情况下被管理组件、同步组件或定制运维脚本解释，因此它仍需要资产背景或主机线索配合，不能独自承担全部结论。
- **观测引用**：`obs-001`

#### 证据 E-03

- **证据类别**：主机执行线索
- **证据强度**：高
- **事实描述**：在对外通信已经开始之后，`web-app-01` 的 `w3wp` 上下文中出现 PowerShell launcher 执行行为。这条线索把“网络上的可疑外联”进一步落到“主机上的可疑执行”，说明事件已经从网络侧弱信号延伸到主机侧异常。
- **为什么支持当前主假设**：`w3wp -> powershell` 这一模式对公网 Web 服务器来说具有较强的攻击后行为指向性，它为“利用后植入或远程执行”提供了关键补强，使本案不再只是 beacon 猜测，而是具备执行阶段证据。
- **局限性与边界**：当前还没有更细的命令行、落地文件或持久化结果，因此我们能够确认“存在执行线索”，但还不能完整描述后续载荷或控制方式。
- **观测引用**：`obs-002`

### 4.2 反证与替代解释

#### 证据 C-01

- **反证或替代解释**：一种可能的替代解释是，这些通信来自某个正常 Web 插件、监控组件或第三方回调服务，而不是攻击活动。
- **相关事实**：目前没有在同时间窗中观察到已知变更窗口、批准的第三方业务回调、同类资产共享访问基线或可信供应商上下文；同时，外部基础设施在种子告警前后与 exploit 请求、稀有 DNS 解析、重复外联、`w3wp -> powershell` 执行线索连续相连。
- **为什么它不足以推翻主结论，或为什么它足以让我们降级**：如果只有一次出站连接，这个替代解释仍值得重点保留；但在当前证据组合下，它已经很难解释“利用请求、DNS 准备、重复 beacon、执行线索”同时出现的模式。因此本案可以确认事件成立，但仍需在后续排查中继续验证是否存在更深的主机侧落地与横向扩散。
- **观测引用**：`obs-001`、`obs-002`

### 4.3 背景情报与家族上下文（可选）

- **相关家族/工具提示**：种子规则给出的提示为 “Possible web-shell beacon”。
- **背景情报摘要**：这类提示通常用于描述“公网应用被利用后，通过伪装 HTTPS 或稀有域名进行后续控制通信”的模式。它和本案看到的“可疑 exploit 请求 + 稀有域名解析 + 重复外联 + Web 进程衍生执行”在形态上是匹配的。
- **与本事件的关联方式**：这里的价值在于帮助我们理解攻击链形态，而不是直接把事件归因为某个家族或某个现成样本。
- **不能据此直接推出的结论**：仅凭规则名称，仍不能直接推出具体 web shell 名称、攻击团伙或确定的后门家族。
- **观测引用 / 来源引用**：`obs-001`

## 5. 综合研判

- **主假设**：`web-app-01` 在遭受针对公网应用的可疑利用后，与外部基础设施 `198.51.100.88` / `cdn-auth-check.net` 建立了重复 beacon 式通信，并进一步出现 Web 进程上下文中的可疑执行，因此当前应按已成立的单资产入侵事件处理。
- **备选解释**：某个正常组件或第三方回调在相同时间窗内产生了看似异常的流量与执行行为。
- **为什么当前结论成立**：本案并不是依赖单条指纹命中，而是依赖一个逐步收敛的证据链条。利用请求解释了为什么这台公网服务器进入了高风险状态；稀有 DNS 解析和重复外联把单点告警扩展成了连续事件链；`w3wp -> powershell` 执行线索进一步把网络侧可疑性落到主机侧异常。三者共同作用，使“确认事件”明显优于“继续观察”。
- **为什么没有升级或为什么没有降级**：当前已经足够升级为确认事件，但还没有证据支持把事件范围扩大到多资产传播，也没有证据支持把其上升为明确的数据泄露事件。因此本案适合定性为“单资产已成立事件”，而不是更大的已确认扩散事件。
- **仍未解决的问题**：利用是否成功落地了具体文件或持久化项；是否存在 Web 日志外的二次载荷；同时间窗是否还有其他资产访问同一基础设施。
- **下一次复核时最值得补的证据**：Web 进程完整命令行、落地文件、内存取证结果，以及基于相同域名/IP/JA4 的横向扩线结果。

## 6. 处置建议

### 6.1 立即动作（0 到 2 小时）

- [HIGH] 立即对 `web-app-01` 执行网络隔离或最小化暴露处置，并保留日志、内存和磁盘取证条件。
- [HIGH] 在边界、代理和 DNS 控制面阻断 `198.51.100.88` 与 `cdn-auth-check.net` 的后续访问。
- [HIGH] 以相同外部基础设施和相同 JA4 指标为 pivot，快速检索过去 7 天内是否还有其他资产形成相同通信模式。

### 6.2 短期排查（24 小时内）

- [HIGH] 检查 `web-app-01` 上 `w3wp` 相关子进程、命令行、脚本落地和计划任务，确认是否存在植入、内存马或进一步下载器。
- [MEDIUM] 回溯登录接口、WAF、反向代理和应用日志，确认 exploit 请求是否对应已知漏洞路径，以及是否存在成功执行证据。
- [MEDIUM] 核查同类 Web 服务器是否共享相同组件、相同暴露面或相同外联模式，防止遗漏平行受害资产。

### 6.3 后续改进（可选）

- [MEDIUM] 把本案沉淀为“公网应用利用后 beacon”场景的标准事件剧本，补充 DNS 准备动作与 Web 进程衍生执行的关联规则。
- [LOW] 将 `198.51.100.88`、`cdn-auth-check.net`、相关 JA4 以及 `w3wp -> powershell` 这一模式纳入持续检测基线。

## 7. 附录

### 7.1 IOC / IOA 清单

| 类型 | 值 | 与本案关系 |
| -- | -- | -- |
| IP | `198.51.100.88` | 事件中的核心外部基础设施 |
| 域名 | `cdn-auth-check.net` | 种子告警前后持续出现的稀有域名 |
| JA4 | `t13d1517h2_91adbe210bc1_7cae1281e991` | 种子告警命中的指纹 |
| 进程链 | `w3wp -> powershell` | 执行阶段的重要主机线索 |

### 7.2 观测引用对照

| 观测编号 | 来源工具 | 摘要 |
| -- | -- | -- |
| `obs-001` | `search_related_events` | 聚合了 exploit 请求、DNS 解析、种子告警和重复外联，建立了网络侧事件主链 |
| `obs-002` | `expand_asset_scope` | 提供了 `w3wp -> powershell` 执行线索并帮助界定当前事件边界 |

### 7.3 扩展上下文（可选）

- 当前没有证据证明已经发生横向移动或数据外传，因此未单独展开“扩散”或“泄露”章节。
- 当前没有足够的主机取证信息支撑更细的载荷归因，因此未输出样本家族级强归因。

---

## 使用建议

如果后面我们要把 renderer 对齐到这份模板，我建议优先做到下面几点：

1. 固定输出主干七段，不要再回到“大而全字段表单”。
2. 关键证据默认写成小段落，而不是短 bullet。
3. 每份报告都保留“反证与替代解释”。
4. `confirmed`、`needs_review`、`monitor_only` 三种状态分别走不同的综合研判语气。
5. 附录只在数据可得时补充，避免空栏目。
