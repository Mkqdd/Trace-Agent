# Incident-Agent 调查型报告模板（V5）

## 模板定位与交付目标

这份模板不再把 `report.md` 当成“单条告警结果卡片”，而是把它定位成一份真正可交付的调查报告。

它主要面向三类读者：

- 运维人员：要在最短时间内知道事件是否成立、影响到哪、现在先做什么。
- 安全运营/分析人员：要知道证据链是否闭合、边界在哪里、哪些对象已经确认、还缺什么。
- 协同负责人：要知道为什么当前状态是 `confirmed_incident`、`needs_review` 或 `monitor_only`，以及下一步协同重点。

建议固定输出三层产物：

- `report.md`
  - 面向运维与安全运营协同的主报告，强调结论、范围、证据链和动作。
- `report_appendix.md`
  - 面向分析员的技术附录，承载 IOC/IOA、关键对象、观测对照、引用来源、证据缺口和可选技术细节。
- `report_outline.json`
  - 面向 renderer / agent 的结构化中间态，约束“每一章写什么、引用哪些证据、哪些章节可省略”。

一个合格的主报告，应该让读者在 30 秒内回答以下问题：

1. 当前判断是什么。
2. 为什么目前能做出这个判断。
3. 已确认影响到哪里，哪里还只是候选。
4. 当前最关键缺口是什么。
5. 现在最值得先做什么动作。

## 设计原则

### 2.1 先问题，后答案

参考 `reference/report_reference` 中的报告，这类交付件的核心不是“字段是否填满”，而是回答下面这些问题：

1. 这起事件为什么值得调查。
2. 本次调查准备证明什么，排除什么。
3. 哪些对象是核心对象，哪些只是候选对象。
4. 哪些事实支撑当前判断，哪些事实在限制当前判断。
5. 当前影响已经确认到哪一步，还缺哪一段证据。

### 2.2 事实、解释、边界分离

每条关键证据至少要分成三层：

- 事实：看到了什么。
- 解释：它为什么重要。
- 边界：它不能单独证明什么。

### 2.3 对象覆盖策略必须显式交付

报告不只列出对象，还要说明：

- 哪些对象已经纳入证据链。
- 哪些对象是当前核心观测对象。
- 哪些对象只是横向对比对象。
- 哪些对象仍是扩展查询候选。

否则 reader 很难理解为什么 agent 只调查了这些对象，而不是别的对象。

### 2.4 结论要前置，但不能跳过证据

主报告首页应尽快告诉读者当前状态、影响范围和立即动作，但后文仍必须把关键证据、反证、边界和缺口补齐。

换句话说：

- 结论应该让人先看见。
- 证据应该让人能复核。

### 2.5 正文、附录、结构化中间态必须分层

主报告只保留能驱动判断和动作的信息。

下列内容默认不直接出现在 `report.md`：

- `observation_id`
- tool 名、查询轨迹、内部 prompt / workflow
- readiness / delivery gate / 内部评分逻辑
- 大段原始日志、长 JSON、长网页正文
- 过细的 JA3 / JA4 / DNS answers / 原始包字段

这些内容应下沉到 `report_appendix.md`、`incident.json` 或 `investigation_trace.json`。

### 2.6 证据链摘要必须在结论前显式交付

参考报告里最有价值的一点，不是“参考资料很多”，而是会在结论前明确告诉读者：

- 当前判断主要建立在什么证据组合上。
- 哪类证据最强。
- 哪类证据缺失。
- 结论最多只能走到哪一层。

这部分不应只藏在附录里。

### 2.7 观测缺口要作为正文对象，而不是附带一句话

报告不能只说“还有不确定性”，而应明确交付：

- 当前缺什么证据。
- 这个缺口会限制哪一段结论。
- 下一步如果补查，最值得补什么。

### 2.8 如果正文带图，必须给出图示要点

任何图、拓扑、时间线、关系图，如果进入交付件，都必须补一条“图示要点”：

- 这张图最想让读者记住什么。
- 它能支撑什么。
- 它不能直接推出什么。

### 2.9 语言必须面向运维协同，而不是面向开发调试

正文建议优先使用以下表达：

- `已确认`
- `疑似`
- `未确认`
- `当前未观察到`
- `当前未获取`
- `当前证据不足以支持`

正文应避免以下表达：

- “模型觉得”
- “agent 决定”
- “工具 X 选择了 Y”
- “由于 workflow / fallback / prompt 设计”

### 2.10 缺信息时宁可留白，不要补写

没有的数据直接省略或显式写成“当前未获取”，不要为了报告完整度虚构。

### 2.11 来源说明以“章内简标 + 附录全表”为主

主报告每章保留“本节主要依据来源”即可，不强制单独做一章“参考资料”。

但如果本案高度依赖公开资料，允许在正文末尾增加一段简短“来源说明”，前提是：

- 它只说明主要来源类型和作用。
- 不把公开背景写成本案直接证据。

## 主报告结构与章级输出条件

建议主报告由“首页摘要 + 11 个章节”组成。

首页摘要必须存在；章节中允许存在条件输出，但不能用硬写来填充空白。

### 首页摘要（必选）

首页摘要负责回答：

- 当前状态是什么。
- 当前影响范围到哪里。
- 当前最强证据是什么。
- 当前最关键缺口是什么。
- 现在最值得先做什么。

### 章节建议

1. 事件背景与已知线索
2. 范围界定与调查假设
3. 对象覆盖策略与关键实体
4. 事件机制分解
5. 关键证据与异常事实
6. 时序特征与行为模式
7. 传播与关联分析
8. 影响分析
9. 证据链摘要与观测缺口
10. 结论与后续建议
11. 技术附录提示

### 章级输出条件

默认建议如下：

- 必选：首页摘要、1、2、5、8、9、10、11
- 强烈建议：3
- 条件输出：4、6、7

条件输出规则：

- 第 4 节只有在“起点 -> 主链 -> 放大器 -> 未确认环节”能形成清晰机制时再独立成章。
- 第 6 节只有在时序证据足以支撑“模式判断”时再独立成章。
- 第 7 节只有在确实做过扩线、pivot 关联或拓扑关联时再独立成章。

如果某节没有可靠输入：

- 可以并入相邻章节。
- 或者省略该节。
- 但不能用空泛话术硬写。

## 主报告模板

### 首页摘要

主报告首页建议先给一个紧凑摘要块，而不是直接进入长篇背景。

建议字段：

- **事件标题**：
- **分析窗口**：
- **当前状态**：`confirmed_incident` / `needs_review` / `monitor_only`
- **严重度**：
- **研判把握**：
- **已确认影响范围**：
- **当前最强证据**：
- **当前最关键缺口**：
- **建议立即动作**：
- **一句话结论**：

建议写法：

- 这一块应尽量控制在一屏内。
- 重点不是堆字段，而是让运维先形成判断。
- “当前最强证据”和“当前最关键缺口”必须同时出现，避免只有结论没有边界。

标题命名建议：

- 优先采用“资产/范围 + 行为/异常 + 当前判断”。
- 避免只写成“某告警分析报告”。

---

# 事件调查报告

技术细节、IOC 与观测对照请见《事件调查技术附录》。

## 1. 事件背景与已知线索

本节回答“为什么会有这份报告”。

建议字段：

- **事件标题**：
- **分析窗口**：
- **Seed alert / 调查起点**：
- **初始命中线索（规则 / 指纹 / 模型）**：
- **上游检测或已知背景**：
- **本次调查关注点**：
- **本节主要依据来源**：

建议写法：

- 交代种子告警是什么，为什么值得继续调查。
- 如果 seed alert 来自规则、指纹或模型命中，建议明确写出命中类型、命中对象，以及它在本案里是“调查起点”还是“直接支撑证据”。
- 如果存在公开背景、内部背景、上游模型/指纹命中理由，可以写，但必须注明这是“已知线索”，不是最终结论。
- 如果没有外部背景，不要硬补。

---

## 2. 范围界定与调查假设

本节回答“这轮调查准备证明什么，排除什么”。

建议字段：

- **本次调查范围**：
- **主假设**：
- **备选解释**：
- **本轮不覆盖的范围**：
- **主假设成立所需条件**：
- **主假设失效条件**：
- **本节主要依据来源**：

建议写法：

- 主假设不要写成口号，要写成可验证命题。
- 备选解释至少保留一个，例如：
  - 共享基础设施
  - 计划内变更 / 补丁窗口
  - 第三方正常组件
  - 单点噪声 / 弱命中
- 这一节决定后面 agent 应该补什么证据。

---

## 3. 对象覆盖策略与关键实体

本节回答“为什么调查这些对象，而不是无限扩线”。

建议字段：

- **已纳入证据链的对象**：
- **当前核心观测对象**：
- **横向对比对象**：
- **扩展查询候选**：
- **种子资产**：
- **已确认受影响资产**：
- **待确认关联资产**：
- **核心外部基础设施**：
- **关键指示物**：
- **对象覆盖边界**：
- **本节主要依据来源**：

建议写法：

- 不只是列值，更要写角色。
- 明确区分：
  - 当前主证据落点
  - 候选关联对象
  - 对照对象
  - 还没进证据链的扩展对象
- 这一节要让 reader 明白“为什么本轮调查到这里先停”。

---

## 4. 事件机制分解

本节回答“这起事件是如何发生、如何放大、如何形成当前影响的”。

建议字段：

- **触发因素 / 起点**：
- **事件主链**：
- **影响放大器 / 传播机制**：
- **当前不能确认的机制环节**：
- **本节主要依据来源**：

建议写法：

- 对安全事件来说，这一节很重要，因为它强迫我们把“触发线索”“主事件链”“放大因素”拆开。
- 例如：
  - 种子告警命中只是起点
  - 重复外联构成主链
  - 共享基础设施导致的扩线误差是放大器
  - 主机侧执行是否成立仍是未确认机制环节
- 不要把所有现象混成一句“像攻击”。

---

## 5. 关键证据与异常事实

这是主报告最重要的一节。

### 5.1 主支撑证据

每条关键证据建议使用统一结构：

#### 证据 E-01

- **证据类别**：网络 / 主机 / 上游检测 / 情报 / 资产背景 / 历史扩线
- **证据强度**：高 / 中 / 低
- **事实描述**：
- **为什么支持当前主假设**：
- **局限性与边界**：
- **主要依据来源**：

建议写法：

- 一条证据可以写成 3 到 5 句，不要压缩成短语。
- 如果若干观测共同支撑一个判断，应合并成一个“厚证据块”。
- 如果证据只到 `context` 或 `candidate` 层，不应把它写成强支撑证据。

### 5.2 反证与替代解释

#### 证据 C-01

- **反证或替代解释**：
- **相关事实**：
- **为什么它不足以推翻主结论，或为什么它足以让我们降级**：
- **主要依据来源**：

建议写法：

- `confirmed_incident` 也必须保留这一节。
- `needs_review` 和 `monitor_only` 场景下，这一节通常比主支撑证据更关键。

### 5.3 辅助背景与解释（可选）

- **相关背景**：
- **它如何帮助理解本案**：
- **不能据此直接推出的结论**：
- **主要依据来源**：

建议写法：

- 背景情报只能“帮助解释”，不能替代事件证据。
- 如果只有规则名称或弱情报，不要写成家族级强归因。

---

## 6. 时序特征与行为模式

本节回答“事件是如何展开的，它呈现出什么模式”。

建议保留两部分：

### 6.1 核心时间线

| 时间 | 事件 | 作用 |
| -- | -- | -- |
|  |  | `种子告警` / `主证据` / `反证` / `执行线索` / `处置动作` |

### 6.2 模式总结

建议字段：

- **告警前准备动作**：
- **告警后复现或持续性**：
- **是否看到执行 / 横向 / 外传模式**：
- **是否更像维护窗口 / 正常批处理 / 共享基础设施**：
- **如本节带图，图示要点**：
- **本节主要依据来源**：

建议写法：

- 时间线负责“发生了什么”。
- 模式总结负责“为什么这不是孤立点，而是一种行为模式”。
- 如果用了时间线图、峰值图、趋势图，必须加一句“图示要点”。

---

## 7. 传播与关联分析

本节回答“事件是如何扩线的，哪些关联已经被证实，哪些还只是候选”。

建议字段：

- **扩线 pivot**：
- **已确认关联**：
- **候选关联**：
- **未完成独立验证的对象**：
- **当前关联边界**：
- **如本节带拓扑或关系图，图示要点**：
- **本节主要依据来源**：

建议写法：

- 明确区分：
  - 同资产重复通信
  - 同 IP / 同域名 / 同 JA3 / 同 JA4 / 同证书扩线
  - 已验证关联
  - 仅共享 pivot 的弱关联
- 不要把候选对象直接写成受害对象。
- 如果用了拓扑图，图示要点必须说明“哪些关系已证实，哪些只是结构性邻近”。

---

## 8. 影响分析

本节回答“当前已经确认影响到哪，风险体现在哪里”。

建议字段：

- **当前已确认影响**：
- **当前疑似影响**：
- **时间范围**：
- **资产范围**：
- **外部基础设施范围**：
- **当前攻击阶段**：
- **仍未确认的影响**：
- **本节主要依据来源**：

建议写法：

- 把“已确认”与“疑似”拆开。
- 即便当前结论较弱，这一节也建议保留，因为“尚未确认影响”本身就是重要交付。
- 对 `monitor_only` 场景，要明确写出“当前更像背景活动”的原因。
- 对 `confirmed_incident` 场景，要明确写出“为什么已经足以进入处置”。

---

## 9. 证据链摘要与观测缺口

本节回答“当前判断到底站在什么证据上，又被什么缺口限制住了”。

建议字段：

- **当前判断主要建立在**：
- **当前最强证据**：
- **当前最关键缺口**：
- **当前最多只能走到的判断层级**：
- **因缺口而不能写出的结论**：
- **下一轮最值得补的证据**：

建议写法：

- 这一节不要偷懒写成一句“还存在不确定性”。
- 要明确写：
  - 现在哪类证据最强
  - 还缺哪类证据
  - 缺口在限制哪条结论
- 这是参考报告最值得学习的精华之一。

---

## 10. 结论与后续建议

本节回答“最终判断是什么，为什么是它，不是别的状态”。

建议字段：

- **当前结论**：
- **当前状态**：`confirmed_incident` / `needs_review` / `monitor_only`
- **严重度**：
- **研判把握**：
- **一句话概括**：
- **为什么当前结论成立**：
- **为什么不是相邻状态**：
- **建议立即动作**：
- **短期排查动作**：
- **持续监控或复核动作**：
- **下一次复核最值得补的证据**：

建议写法：

- 建议动作最好按“立即 / 短期 / 持续”分层。
- 动作必须可执行，不能写成纯口号。

### 状态差异化写法

#### `confirmed_incident`

- 要解释闭环是如何形成的。
- 要解释为什么反证不足以推翻。
- 要给出处置动作，而不只是继续观察。

#### `needs_review`

- 要解释为什么目前仍然可疑。
- 要解释哪个关键 gap 还会改变结论。
- 要说明为什么现在不能直接 confirmed，也不能直接 monitor。

#### `monitor_only`

- 要解释为什么当前更像背景流量、共享基础设施或计划内活动。
- 要解释为什么没有足够强的正证据支持升级。
- 动作应偏向保留样本、监控复发、核实背景，而不是一律隔离。

---

## 11. 技术附录提示

正文最后只保留一句：

- 技术细节、IOC/IOA、关键对象清单、观测对照、引用来源、证据缺口与图表说明请见《事件调查技术附录》。

## 技术附录模板

`report_appendix.md` 建议承载下列内容。

### IOC / IOA 清单

| 类型 | 值 | 角色 | 状态 |
| -- | -- | -- | -- |
| IP |  | 核心外部基础设施 / 候选基础设施 | 已确认 / 待确认 |
| 域名 |  |  |  |
| URL |  |  |  |
| JA3 / JA4 |  |  |  |
| Hash / 文件 / 进程 |  |  |  |

### 关键对象清单

| 对象 | 类型 | 当前角色 | 是否已纳入证据链 | 备注 |
| -- | -- | -- | -- | -- |
|  | 资产 / 域名 / IP / 指纹 / 文件 / 账号 | 种子 / 核心 / 横向对比 / 扩展候选 | 是 / 否 |  |

### 观测引用对照

| 观测编号 | 来源工具 | 事实摘要 | 关系 |
| -- | -- | -- | -- |
| `obs-001` |  |  | `supporting` / `counterevidence` / `context` / `candidate` |

### 正文章节引用映射（推荐）

| 正文章节 | 主要 evidence / observation | 用途 |
| -- | -- | -- |
| 首页摘要 |  | 当前状态 / 当前最强证据 / 当前最关键缺口 |
| 第 5 节 |  | 主支撑证据 / 反证 |
| 第 9 节 |  | 证据链摘要 / 缺口 |
| 第 10 节 |  | 结论 / 动作 |

### 证据来源与参考资料

| 来源类型 | 来源名称 | 用途 | 边界 |
| -- | -- | -- | -- |
| 上游检测 |  | 说明规则 / 指纹 / 模型为何触发 | 命中本身未必等于事件成立 |
| 内部观测 |  | 支撑事实 | 仅覆盖本地可见范围 |
| 本地情报库 |  | 验证 IOC / 指纹 | 依赖库覆盖度 |
| 外部结构化情报 |  | 基础设施或家族补充 | 不能单独替代本地事实 |
| 外部网页 / 报告 |  | 背景解释 | 不等于本案直接证据 |

### 证据缺口与待补查询

| 缺口 | 限制的结论 | 当前是否可补 | 最值得补的查询 |
| -- | -- | -- | -- |
|  |  | 是 / 否 |  |

### 图表与拓扑解读（可选）

| 图表 / 拓扑 | 图示要点 | 能支撑什么 | 不能直接推出什么 |
| -- | -- | -- | -- |
|  |  |  |  |

### 可选扩展块

仅在数据可得时输出：

- 更细五元组或流量统计
- 主机侧取证细节
- 查询轨迹
- 页面正文摘录
- 可视化图表说明

## 不应写进主报告的内容

以下内容默认不直接进入 `report.md`：

- LLM 为什么选择某个 tool
- heuristic fallback 如何运转
- 内部 step budget、readiness gate、delivery gate
- prompt、tool schema、调试日志
- 过细的 source JSON
- 尚未进入证据链的原始中间态推断
- 反复试错的查询过程

## 生成约束

如果后续让 agent 自动生成报告，建议严格遵守：

1. 报告只能基于 `incident.json` 中已存在的事实，不得在成文阶段引入新结论。
2. 首页摘要、关键结论、建议动作必须与正文证据链一致，不能各写各的。
3. 每条关键结论都应能映射到 evidence / observation / source。
4. 证据块必须同时包含“事实、解释、边界”三层。
5. `confirmed_incident`、`needs_review`、`monitor_only` 必须使用不同语气与不同动作类型。
6. 关键对象必须带角色，不允许只列值不说明角色。
7. 观测缺口必须显式交付，不允许只在结尾含糊说“仍有不确定性”。
8. 如果正文用了图、时间线、拓扑或统计图，必须生成一句“图示要点”。
9. 如果某章节没有可靠输入，允许省略，但不能硬写。
10. 外部情报和公开报告只能作为背景、验证或解释，不能越权替代本地事件证据。
11. 建议动作必须可执行，且要与当前状态相匹配。

## 交付验收清单

交付前，至少用下面这份清单自检一次：

1. 读者是否能在首页摘要里快速看到当前状态、影响范围、最强证据、最大缺口和立即动作。
2. 主报告是否明确区分了“已确认”“疑似”“未确认”“当前未获取”。
3. 主报告是否至少给出一条反证或替代解释，而不是只堆正证据。
4. 第 3 节是否解释了为什么调查这些对象，而不是只列了一堆 IP / 域名 / JA3。
5. 第 9 节是否明确写出“当前最多只能走到哪一层判断”。
6. 第 10 节是否解释了“为什么是这个状态，而不是相邻状态”。
7. 正文里是否避免了 agent、tool、prompt、workflow、调试轨迹等内部实现细节。
8. 附录是否能把正文关键结论追溯到 observation / evidence / source。
9. 如果用了图表或拓扑，是否提供了“图示要点”和“不能直接推出什么”。
10. 报告整体是否更像面向运维协同的调查交付，而不是面向开发调试的系统日志。

## 反向约束 Agent 的最低数据需求

如果未来要让 agent 真正围绕这份模板调查，至少要稳定产出这些结构：

- `report_header`
  - 标题、分析窗口、事件标签等首页信息
- `executive_summary`
  - 当前状态、严重度、把握、最强证据、最大缺口、立即动作
- `background`
- `hypotheses`
- `scope_definition`
- `entity_registry`
- `coverage_plan`
- `mechanism_breakdown`
- `evidence_ledger`
- `counterevidence`
- `timeline`
- `relationship_analysis`
- `impact_assessment`
- `evidence_gaps`
- `decision_basis`
- `recommended_actions`
- `source_registry`
- `citation_map`
- `visual_takeaways`
- `appendix_payload`
- `section_presence`

其中：

- `citation_map` 用来保证正文关键结论都能追到 evidence / observation / source。
- `section_presence` 用来控制哪些章节应输出、哪些应合并、哪些应省略。
- `appendix_payload` 用来把正文不该承载的技术细节稳定地下沉到附录。

换句话说，这份模板不是 renderer 的文案要求，而应该成为 agent 的调查 contract。
