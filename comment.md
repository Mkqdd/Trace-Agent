## 针对 Plan 7 的架构级 Review 意见

### 1. 阶段排序错误导致“弱信号孤岛”：Seed Verify 不应早于 Incident Assemble
**问题设计**：在 Plan 7 提出的新主链（Main Pipeline）中，流程被设计为：
`seed alert -> seed verify -> incident assemble -> ...`
并且在 `Seed Verify` 阶段的任务是“判断：这条 seed alert 是否值得继续作为 incident 入口，是高置信安全信号、弱信号、还是明显误报”。
**批评意见**：
这是一个典型的违背真实安全运营（SOC）溯源逻辑的致命缺陷。在实战中，高级别针对性攻击（APT）早期的告警往往是非常边缘的“弱信号”（比如一次普通的疑似异常登录、一条仅命中了宽泛特征的内网外连记录）。如果你在没有任何上下文关联的情况下，仅凭这一条孤立的“弱信号”就执行 `Seed Verify` 并将其判定为“价值不大”或“误报”而丢弃，那么后续的 `Incident Assemble` 就永远没有机会被触发。
而事实往往是，如果把这个看似普通的弱信号和同时间段内该主机的异常 DNS 请求、横向移动探测等流量聚合在一起看，它就是一个高危事件的开端。
**修改建议**：
必须将 `Incident Assemble` 的时机前置到 `Seed Verify` 之前，或者将两者合并为 `Context-Aware Verify`。只有在利用 `TraceStore` 汇聚了同时间窗内的流量、日志和其他资产相关告警（即形成了 `IncidentBundle` 上下文）之后，才能准确判断这个种子告警是否值得深入调查。在缺乏聚合上下文的情况下对告警进行“弱信号”单点过滤，将导致系统丢失大量真实的复杂攻击事件。
