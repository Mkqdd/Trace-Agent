# 针对优化后 Plan 5 的复核意见

我看过了最新修改的 `plan5.md`。Codex 非常好地吸收了我提出的 3 点核心重构意见（保持 analysis 纯净引入 ViewModel、引入轻量 Synthesizer 替代单条规则翻译、处置建议双擎驱动）。

目前的计划大方向已经**完全没有问题**了，但在文档的细节上，似乎还遗留了一处前后矛盾的“历史包袱”，建议在落地时注意：

### 残留的矛盾点：Phase 5 还是在用规则翻译
在“设计原则”中，计划已经明确指出：**“3. 不让 Python 规则假装写报告，不继续扩展大量 `_localized_claim` 式的规则”**，并且在 Phase 4 引入了轻量 Synthesizer。
但是在“改造方向”中，依然保留了 **Phase 5: 页面 claim 转可读报告语言**（“而是转成更适合事件报告的中文摘要... 保守改写”）。

这在实施时会让人产生困惑：既然有了 Phase 4 让 LLM 去做高优证据的深度摘要合成，就不应该再花精力去完善 Phase 5 的单条证据“保守改写”了。

### 落地建议：
在实际写代码时，**直接忽略或砍掉 Phase 5**。
- 对于要在报告列表中罗列的单条证据，直接展示原始的 Claim 或 Snippet 即可（作为底层数据支撑）。
- 真正用来给用户看的、流畅的中文分析，完全交给 Phase 4 的 Synthesizer 生成的那段“深度分析摘要”来承担。这样能最大程度节省代码量，同时让报告的层次（人类读者的摘要 vs 机器提取的原始证据链）泾渭分明。

除了这个小瑕疵，这份计划已经非常完美。你们可以直接开始着手重构 `renderers/report_markdown.py` 了！