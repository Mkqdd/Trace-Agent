# Plan 4: 让报告真正写出内容

## Summary

当前 `agent-pro` 主线的核心问题不是“能不能生成报告”，而是报告正文过于依赖链接、标题和弱模板句，缺少对页面内容和结构化情报的有效总结。下一阶段保持 `plan 主链 + React 补查` 不变，优先提升对外报告质量：让高质量来源的页面内容、结构化情报结果和关键 claim 被稳定转成中文摘要，真正进入 `report.md`，同时默认隐藏内部缺口和系统术语。

## Key Changes

### 1. 在 analysis 中增加“可报告摘要”层

- 在最终 `analysis` 里新增稳定的摘要对象集合，作为报告正文的直接输入，而不是让 renderer 临时从 raw evidence 现场拼句子。
- 每条摘要对象至少包含：
  - `title`
  - `source`
  - `url`
  - `summary_zh`
  - `evidence_tier`
  - `section_hint`
- `summary_zh` 的生成优先级固定为：
  - 页面正文提取出的 claim / summary
  - 结构化情报返回的说明
  - 搜索 snippet 的保守改写
- 如果没有足够内容支撑，就不要生成摘要，避免为了“显得有内容”而编造。

### 2. 让页面内容真正进入报告

- 对高质量来源，优先生成 1 到 2 句中文摘要，不再只展示标题和 URL。
- 首批重点支持这些来源：
  - Malpedia
  - Microsoft
  - Trend Micro
  - Check Point
  - ThreatFox
  - URLhaus
  - VirusTotal
  - JA4DB / SSLBL
- 页面摘要必须体现“来源在说什么”，例如：
  - 该页面将某指标归入某家族
  - 该研究文章描述了该家族的行为特征
  - 该结构化情报记录说明某 IOC 与某恶意活动相关
- `family_intel`、`search_result`、`supplemental` 都需要进入统一的摘要化流程，避免继续由零散的 `claim` 或模板句主导正文。

### 3. 重写 report 的正文输入逻辑

- `关键证据与情报` 改为优先展示“可报告摘要对象”，不再直接消费原始 evidence。
- `相关情报参考` 降级为补充阅读，不再承担主要信息表达。
- `参考链接` 保留为末尾附录，只放去重后的 URL 列表。
- 默认隐藏内部缺口、未解决项和系统术语，不在对外报告里直接暴露 `gap`、`single_source`、`unresolved` 等内部概念。
- 只有在确实可能误导读者时，才用更温和的话语表达不确定性，例如“建议结合人工复核”。

### 4. 控制“写出内容”和“避免编造”的边界

- 如果抓到了页面正文且正文中有有效 claim，报告中必须写出中文摘要。
- 如果只有 snippet，没有正文，可以生成保守摘要，但语气必须弱于正文摘要。
- 如果正文和 snippet 都很弱，就不要强写内容，只保留链接或直接不展示该条。
- 对本地情报命中场景，要求优先补至少一条外部背景/确认摘要；否则报告会继续显得像“数据库命中回显”。

### 5. 小范围补上游，不重构主链

- 不改变 `agent-pro` 当前 `plan 主链 + React 补查` 的总体架构。
- 只补与“报告写内容”直接相关的上游收敛逻辑：
  - 页面正文/claim 到 `summary_zh`
  - 结构化情报结果到 `summary_zh`
  - evidence 去重与 section 归类
- 本阶段预期主要集中在：
  - `analysis` 收敛逻辑
  - `report_markdown` 渲染逻辑
  - 页面/claim/搜索结果的摘要化助手函数

## Test Plan

- 强命中 case：
  - JA4 / SSL_SHA1 命中且存在外部页面时，报告正文必须出现至少 1 条中文内容摘要，而不是只剩链接。
- 弱证据 case：
  - 只有 snippet 时，报告可以写保守摘要，但不能写成确定性结论。
- 空结果 case：
  - 没有可靠页面内容时，报告不能编造摘要。
- 去重验证：
  - 同一来源不能同时在“关键证据”“相关情报参考”“参考链接”里重复刷屏。
- 展示策略：
  - 默认不显示内部 gaps / unresolved / 系统术语。
- 回归验证：
  - 重新跑当前 `agent-pro` 主线的功能 smoke，确认 `analysis / report / topology` 仍然正常生成，且报告正文比现状更充实。

## Assumptions

- 当前以 `agent-pro` 为默认主线，不继续把 `agent_react` 作为近期交付方向。
- 当前最优先目标是提升对外报告质量，而不是继续重构主链架构。
- 页面内容摘要遵循“高质量来源优先、保守生成、绝不编造”的原则。
- 内部缺口信息保留在 `analysis` 中，对外报告默认隐藏，仅在必要时以温和方式表达风险和不确定性。
