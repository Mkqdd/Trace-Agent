# Plan 5: 报警报告成品化优化

## 当前判断

当前主线已经能稳定生成 `analysis.json -> report.md`，但报告的主要问题不再是“能不能生成”，而是“生成出来像不像一份成熟的报警报告”。

结合当前默认输出中的代表性样例：

- [000/report.md](/home/estar0x/project/maltrail_test/Trace-Agent/outputs/agent_runs/default/000/report.md)
- [002/report.md](/home/estar0x/project/maltrail_test/Trace-Agent/outputs/agent_runs/default/002/report.md)
- [004/report.md](/home/estar0x/project/maltrail_test/Trace-Agent/outputs/agent_runs/default/004/report.md)
- [005/report.md](/home/estar0x/project/maltrail_test/Trace-Agent/outputs/agent_runs/default/005/report.md)

当前报告已经具备：

- 基本事件概述
- 家族归因
- 关键来源链接
- 基础处置建议

但它还不够像真正的“告警研判报告”，更像：

- 家族背景资料汇编
- 多条来源摘要的模板拼接
- 归因成立后的参考页集合

需要特别避免的一种退化形式是：

- 每条证据只剩“空泛一句总结 + 一个链接”

Plan 5 的落地要求之一，就是让进入正文的高价值来源真正写出“来源里说了什么”，而不只是挂链接。

---

## 核心问题

### 1. 报告重心偏向“家族背景”，不够聚焦“当前事件”

当前很多内容在解释：

- 这个家族是什么
- 哪些公开页面提到了这个家族

但对下面这些“事件特异性问题”写得还不够：

- 当前这条告警为什么成立
- 当前源主机 / 目的地址意味着什么
- 当前命中指标和该家族之间是怎样的直接关系
- 这条流量对排查和阻断意味着什么

结果是报告像“家族介绍页”，不像“围绕单条告警的分析报告”。

### 2. 同一证据被重复渲染

同一来源经常在多个章节重复出现：

- 关键证据与情报
- 本地指纹情报
- 相关情报参考
- 参考链接

这会导致：

- 报告看起来很长
- 但真正新增信息有限
- 阅读体验像在反复看同一条证据

当前最典型的重复包括：

- 本地指纹情报既出现在正文证据区，又单独出现一节
- `相关情报参考` 和前面的主证据区使用了相同来源
- `参考链接` 只是把前面已经出现过的来源再列一次

这类重复不只是“看起来啰嗦”，而是会直接削弱报告的结构感。

### 3. 页面内容没有被真正“消化”为报告语言

当前很多证据说明虽然已经不只是裸链接，但仍然存在：

- 英文 snippet 残留
- 模板化转述
- 页面 claim 直接拼接

缺少“分析师语言”的加工：

- 这条页面到底支持了什么判断
- 它对本次事件意味着什么
- 它是直接证据、补充背景，还是低权重参考

### 4. 处置建议仍然偏通用模板

现在很多建议大致都是：

- 隔离源主机
- 监控目标地址
- 横向检索命中指标
- 结合家族 TTP 排查

这些建议方向不算错，但缺少：

- 指标类型驱动的差异化建议
- 事件上下文驱动的优先顺序
- 更贴近 SOC/IR 实操的动作表述

---

## 上游证据收集是否足够

### 结论

对于强命中样例：

- `JA3`
- `JA4`
- `SSL_SHA1`

当前证据收集已经足以支撑“生成一份像样的报告主干”。

但对于“高质量成品报告”来说，还存在一个结构性问题：

> 当前收集到的证据更多是“家族背景页”，而不是“事件直接证据”。

也就是说：

- 现在不是完全没证据
- 而是证据的类型偏背景资料
- 对报警报告最关键的“事件解释力”还不够强

### 当前证据收集的不足

1. 直接支持“当前事件”的证据不够靠前
2. 目的地址 / 基础设施上下文经常写得不够
3. 页面中的有效 IOC / TTP / 处置线索没有被充分提取到报告层
4. 家族背景来源很多，但事件关联来源不够突出

---

## 设计目标

下一阶段不是继续扩主架构，而是把报告从：

> “可以生成”

提升到：

> “适合展示、适合研判、适合交付”

所以 Plan 5 的目标是：

1. 报告优先讲清楚这条告警，而不是先讲家族百科
2. 证据去重，减少重复渲染
3. 让报告拥有一段真正自然的中文分析摘要
4. 让建议更贴近事件和威胁性质
5. 高价值证据在正文中必须体现其关键内容，而不是“标题/一句空话 + 链接”

---

## 设计原则

### 1. `analysis.json` 继续保持单一事实源，不混入展示层字段

`analysis.json` 仍然只保存：

- 事件事实
- 证据集合
- 归因判断
- 处置相关事实

不在其中新增：

- `report_ready_sections`
- `summary_zh`
- `report_weight`
- 其它仅用于排版和展示的字段

这些内容应该在 `renderers` 模块执行期生成，而不是提前污染 SSOT。

### 2. 报告视图在 renderer 内部通过 `ReportViewModel` 组装

推荐在 `renderers/report_markdown.py` 中引入中间视图层，例如：

- `ReportViewModel`
- `EventEvidenceView`
- `AttributionSupportView`
- `ThreatHuntActionView`

由 renderer 负责把 `analysis` 中的纯事实：

- 提取
- 去重
- 分层
- 排序
- 转换成适合渲染的视图对象

然后再由模板或字符串拼装生成 Markdown。

### 3. 不让 Python 规则假装“写报告”

不继续扩展大量 `_localized_claim` 式的规则，把每条页面 claim 靠 if-else 硬翻成中文。

更稳的路线是：

- 代码负责筛选高质量证据
- 代码负责确定章节结构和证据顺序
- 一个轻量的 Synthesizer LLM 只负责对高优证据做一次性总结

这样可以避免：

- 报告继续充满模板味
- 代码分支越来越臃肿
- 每条证据都单独调用 LLM 导致时延爆炸

---

## 改造方向

### Phase 1: 在 renderer 内引入 `ReportViewModel`

不修改 `analysis` 结构，先在 `renderers/report_markdown.py` 内新增视图模型构建步骤。

目标：

- 从纯 `analysis` 中提取可报告事实
- 做证据去重和章节分层
- 为最终模板提供稳定输入

建议的视图对象可包含：

- `event_overview`
- `event_evidence`
- `attribution_support`
- `background_references`
- `reference_links`
- `base_actions`
- `threat_hunt_actions`

但这些对象只存在于 renderer 运行期，不回写到 `analysis.json`。

### Phase 2: 事件直接证据优先

报告默认结构要调整为：

1. 事件概述
2. 研判结论
3. 事件直接证据
4. 归因依据
5. 家族背景与补充参考
6. 处置建议
7. 参考链接

其中：

- “事件直接证据”优先展示当前指标、目标地址、结构化命中、外部验证结果
- “家族背景与补充参考”单独后置

同时增加一条 section 归属规则：

- 每条证据只能有一个“主归属章节”
- 不允许同一来源在“关键证据”“本地指纹情报”“相关情报参考”“参考链接”中重复担任正文角色

### Phase 3: 证据去重和证据分层

同一来源不应在多个章节反复刷屏。

建议引入更明确的报告级证据层次：

- `primary_event_evidence`
- `attribution_support`
- `background_context`
- `low_priority_reference`

渲染时优先：

- 同一来源只保留一次主表述
- 其余章节仅保留简短引用或完全省略

并增加一个明确约束：

- 进入正文主证据区的来源，必须附带“内容型摘要”
- 只有低优参考区的来源，才允许简化成标题/链接展示

这里再补三条更具体的去重规则：

1. 本地指纹情报去单独章节化

- 如果本地指纹命中已经进入“事件直接证据”或“归因依据”，则不再单独保留 `本地指纹情报` 章节
- 本地库更适合被当作一条高权重证据，而不是正文里重复出现两次

2. `相关情报参考` 只保留非重叠背景来源

- 如果某来源已经进入高优正文证据区，就不再进入 `相关情报参考`
- `相关情报参考` 只用于承载那些没有进入主证据区、但仍有补充价值的背景来源

3. `参考链接` 默认收缩为附录或直接移除

- 如果链接已经在正文证据项中出现，则不再在 `参考链接` 重复列出
- `参考链接` 只保留“未在正文显式出现、但值得补充阅读”的少量链接
- 如果当前报告没有这类增量链接，则整个 `参考链接` 模块可以不显示

### Phase 4: 引入轻量 Synthesizer，总结高优证据

不让 LLM 自由生成整篇报告，也不让 Python 逐条假装写分析。

改为在 renderer 内增加一个极轻量的终态合成步骤：

- 输入：去重后的 Tier 1 / Tier 2 高优证据集合
- 输出：2 到 3 句连贯、专业、保守的中文“深度分析摘要”

建议提示词目标：

- 总结这些证据如何共同支持当前事件结论
- 点明哪些是直接事件证据，哪些是归因支持
- 不编造 analysis 中不存在的新事实

这一步的定位是：

- 结构由代码控制
- 灵魂由 LLM 轻量补足

### Phase 5: 单条证据保持“原始支撑”，不再做逐条翻译规则

在引入 Synthesizer 之后，不再为单条证据额外建设复杂的中文改写规则。

对列表型证据，保留其作为“底层证据链”的角色即可：

- 标题
- 来源
- URL
- 原始 claim / snippet / 简短说明
- 证据层级

但这里要区分两类展示：

1. 正文中的高优证据

被选入“事件直接证据”或“归因依据”的来源，不能只是：

- 一句空泛总结
- 一个 URL

而应至少体现 1 到 2 条真正有信息量的关键内容，例如：

- 该来源如何将当前指标与家族关联
- 该页面提到了哪些 TTP / IOC / payload / 基础设施线索
- 该来源对本次事件判断的直接支持点是什么

也就是说，高优证据项在正文中的推荐形态应该是：

- 来源标题
- 关键内容摘要
- 参考链接

2. 低优参考区

只有进入“家族背景与补充参考”或“参考链接”区的来源，才允许退化成：

- 简短说明
- 链接列表

真正面向读者、流畅自然的中文分析，统一由 Phase 4 的 Synthesizer 输出承担。

这样做的好处是：

- 减少 Python if-else 翻译逻辑
- 保持证据链原貌，方便复核
- 让“读者摘要”和“底层证据”边界清晰
- 避免继续堆出模板味很重的句子

但要注意：

- “保留原始支撑”不等于“重复复制到多个章节”
- 底层证据链仍要遵守 Phase 3 的 section 去重规则

### Phase 6: 处置建议改为“双擎驱动”

把建议从“家族通用模板”改成“双擎驱动”：

1. 基础响应：按指标类型生成

例如：

- JA3 / JA4：侧重横向检索相同 TLS 指纹、同 JA4 会话、相似客户端模式
- SSL_SHA1：侧重相同证书指纹的历史通信、证书复用资产、目的端追踪
- IP / DOMAIN / URL：侧重基础设施、投递链、额外 IOC 和关联资产扩线

2. 威胁猎捕动作：直接挂钩在已提取的 TTP / IOC / 行为线索上

例如：

- 如果补查阶段拿到了“计划任务持久化”，则动态生成“排查计划任务异常项”
- 如果拿到了“注入进程 / C2 beacon / DLL side-loading”等线索，则生成对应的主机或网络侧排查动作

建议里要尽量体现：

- 为什么做
- 对当前事件有什么帮助
- 优先级如何排序

---

## 不建议做的事情

### 1. 不优先继续扩大 report 的自由生成比例

当前已经验证：

- 完整 `analysis -> LLM写整篇报告` 路径不稳定

所以不建议回到“让 LLM 自由写整篇报告”为主的路线。

### 2. 不把展示逻辑写回 `analysis.json`

不建议把下面这些展示层对象塞回 SSOT：

- 摘要中文文案
- 报告章节结构
- 渲染权重
- 纯展示排序字段

### 3. 不继续扩展单条证据翻译规则

既然已经采用轻量 Synthesizer 来生成整体“深度分析摘要”，就不再继续投入精力去做：

- 每条 claim 的中文模板化转述
- 大量 `_localized_claim` 风格规则
- 对英文 snippet 的逐条润色逻辑

单条证据更适合作为“可核验支撑材料”存在，而不是每条都被代码硬翻成分析师口吻。

### 4. 不优先继续扩主架构

当前问题已经不是：

- 主链不通
- gap fill 不存在
- 工具系统太弱

当前问题主要在：

- 报告信息组织
- 证据层次
- 报告语言加工

### 5. 不保留没有增量信息的章节

如果某个章节只是在重复前文已经出现过的证据或链接，那么它就不应该继续存在。

尤其是：

- 仅重复本地指纹命中的 `本地指纹情报`
- 与主证据区高度重叠的 `相关情报参考`
- 没有新增链接价值的 `参考链接`

---

## 建议实施顺序

1. 在 renderer 内引入 `ReportViewModel`
2. 重写 `report_markdown.py` 的章节输入逻辑
3. 做证据去重与章节分层
4. 加入轻量 Synthesizer 生成“深度分析摘要”
5. 收缩单条证据展示，保留原始支撑信息
6. 确保高优来源以“关键内容摘要 + 链接”形式进入正文
7. 清理重复章节，落实“一条证据一个主归属”
8. 让“事件直接证据”优先于“家族背景”
9. 把处置建议升级成“双擎驱动”

---

## 验收标准

优化后，强命中样例报告应满足：

1. 不再像“家族资料页”
2. 不再大量重复同一来源
3. 关键证据摘要大多为中文、自然、可读
4. 至少能清楚回答：
   - 告警为什么成立
   - 为什么关联到该家族
   - 当前应该先做什么排查
5. 单条证据列表不再充满模板化中文硬翻
6. 正文里的高优证据不再退化成“空泛一句总结 + 链接”
7. 本地指纹情报不再在正文中重复出现两次
8. `相关情报参考` 和 `参考链接` 只在有新增信息时才显示
9. `004 / 005 / 002` 这类样例在阅读体验上明显优于当前默认输出

---

## 一句话总结

Plan 5 的目标不是让 report “更长”，而是让它：

> 更像一份围绕单条告警的专业研判报告。
